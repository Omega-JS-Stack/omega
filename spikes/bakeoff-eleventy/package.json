{
  "name": "@omega.js/bakeoff-eleventy",
  "version": "1.0.0",
  "description": "Bench harness for the winning bake-off candidate — builds the shared corpus through @omega.js/web (the engine core promoted from this spike in Phase B1); cold benches + watch-mode incremental probes",
  "private": true,
  "main": "./eleventy.config.js",
  "scripts": {
    "build": "node src/build.js",
    "build:ssg": "npx @11ty/eleventy",
    "dev": "npx @11ty/eleventy --serve",
    "bench": "node src/bench-run.js"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@omega.js/bakeoff-shared": "*",
    "@omega.js/web": "*"
  },
  "engines": {
    "node": ">=22"
  },
  "author": "ITW Creative Works",
  "license": "MIT"
}
