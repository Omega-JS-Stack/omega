/**
 * SITE-layer index page module — overrides the core layer's index.js
 * (3-layer page-module resolution: site > theme > core).
 */
console.log('[omega:index] site layer module — consumer wins');
