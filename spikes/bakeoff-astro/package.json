{
  "name": "@omega.js/bakeoff-astro",
  "version": "1.0.0",
  "description": "Bake-off candidate 2: Astro building the shared corpus with @omega.js/template-kit as direct helper imports — validates theme layering via an omega: Vite resolver, content collections, and the full A1 slice",
  "private": true,
  "scripts": {
    "test": "node --test --test-concurrency=1",
    "build": "node src/build.js",
    "build:ssg": "npx astro build",
    "dev": "npx astro dev",
    "bench": "node src/bench-run.js"
  },
  "dependencies": {
    "@omega.js/bakeoff-shared": "*",
    "@omega.js/config": "*",
    "@omega.js/template-kit": "*",
    "@omega.js/web": "*",
    "astro": "^5.0.0",
    "gray-matter": "^4.0.3",
    "liquidjs": "^10.27.1",
    "markdown-it": "^14.3.0"
  },
  "engines": {
    "node": ">=22"
  },
  "author": "ITW Creative Works",
  "license": "MIT"
}
