---
# The sandbox brand is a FIXTURE, not a site: one honest page saying so, and
# the /e2e page the cross-stack lane drives. `core/root` is the bare layout —
# the shell, the head, and the asset scripts, with no marketing chrome to
# maintain in a brand nobody reads.
layout: core/root
permalink: /
meta:
  title: "Sandbox Brand"
  description: "Permanent dogfood consumer for the OMEGA monorepo. Nothing here is real."
---

<main class="container py-5">
  <h1>Sandbox Brand</h1>
  <p>Permanent dogfood consumer for the OMEGA monorepo. Every value in this brand is fake by design, and its backend is an offline <code>demo-*</code> emulator project.</p>
  <p>The cross-stack e2e lane drives <a href="/e2e">/e2e</a>.</p>
</main>
