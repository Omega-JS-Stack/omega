{
  "name": "omega",
  "version": "0.0.0",
  "description": "OMEGA monorepo — @omega.js framework packages, shared internals, spikes, and sandbox apps",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node scripts/watch-all.js",
    "test": "npm run test:packages && npm run test:corpus && npm run test:e2e && node scripts/e2e-journey.js",
    "test:packages": "node --test scripts/*.test.js && node scripts/test-workspaces.js",
    "test:corpus": "node scripts/corpus-shapes.js && npm run test:backend -w sandbox-brand",
    "test:e2e": "npm run test -w sandbox-brand",
    "test:journey": "node scripts/e2e-journey.js",
    "release:check": "node scripts/release-check.js"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@omega.js/devkit": "*"
  }
}
