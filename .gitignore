# ========== Default Values ==========

.DS_Store
node_modules/
/dist/
/release/
/.cache/
/.omega/
/.claude/
.env
*.log
/logs/

# Apple signing artifacts — never commit these
config/certs/
*.p12
*.cer
*.mobileprovision
*.p8
*.pem

# ========== Custom Values ==========
.env.*
logs/
.temp/
.cache/
.omega/
firebase.resolved.json
_attic/
.workkit/*
!.workkit/settings.json

# Rendered per rehearsal by scripts/playground-desktop-rehearsal.js: it lives in
# the private rehearsal snapshot repo only, never in this tree.
/.github/workflows/playground-desktop.yml
