// The options context: ONE ready-made instance, initialized by the consumer's entry
import { Omega } from './page-context.js';

const omega = new Omega('options');

export default omega;
export { Omega };
