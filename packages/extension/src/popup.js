// The popup context: ONE ready-made instance, initialized by the consumer's entry
import { Omega } from './page-context.js';

const omega = new Omega('popup');

export default omega;
export { Omega };
