{
  "name": "@omegajs/account",
  "version": "1.0.0",
  "description": "The OMEGA user/account schema engine + subscription resolution — the single source of truth shared by @omegajs/backend and web-manager (private workspace package, vendored/bundled into frameworks at prepare time)",
  "private": true,
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./schema": "./src/schema.js",
    "./engine": "./src/engine.js",
    "./subscription": "./src/subscription.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "author": "ITW Creative Works",
  "license": "MIT"
}
