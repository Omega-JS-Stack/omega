# GENERATED output — prepare-package rebuilds dist from src on install/pack/publish
/dist/

# GENERATED — the prepare-time docs lane (@omega.js/devkit tools/vendor-docs.js)
# vendors the monorepo guide + docs/shared/ into the shipped package (#64)
/docs/
# …plus the Claude plugin + its marketplace, vendored for consumer brands (#62)
/claude-plugin/
/.claude-plugin/
