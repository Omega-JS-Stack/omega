{
  "name": "backend-fixture",
  "version": "0.0.1",
  "description": "@omega.js/backend self-test fixture app (emulator-only, demo project) — src/dist pillar shape: this manifest + src/ are authored, functions/ is staged",
  "private": true,
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@omega.js/backend": "file:../../../..",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  }
}
