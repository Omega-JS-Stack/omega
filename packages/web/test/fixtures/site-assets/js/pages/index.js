/**
 * SITE-layer index page module — a consumer-supplied entry the core layer
 * doesn't ship (core's homepage behavior rides the §7 section lane).
 */
console.log('[omega:index] site layer module — consumer wins');
