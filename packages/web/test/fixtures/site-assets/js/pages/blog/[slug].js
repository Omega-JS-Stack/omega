/**
 * SITE-layer wildcard page module — overrides the core layer's
 * js/pages/blog/[slug].js (3-layer page-module resolution: site > theme >
 * core, wildcard filenames included).
 */
console.log('[omega:blog-slug] site layer wildcard module — consumer wins');
