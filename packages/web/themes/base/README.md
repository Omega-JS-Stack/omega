# base

The structural markup layer every theme chain ends at: layouts, sections, includes, and components for all shared surfaces.
It owns the neutral `omega-*` BEM vocabulary; themes are skins (scss over these selectors) and fork markup only for identity surfaces.
Convention: `docs/web/sections.md` § "Markup convention: base, skin, fork" (#177).
