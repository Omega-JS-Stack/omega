{
  description: 'Serif display hero — rotating italic second line, ink CTA + mono command, optional stat cards, floating product frame, optional demo capture (input/form/custom). The classy homepage opener.',

  // Top-level arg contract (validation is warn-only; names are API — renames
  // follow deprecation discipline, spec §6)
  args: {
    badge: 'object',
    headline: 'string',
    headline_accent: 'string',
    rotating: 'array',
    description: 'string',
    primary_button: 'object',
    command: 'string',
    command_href: 'string',
    secondary_button: 'object',
    meta: 'array',
    cards: 'array', // stat cards under the hero ({ number, label, icon? }) — the marketing/stats vocabulary, no default: brand content is never a theme's voice
    frame: 'object',
    demo: 'object',
    demo_html: 'html', // finished markup for the demo position ({% slot demo_html %}) — replaces the typed demo lanes when set
  },

  // Every key consumer-overridable; string values liquify at the call site
  // (so {{ site.brand.name }} resolves against the calling page's scope).
  defaults: {
    badge: {
      text: 'Introducing {{ site.brand.name }}',
      href: null,
    },
    headline: 'The platform for',
    rotating: [
      'modern business',
      'shipping faster',
      'scaling your team',
    ],
    description: 'Everything you need to launch, measure, and grow: auth, payments, analytics, and every surface of your product in one place.',
    primary_button: {
      text: 'Get started free',
      href: '/signup',
    },
    // No command default (pass B): the mono $ button renders only when the
    // BRAND sets one — a framework command is never a theme's voice.
    secondary_button: {
      enabled: false,
      text: 'See pricing',
      href: '/pricing',
    },
    meta: [
      'Free plan',
      'No credit card required',
      'Set up in minutes',
    ],
    frame: {
      enabled: true,
      title: '{{ site.brand.name }} · Overview',
    },
    demo: {
      enabled: false,
      type: 'input',
      options: {
        class: 'mw-md',
        placeholder: 'Enter your email',
        name: 'email',
        input_type: 'email',
        required: true,
        redirect: '/dashboard/account',
        subtext: 'No credit card required',
        button: {
          text: 'Start free trial',
          class: 'btn-adaptive',
        },
      },
    },
  },

  // Showcase variants (spec §9) — args ride the data bridge over the
  // defaults above, exactly like a consumer composition.
  demo: [
    { label: 'Default' },
    {
      label: 'Email capture',
      args: {
        demo: { enabled: true },
      },
    },
    {
      label: 'No frame, second button',
      args: {
        frame: { enabled: false },
        secondary_button: { enabled: true },
      },
    },
  ],
}
