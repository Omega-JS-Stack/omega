# Dogfood arc — C1–C5 + D13 → the OMEGA brand on all four targets

> Status: **ACTIVE (opened cp92, 2026-07-11)** — the execution plan for PROGRESS Next #1. Binding spec: [omega-core-changes-inbox.md](omega-core-changes-inbox.md) §C (C1–C5) + decisions D7/D8/D9/D10/D11/D13. This doc = survey findings + checkpoint slicing; per-checkpoint detail lands in CHANGELOG entries as usual. Exit criterion for the whole arc: **the OMEGA brand runs on all four targets via `omega dev --local`, built from a wizard-scaffolded brand, themed once, deployed deliberately** — then brand rebuilds (somiibo → sweet-saucy) start.

## Survey — where each slice actually stands (verified 2026-07-11)

**C1 (template/onboarding).** `omega-manager onboard` (cp58+60+91) scaffolds the brand ROOT only: `config/omega.json5`, root `package.json`, `.gitignore`, `.env` stub (crypto-provisioned omega keys + placeholder external keys, D14), `README.md`, and a bare `apps/<dir>/package.json` per target ([scaffold.js](../packages/manager/src/lib/scaffold.js) `buildScaffoldPlan`). App INTERIORS come from each framework's own setup (web: [scaffold/](../packages/web/scaffold/) — CLAUDE.md, `_.env`, `config/omega.json5`, `src/`, `.github/workflows/build.yml`). 8 services have interactive config-flows (cp60). **Gaps**: the chain has never run end-to-end for real (fixtures only); no cloud-setup walkthrough (which consoles, which keys, in what order); no template repo; the scaffolded README is a stub.

**C2 (blueprint/pricing).** More done than the spec assumed: `payment.products` is in the shared schema (backend-shaped catalog), the engine composes `site.web_manager.payment.products` from config `payment` ([engine.js:80](../packages/web/src/engine.js)), and classy's pricing layout loops it (themes/classy/_layouts/frontend/pages/pricing.html:686,732; core resolve-plan include). The manager has NO pricing dispersal (only chatsy, which correctly GENERATES from `payment.products`). **What remains**: the pricing layout still carries frontmatter knobs; every default page rides the UJM `### ALL PAGES ###` / `### REGULAR PAGES ###` section-marker frontmatter convention (the dispersal-era format — dead protocol, live syntax); blueprint layers (`defaults/pages/*.md` → `core/_layouts/blueprint/*` → theme `frontend/pages/*`) each carry frontmatter that belongs in templates. KEEP (decided): 3-layer default/theme/consumer JS+CSS, legal append/addendum, virtual default pages with same-URL override (D8, no eject).

**C3 (redesign).** Themes today: `_template`, `bootstrap`, `classy` (base — layer chain is `active → classy → core`), `neobrutalism`, `newsflash`. License to FULLY BREAK granted; cross-target core css/js from day one (D10); two-tier consumer story (override stock theme at main.css level / author full theme on core baked CSS+JS).

**C4 (cross-target sharing).** `@omega.js/client` is already the shared runtime singleton. Not shared yet: form-manager.js (web-only), ads (Jekyll adunit includes; `vert.js` hardcodes `https://promo-server.itwcreativeworks.com` + `brand.id === 'promo-server'` dev check), fontawesome, analytics module, and the theme itself ("brand theme once, all three targets" = acceptance test).

**C5 (test scoping).** Per-app `omega test` exists (web: build + smoke + consumer `test/`). Missing: the brand-root dispatcher — bare `npx omega test` = project tests only; `framework:`/`omega:` prefixes = framework suites; `full:` = both; `brand:`/`project:` aliases. Folds parked 1.2a (backend test path filter misses corpus paths).

**D13 (deliberate deploys).** The autopublish model is ALIVE in the new stack: scaffolded [build.yml](../packages/web/scaffold/.github/workflows/build.yml) triggers on push-to-main and deploys to gh-pages; `omega deploy` = build + `npu sync` (push IS deploy). Manager update service is install+build only (bump/deploy/sync unported — [update/index.js](../packages/manager/src/services/update/index.js)). D9's addendum binds the fix: ONE executor reachable from CLI / HTTP / CMS; artifacts that can't auto-publish → GitHub releases.

**Dogfood substrate.** `omega dev --local` works (cp69); sandbox brand ([apps/sandbox-brand](../apps/sandbox-brand)) nests as a workspace with backend+website only — the four-target brand is new coverage. Port allocation (N7) and personas/e2e harness (N6) are done and waiting for exactly this.

## Checkpoint slicing (order = D10's decided sequence; C5 pulled early as arc DX)

**cp93 — Scaffold the OMEGA brand (arc opener + C1 survey instrument).** Run the REAL wizard end-to-end to create `apps/omega-brand/` (in-monorepo sibling of sandbox-brand — see calls below) with ALL FOUR targets on current classy (D10: dogfood scaffolds on current classy, redesign lands later). Fill app interiors via each framework's setup, `omega dev --local`, all four targets boot. Deliverable besides the brand: a **first-run friction log** (every wart, gap, confusing prompt, missing default) — this becomes C1's work list. *Acceptance: four targets serve/build from one wizard-born brand; friction log committed.*

**cp94 — C5 test-scoping DX.** Early because the arc itself lives in `omega test` from here on. Brand-root dispatcher semantics (`bare` = project, `framework:`/`omega:`, `full:`, `brand:`/`project:` aliases) + fix 1.2a. *Acceptance: from omega-brand root, bare run touches zero framework suites; prefixed runs scope exactly; devkit/manager tests green.*

**cp95 — C1 first-run product polish.** Burn the friction log: scaffolded README becomes the shipfa.st-grade walkthrough (exact consoles, exact keys, exact order — mirrors the .env stub's placeholders), config-flow gaps closed, onboarding output polished. Template-repo SHAPE finalized in-tree (`the polished scaffold IS the template`); actual GH-org template repo cut at arc close, not before. *Acceptance: a fresh onboard run needs zero tribal knowledge; every .env placeholder is documented where the user meets it.*

**✅ SHIPPED as cp97 (2026-07-11; cp96 went to Ian's translation order) — C2 blueprint/pricing rethink.** Pricing 100% from `payment.products` (kill remaining layout frontmatter knobs); retire the `### ALL PAGES ###` marker convention across defaults/blueprints (theme logic → templates; frontmatter = consumer surface only); slim the blueprint layer. KEEP list untouched (3-layer JS/CSS, legal append, D8 virtual pages). *Acceptance MET: pricing renders from config alone on omega-brand (live build: 2 buttons = premium+launch-kit, zero fiction) + sandbox (real catalog through the engine — its website app is a client harness with no Eleventy pages, so the engine was driven directly); 167 files swept + 3 emitters silenced + contract guard test pins the convention dead; web 80/80.*

**✅ SHIPPED as cp98 (2026-07-11) — D13 deliberate deploys.** Scaffolded workflows lose push triggers (workflow_dispatch + repository_dispatch); `omega deploy` becomes the explicit verb → ONE executor (locally: build; remotely: dispatch the SAME build via gh CLI/HTTP — D9 addendum); desktop/extension artifacts → GH releases channel; backend admin post route gains `deploy: true` default (content-publish implies deploy; code/config commits deploy nothing). *Acceptance MET: omega-brand workflows regenerated trigger-free (push deploys NOTHING); `omega deploy --dry-run` prints the exact dispatch on web/extension/desktop; devkit executor unit-tested (7); BONUS live proof: `omega deploy --only hosting` shipped omegajs-playground.web.app (200) — a real deliberate deploy on Spark. Live Actions runs stay Ian-gated (runner minutes); functions deploy stays Blaze-gated. docs/deploys.md is the contract.*

**cp99+ — C3 classy redesign (multi-checkpoint).** Front-load a **design brief for Ian** (typography/palette/motion directions — the one genuinely-Ian surface) while systems work proceeds: (a) cross-target core tokens/primitives (core css/js consumable by desktop/extension, D10), (b) two-tier theme mechanics (stock-theme override at main.css level; full themes on core baked CSS/JS), (c) default pages + blueprints reskinned, (d) auth/account/payment surfaces, (e) omega-brand becomes the showcase. Full break of the current theme system licensed. *Acceptance: omega-brand site is the visible proof; default pages auto-match theme; a toy second theme proves both consumer tiers.*
*Systems progress: brief + boards OUT cp99 ✅ · (a) tokens + brand ramp cp101 ✅ · (b) two-tier mechanics cp102 ✅ (toy theme proves both tiers) · app-shell mechanics cp103 ✅ (regions/states + purge safelist). Remaining (c)/(d)/(e) = the SKIN pass — blocked on Ian's board notes (still analyzing, 2026-07-11); C4 proceeds first per Ian.*

**cp100 — manage-cycle dogfood ✅ SHIPPED (inserted, Ian's nudge 2026-07-11).** The other half of the dogfood: omega-brand through `omega-manager manage`. Dry-run walks all 25 services with honest per-service skip reasons (the wiring map Ian asked for); two finds fixed same-checkpoint (friction #22 porcelain path clip, #23 projectId derivation from cloud.config); `firebase.billingAccount` configured so Blaze lands via the real ensure-step. LIVE run gated on Ian: GOOGLE_CLIENT_ID/SECRET into the brand .env + the first-run OAuth consent click → then firebase reconcile (Blaze, SA, sdk-config, auth, Firestore…) + the functions deploy proof. *Acceptance: firebase service completes live against the playground.*

**cp10x — C4 cross-target sharing. OPENED cp103+ (Ian's go 2026-07-11) — survey + slicing in [c4-cross-target.md](c4-cross-target.md).** Theme-once-all-targets (the acceptance test), shared forms module, shared ads module (role-keyed `advertising.*` config, ad TYPE param, inhouse fallback URL de-ITW'd into config), fontawesome + analytics modules; revisit `@omega.js/themes` packaging once C3 stabilizes. Survey headline: bootstrap+classy TRIPLICATED and drifting, FA implemented 3 incompatible ways, GA4 written 2–3×, `advertising` unschema'd with ITW promo-server hardcodes. Slices: cp104 token plumbing → cp105 advertising/de-ITW → cp106 analytics engine → cp107 forms → cp108 fontawesome → cp109 theme-once acceptance (with/after skin). *Acceptance: omega-brand desktop + extension render the brand theme + shared modules with zero copy-paste.*

**Arc close — the dogfood proof.** Full matrix on omega-brand: dev (all four, `--local`), build, `omega test` scoping, e2e harness (N6 personas) against the brand, deploy dry-runs; cut the template repo in Omega-JS-Stack from the polished scaffold; graduate PROGRESS Next #2 (somiibo rebuild) to Now.

## Calls made in this plan (flag if wrong)

1. **omega-brand lives IN the monorepo** (`apps/omega-brand/`, sibling of sandbox-brand) for the whole arc — rides local links, zero external state, no publishes. The real `omega.js` site repo + template repo get cut from it at arc close (org repo creation is granted, but there's nothing to gain from external repos mid-arc).
2. **C5 jumps the queue** (before C1 polish) — pure DX, no design dependency, and every later checkpoint runs tests from the brand root.
3. **D13 live CI proof is Ian-gated** like all runner-minute spends; everything else in D13 lands on schedule.
4. **C3 opens with a design brief checkpoint** so Ian's aesthetic calls never block the systems work running alongside.
