{
  "name": "omega-playground",
  "private": true,
  "description": "OMEGA Playground — the in-monorepo rehearsal brand: one brand config, every target (web, backend, desktop, browser extension). Test infrastructure, never production.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "omega-manager",
    "pipeline": "omega-manager pipeline --require=search-console,sendgrid,recaptcha,analytics,account,disperse,update,bookmark"
  }
}
