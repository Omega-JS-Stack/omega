{
  "name": "omega-functions",
  "version": "0.0.1",
  "description": "OMEGA Cloud Functions (emulator-only demo project during dogfood)",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "@omega.js/backend": "file:../../../../../packages/backend",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {},
  "scripts": {
    "start": "npx omega setup && npx omega serve",
    "deploy": "npx omega setup && npx omega deploy",
    "emulator": "npx omega setup && npx omega emulator",
    "test": "npx omega setup && npx omega test",
    "setup": "npx omega setup"
  }
}
