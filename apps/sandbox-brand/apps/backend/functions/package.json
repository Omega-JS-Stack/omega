{
  "name": "sandbox-brand-functions",
  "version": "0.0.1",
  "description": "Sandbox brand Cloud Functions (emulator-only, demo project) — the Omega monorepo's permanent dogfood consumer",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "backend-manager": "file:../../../../../packages/backend",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {},
  "scripts": {
    "start": "npx mgr setup && npx mgr serve",
    "deploy": "npx mgr setup && npx mgr deploy",
    "emulator": "npx mgr setup && npx mgr emulator",
    "test": "npx mgr setup && npx mgr test",
    "setup": "npx mgr setup"
  }
}