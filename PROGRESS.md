# Project Progress Tracker
> Agents and maintainers should update this file regularly to reflect the current state of the project.

## 🎯 Current Focus
* **Goal:** Phase 1 — foundation packages — is **functionally COMPLETE**: sandbox (1.2), @omegajs/account (1.3), BEM harmonization (1.4a–d — **1.4c defaults parity DONE**: BEM scaffolds via the devkit engine, `_.env`/`_.gitignore` moved into defaults with marker merges; BEM's merge-line-files custom-key promotion absorbed INTO devkit as canonical), @omegajs/config (EM+BEM+BXM flipped, 1.5a–d), devkit slices (1.6a cli-router, 1.6b defaults-engine). Remaining Phase-1 stragglers: build-task factories (1.6c) deliberately deferred to ride @omegajs/web's gulp-ectomy; Ian-owned publish gates. **Phase 2 (frontend bake-off) is UNDERWAY**: A0 baseline + corpus done (Task 2.1 — **the real somiibo Jekyll baseline is 332s cold**, recorded in spikes/bakeoff-shared/BASELINE.md; deterministic corpus generator mirrors somiibo's exact shape). **A0 groundwork is COMPLETE** (Task 2.2 — @omegajs/template-kit ships the full uj_* surface as engine-neutral JS + registerLiquid() adapter proven vs real liquidjs; @omegajs/config gained toSiteGlobal()). **Task 2.3 — BOTH A1 candidate spikes — is DONE.** Eleventy (2.3a): cold 3.60s vs 332s Jekyll (~92×), all bets validated, 17/17. Astro (2.3b): cold 5.56s (~60×), **URL set IDENTICAL to the Eleventy build**, all ITS bets validated (`omega:` Vite resolver layering, import.meta.glob dispatch, exported layout defaults + computeResolved, content collections, injectRoute defaults), 13/13 — with the honest costs recorded (layouts port Liquid→.astro by hand; content stays Liquid so the liquidjs+template-kit engine ships regardless; no incremental static rebuild; ~16.5s dev boot / instant warm renders). The SSG-agnostic pieces (asset pipeline, collectLayered, frontmatter-Liquid, consumer scan, mini-site fixture) now live in bakeoff-shared — both spikes build identical content through identical plumbing. **Task 2.4 — A2 scoring + decision memo — is DONE: ELEVENTY WINS (4.70 vs 3.55, margin 1.15).** The migration-cost dimension was measured on 3 REAL files ported into both candidates: Eleventy took 43 mechanical single-line edits (all codemod-able — the 8 extracted rules ARE the B4 conversion-plan seed) vs ~700 hand-rewritten lines for Astro, projected across 247 files/38.6k lines (46-repo census). Output parity: 1,279/1,279 corpus pages normalized-identical across candidates. Re-benched post-changes: Eleventy 3.52s / Astro 5.31s. Memo: spikes/bakeoff-shared/DECISION.md. **Phase B is UNDERWAY: B1 engine core DONE (Task 2.5)** — `packages/web` (@omegajs/web@0.1.0) owns the engine + packaged content + suite; both spikes are thin harnesses. **B2 REAL-CONTENT PORT DONE (Task 2.6)** — the full real UJM content lives in the package: 45 blueprint layouts + core/root chrome (885-line head/body/foot adapted to the content-hash manifest), classy FULL + neobrutalism + newsflash themes + vendored bootstrap layer, ~60 default pages at real URLs (incl. paginated blog + taxonomy generator pages replacing dynamic-pages.rb), the real UJM js runtime + css tree as the core asset layer. Engine grew the real-world mechanics: consumer `_layouts` layer (sweet-saucy pattern), core as layout layer, generalized bracket aliases, inject-properties-parity `resolved` site seed, Jekyll site emulation (site.posts/team/updates/alternatives synced arrays, site.data._includes from include-root JSONs, paginator compat, jekyll.environment), pageAssets manifest lookups, updates collection + team/updates permalinks. Assets pipeline rebuilt on LAYER ROOTS (js/css main + pages conventions, boot stubs, __main_assets__/__theme__/web-manager dir aliases, page-css namespaces). FOUR REAL ENGINE BUGS found+fixed by the port: lazy block-tag bodies (falsy iftruthy executed inner assigns — the silent title-killer), jsonify(undefined)→null (invalid JS in Configuration), Eleventy computed-proxy '' permalinks (every doc collided at /index.html), throwing templateContent getters + missing LiquidJS include cache (corpus 40.6s→16.7s eleventy phase after profiling). Suites: web 30/30 (slice 13 + assets 5 + ports 5 + NEW theme contract 7 — every theme builds the full default set, 66 pages each), astro 17/17 (frozen B1 pipeline vendored), template-kit 41/41. Corpus B2 baseline: **1,397 files, cold ×3 mean 20.40s** (real chrome; A2's 3.52s was the synthetic slice — per-page engine cost ~12.7ms vs Jekyll's ~68.6ms ≈ 5.4×; real somiibo comparison = Phase-4 scale test). **B3 CLI + SCAFFOLDING + BOOT RUNTIME DONE (Task 2.7)** — `omega setup/dev/build/deploy/test/clean/version` fully functional (translate/audit = explicit not-ported stubs), consumer scaffolding via the devkit engine (marker merges, omega.json5 seed, Ruby-free CI template, NO page copying), and the ESM boot runtime: bundles are ESM + code splitting so web-manager + boot land in ONE shared chunk — the singleton survives across all bundles (page bundles collapsed to ~330-byte stubs); bootMain/bootPage reproduce the UJM `{ manager, options }` handshake. Live-verified: bare consumer → setup → **56 default pages in 1.65s** → dev server → test → clean. Suites: web 38/38, tk 41, config 38, devkit 69, shared 4, astro 17; corpus post-ESM 18.6s. **B4 MIGRATE CODEMOD + LIQUID-LINT DONE (Task 2.8)** — `omega migrate` converts a UJM consumer in place (`--check` = zero-write preview): legacy configs → validated omega.json5 (unified spellings — analytics.providers, firebaseConfig/payment extracted; web_manager blob + presentation + build settings under targets.web; engine composes the runtime shape back so the chrome/client contract is unchanged), the 8 DECISION.md codemod rules as executable text transforms (semantic render proofs in the suite), runtime-derived liquid-lint, consumer asset-layer migration (seed main.js deleted, `@use 'omega:main' with (…)` theme customization via the containingUrl-skipping sass importer, page-css self-@use drops), legacy files removed. LIVE-PROVEN: deployment-playground migrate→build 57 pages/2.33s (verified $primary/gtag/firebaseConfig in output); **REAL somiibo migrate (73 edits/23 files) → 2,556 pages in 96–104s vs 332s Jekyll (~3.2× whole-pipeline)**; two real-data engine bugs fixed (slug-deduped taxonomy — Marketing/marketing collision; the LIVE somiibo broken-icon bug killed in 12 packaged-layout copies + forward by the codemod). Suites: web 52/52, sisters green, corpus 18.14s. Next: **B5 — `omega verify` parity harness** (first job: somiibo 2,556 vs Jekyll 2,608 URL-set diff).
* **PIVOT (Ian, checkpoint 33): migrators are PAUSED** — no more migration/verify tooling until Ian asks (B5 `omega verify` shelved). New priority: **bring omega-manager's functionality back** — the engine that walks all services and sets everything up idempotently — so a brand monorepo is fully working. **@omegajs/manager (packages/manager) now EXISTS**: the service-runner (strict `{state,output,status,error}` contract) + run-summary ported from omega-manager, brand-monorepo loading (config/omega.json5 IS the brand config — no .brands/ mirror; app→target via declared targets or dir conventions; root resolution mirrors @omegajs/config's walk-up, incl. a brand nested in a parent workspace's apps/), the `.omega/` state store (state.json = durable IDs, runs/{ts}.json = per-run transients; the manager gitignores it itself), the manage orchestrator + `omega-manager` CLI (devkit router; manage default, --service/--dry-run/--continue-on-error), and the first three services: workspace (structure/config/gitignore), update (resolution-gated root npm install + per-app build), testing (per-target health checks). 27/27 tests; live-proven on apps/sandbox-brand (both apps map, install correctly skipped, website builds, 5/5 checks, second run = no-op). External services port next in omega-manager's order.
* **Current Phase:** @omegajs/manager buildout — orchestration core + local services DONE (checkpoint 33); github DONE (34); cloudflare DONE (35 — 12 ops); domain DONE (36); firebase DONE (37 — all 13 ops); recaptcha DONE (38); analytics DONE (39); search-console DONE (40); adsense DONE (41); sendgrid DONE (42); beehiiv DONE (43); payment DONE (44 — all 11 ops across Stripe/PayPal/Chargebee); slapform DONE (45 — form diff-sync + owner plan via the NEW shared Firestore REST client, no firebase-admin dep); chatsy DONE (46 — agent settings + generated-pricing knowledge diff-synced; shared owner-plan lib; fixed omega's live `{website}/pricing` bug); replyify DONE (47 — email-agent filter + knowledge diff-synced; operator-product trio complete; ITW's sponsorship block + GIFT15/WELCOME10 stripped into config); server DONE (48 — company-server brand registry replace-synced at brands/{brand.id}; FirestoreREST gained setDoc); assets DONE (49 — 5 local image ops through the REAL pipeline, mtime-diffed idempotency killed omega's --onboarding gate; fonts de-ITW'd to brand.font; MrLogo AI + PSD trio parked; sandbox 4 passed live); certificates DONE (50 — Apple code-signing all 4 ops; required bundleIdPrefix config; brand-local .omega/certificates/apple/; shared jwt.js ES256/RS256; zero new deps); seo DONE (51 — parasite SEO GitHub repos: content-compared template push, MAX_STALE_FILES collision guardrail verbatim, per-item author identity, config/seo.json5 sidecar; item failures now LOUD; full dry-run guard); account DONE (52 — required Firebase Auth accounts: sign-in-probe password convergence, HMAC-derived passwords from auto-generated ACCOUNT_PASSWORD_SEED, account.admins config replacing hardcoded ADMIN_EMAILS, unauthorized-admin audit = service error; Identity Toolkit REST + FirestoreREST runQuery, zero firebase-admin; shared google-token.js + env-secret.js); migrations DONE (53 — --migration-gated Firestore data migrations on REST: one leaf-masked patch per doc, snapshots, notifications + users ported, omega's 25 company one-offs left behind; FirestoreREST gained listDocs/countDocs/getDocWithMeta/deleteDoc; auth-admin promoted to src/lib); testing-live DONE (54 — target-checks grew the live layer: framework version vs npm latest, homepage + API health with retries, working tree + GitHub Actions; dry-run = zero network; SERVICE ORDER FULLY DRAINED 33–54); **company mode DONE (checkpoint 55, Task M3 — company root = omega.json5 with brands.roots; one manage CHILD per brand for per-brand .env isolation, sequential streams / --parallel buffered + progress, --brand filter, argv forwarded; .omega/company.json stamp → brand-local runs layer company config + .env and print Company:; aggregate RunSummary from each brand's run file with synthetic entries for dead children; suite 429→447, live-proven on a scratch fixture company via the real CLI)**; next per the Roadmap queue: prompting + onboarding ports. Phase 2/B5 + ALL migrator work PAUSED until Ian asks; Phase 1 functionally complete (Ian-owned publish gates outstanding)
* **Priority:** High
* **Last Updated:** 2026-07-09 1:10 PM (checkpoint 55)
* **Notes:** **STANDING RULE (Ian, 2026-07-07): live checks NEVER touch real ITW repos/resources — not even read-only probes** (the checkpoint-34 dry-run read against itw-creative-works/ultimate-jekyll is the example that set the rule). Live verification uses sandbox/fixture resources only; external-service ports prove plumbing via recording fakes + sandbox skip runs, and real-resource verification happens only on Ian's explicit go with resources he designates. Devkit vendoring proven on extension (585d7cc) and desktop (7c75970); runner-core adopted by extension (eaae359, checkpoint 7) and desktop (checkpoint 8, this change). Backend adoption waits for BEM harmonization (no dist layer yet). INCIDENT (resolved): checkpoint-5 commit initially ran in omega-manager by accident (stray shell cwd) — undone via mixed reset, Ian's working tree fully restored, nothing pushed; all git commands now use explicit `git -C`. Pending Ian: electron-manager@1.12.1 publish (his session), @omegajs org claim, GitHub remote creation.

## 🗺 Roadmap — the forward queue (SSOT for "what's next")

**Where direction comes from, in priority order:**
1. **Ian's standing directives** — the checkpoint-33 PIVOT ("bring omega-manager's functionality back ... so a brand monorepo is fully working") overrode the master plan's phase order and drove checkpoints 33–54; the pause list below is also his.
2. **The master plan** — `/Users/ian/.claude/plans/i-need-you-to-jiggly-salamander.md` (OMEGA ecosystem redesign, Phases 0–5). Strategic direction: what exists, what it becomes, the gates. The manager work now underway is the plan's Phase-5 `@omegajs/manager` pulled forward by the pivot.
3. **This queue** — the tactical order the checkpoints follow. Each "continue" takes the top unchecked item unless Ian redirects. The order below is Claude's recommendation; **Ian can reorder by editing this list or just saying so.**

**The queue (after checkpoint 55 — service order drained + company mode built):**
1. ~~**M3 — company mode**~~ **DONE (checkpoint 55)** — the pivot goal's other half: omega-manager ran ~20 brands from one workspace; `omega-manager` from a company root now does the same (one manage child per brand, stamp-based inheritance, `--brand`/`--parallel`).
2. **[next] Prompting + onboarding ports** (prompting lands first — onboarding is built on it). Unlocks the long "rides the prompting/onboarding port" list parked across the service table (firebase project creation/polls, payment key-entry + confirm gates, analytics/search-console/adsense browser polls, slapform/chatsy/replyify setup flows, MrLogo brandmark generation) and omega-manager's `npm run onboard` → the plan §0 "Use this template" brand-creation story.
3. **Config-serializer writebacks** — IDs that currently land in `.omega/state.json` (sendgrid list, beehiiv publication, payment product IDs, slapform/chatsy/replyify agent ids) write back into `config/omega.json5` with canonical key ordering, omega-manager's config-serializer semantics.
4. **Disperse remnants** — cert files into desktop/mobile apps + `.env` composition (the rest of disperse dissolved into the config hierarchy).
5. **Extension port** (browser automation; beehiiv segment creation) + **PSD template trio** in assets (needs the company's binary PSD templates + ag-psd/node-canvas) — company-flavored, low urgency.
6. **Back to the master plan's paused phases, each on Ian's explicit go:** B5 `omega verify` parity harness (PAUSED) → Phase 4 website migrations (pilot order incl. the somiibo scale test) → Phase 3 framework rename cutovers (extension → desktop → backend) → Phase 5 template product + company-instance migration.

**PAUSED until Ian asks:** B5 `omega verify`, ALL migrator tooling, translate/audit ports (explicit stubs in @omegajs/web).
**Ian-owned gates:** electron-manager@1.12.1 publish, @omegajs npm org claim, GitHub remote creation + first push (CI verification). **MAM: parked entirely.**

## 📌 Active Task List
* [ ] Phase 0: Bootstrap the monorepo
  * [x] Task 0.1: Create repo skeleton (packages/, spikes/, apps/, docs/, package.json workspaces, .nvmrc, .gitignore, README, CLAUDE.md, PROGRESS.md, CHANGELOG.md)
  * [x] Task 0.2: Probe @omegajs npm scope (no packages exist; Ian to claim org on npmjs.com)
  * [x] Task 0.3: Initial commit (b9d9aed) + per-checkpoint commits on Ian's go
  * [x] Task 0.4: Install + configure changesets (@changesets/cli ^2.31.0, .changeset/ initialized)
  * [x] Task 0.5: Plain-copy backend-manager, web-manager, browser-extension-manager, electron-manager into packages/{backend,client,extension,desktop} (rsync minus .git/node_modules/.env/logs/lockfiles; secret scan clean — only BEM's demo-project test fixture; MAM excluded as parked)
  * [x] Task 0.6: Workspace install + suites GREEN in-place — client 73 passing; extension 85 passing; desktop 751 passing / 5 designed extended-mode skips (required 2 hoisting fixes: src/test/runners/electron.js + boot.js now resolve electron via require.resolve(paths:[projectRoot]) instead of hardcoded <root>/node_modules/electron — behavior identical for consumers); backend 2 passing = its complete standalone suite (real BEM corpus runs in consumers; emulator stack self-orchestrated successfully from the monorepo, proving java+firebase-tools env)
  * [x] Task 0.7: CI workflow written (.github/workflows/ci.yml — 4 suites w/ xvfb+java, pack→scratch-install smoke; CI verification pending first push to GitHub) + pack-smoke run LOCALLY: all 4 packages pack, scratch-install, and resolve OK
  * [x] Task 0.7b: 🔥 FOUND+FIXED production bug via pack-smoke — electron-manager@1.12.0 ON NPM fails every fresh consumer install (postinstall runs scripts/sync-nvmrc.js but `files` didn't ship scripts/). Fixed in monorepo copy (files += "scripts/"); verified: tarball ships script, install succeeds, postinstall syncs consumer .nvmrc. NOTE: live npm package still broken → 1.12.1 publish is the natural Task 0.8 gate candidate (Ian's go required).
  * [ ] Task 0.8: GATE — publish one old-name patch from the monorepo (electron-manager@1.12.1 with the files fix). **Ian is handling this himself in another session**; Phase 1 proceeds in parallel per his go ("commit and continue").
* [ ] Phase 1: Foundation packages
  * [x] Task 1.1a: @omegajs/devkit first files — logger (identical ×4, EM's cleaned copy canonical), safe-install (byte-identical ×4), attach-log-file (functionally identical ×4; header normalized to `# omega log`). Private workspace package, 17 unit tests green (node:test until the shared runner slice lands).
  * [x] Task 1.1b: Vendor-on-prepare mechanism (packages/devkit/tools/vendor.js) — copies devkit src → dist/vendor/devkit + rewrites requires to relative paths + guards that hosts declare vendored modules' runtime deps. Proven on extension: 3 shims adopted, suite 85 passing (unchanged), pack→scratch-install resolves + runs the vendored logger, shipped dist has zero raw @omegajs requires. CI: devkit suite added; pack-smoke gained the self-containment grep (the hard gate, since prepare-package after-hooks are non-blocking).
  * [x] Task 1.1c: Adopt devkit shims in packages/desktop — logger + safe-install + attach-log-file shimmed (Electron-specific logger-lite stays EM-owned); suite 751 passing / 5 designed skips (unchanged); pack-smoke resolves + self-contained + vendored logger runs in a bare consumer + scripts/sync-nvmrc.js still ships. **Backend deferred to its harmonization step (Task 1.3-adjacent)** — BEM has no dist layer yet, so there's nothing for the vendor hook to rewrite (its bin requires src/ directly).
  * [x] Task 1.1d-part1: devkit test-runner CORE extracted (src/test/runner-core.js — discovery, suite/group/standalone execution, filtering, skip semantics, _init hooks, reporting; UJM's canonical, parameterized by title/packageName/targetAlias/suitesDir/middleLayers/boot config) + test/assert.js (was byte-identical ×3) + extended-mode-warning factory. Extension adopted: runner.js is now ~90 lines of BXM-specific config + layer glue; suite 85 passing (unchanged); runner loads from packed tarball. Vendor tool upgraded: SELECTIVE vendoring (only modules the host's dist requires + transitive relative requires — a safe-install-only host ships 1 file and needs only node-powertools) + rewrites `require.resolve()` forms (chromium runner inlines devkit assert SOURCE into the browser harness). Devkit suite 30 passing. CI grep widened to catch require.resolve forms.
  * [x] Task 1.1d-part2: desktop adopts runner core — runner.js 529→89 lines of EM config + layer glue (main+renderer share one Electron spawn via runners/electron.js with harnessEntry; boot glue passes emDistRoot; bootDefaultTimeout 15000; runners now lazy-loaded like BXM so a missing electron doesn't block build-layer tests); assert.js + extended-mode-warning.js shimmed (EM's harness needed NO require.resolve inlining — main-entry requires ../assert.js which chains through the shim, boot-entry resolves it via runtime path-join, renderer-entry keeps its self-contained mini-expect). Suite 751 passing / 5 designed skips (unchanged); prepare vendors 6 modules / rewrites 6 files; pack-smoke: resolves, self-contained, runner+assert+warning all load from tarball, sync-nvmrc.js still ships. Framework-specific runners (chromium/electron/boot harnesses) stay framework-side — revisit sharing UJM↔BXM chromium plumbing when @omegajs/web is built.
  * [x] Task 1.2a: Sandbox brand monorepo + backend app — apps/sandbox-brand in the day-one brand-monorepo format (package.json workspaces apps/*; README documents the fresh-by-construction + demo-everything rules); apps/backend = real BEM consumer built from packages/backend's own scaffolding sources (templates/ + src/defaults/), demo-sandbox-brand emulator-only project, fresh fake service-account key, committed demo .env (re-included via .gitignore Custom Values), backend-manager linked via file: to packages/backend. **FULL BEM CORPUS GREEN vs the sandbox: 1203 passing / 0 failing / 44 designed skips (exit 0)** — the framework's complete routes/events/rules/email/mcp corpus now runs in the monorepo against a fresh consumer (previously only 2 boot smokes ran here). 🔥 DOGFOOD CATCHES (3): (a) REAL BEM BUG FIXED — rules tests ignored firebase.json's firestore emulator port (runner never passed it to the rules client; every rules test times out on a custom-port consumer); src/test/runner.js now passes emulatorPorts.firestore. (b) FINDING (logged): BEM doesn't truly support custom emulator ports — Manager.getApiUrl()/getFunctionsUrl() hardcode localhost:5002/5001 in dev/testing (contract-asserted by test/helpers/environment.js), test/mcp/* hardcodes 5002; payment/cron self-calls break on custom ports. Sandbox uses DEFAULT ports like every real consumer; harmonization candidate. (c) REAL BEM BUG FIXED — content/post 404 tests hit LIVE GitHub search in normal (non-extended) mode; route 500s without GH_TOKEN so any token-less consumer fails. Gated behind GH_TOKEN presence (mailbox-key static-skip pattern) in test/routes/content/post.js (operative) AND the test/functions/general/fetch-post.js legacy copy (test/functions/ only runs with --legacy). Minor parked: BEM's test path filter matches project tests but not corpus paths.
  * [x] Task 1.2b: Sandbox website app + cross-stack e2e harness + CI wiring — apps/sandbox-brand/apps/website is a minimal web-manager consumer (esbuild bundles src/assets/main.js embedding packages/client; deps resolve from the workspace root, no per-app install needed in-monorepo); `npm test` at the brand root runs e2e/run.js: builds the site, boots BEM's full emulator suite (`npx mgr emulator` in apps/backend/functions), serves the site, and drives real Chromium (puppeteer) through signup → BEM auth onCreate creates the Firestore user doc → signout → signin via web-manager → session persistence across reload → subscription resolution (plan basic/inactive). **11/11 steps green, 2 consecutive runs, ~90s each; clean teardown (all ports freed).** The `omega e2e` CLI grows from this harness later (kept sandbox-local until a second consumer earns the devkit extraction). CI: new `sandbox` job (setup-java + root npm ci + functions npm ci + `npm run test:backend` corpus + `npm test` e2e); verification pending first push like the rest of ci.yml. 🔥 DOGFOOD CATCHES (2 REAL web-manager BUGS FIXED in packages/client): (a) NO auth-emulator support — Firestore had a FIREBASE_EMULATOR_CONNECT path but auth never called connectAuthEmulator, so signin/signup vs an emulated backend silently hit LIVE Firebase Auth; (b) the firestore emulator connect lived only in the lazy firestore module while the auth module reads accounts via `manager.firebaseFirestore` directly, so emulator-mode account reads hit LIVE Firestore unless page code happened to touch `manager.firestore()` first. Fix for both: single connect point in `_initializeFirebase()` right where the auth/firestore instances are created (auth :9099 + firestore :8080, same gate `isDevelopment() && env.FIREBASE_EMULATOR_CONNECT`); duplicate block removed from firestore.js. Client suite 73 passing (unchanged). Design note (Ian can veto): the website app embeds packages/client directly rather than being a UJM consumer — UJM is external/read-only + Ruby, and @omegajs/web replaces this app's internals in Phase 2 anyway; the brand-monorepo slot + e2e contract are what's being locked in now.
  * [x] Task 1.3: @omegajs/account extraction (golden-master gate) — COMPLETE: extracted (1.3a), adopted by web-manager (1.3b) and BEM (1.3c); both drifted schema copies are gone
    * [x] Task 1.3a: packages/account created + golden master GREEN — schema engine extracted verbatim from BEM user.js with `$uuid`/`$randomId`/`$apiKey` generators INJECTED via ctx.generators (BEM will inject uuid4/Utilities().randomId(size 8)/uid-generator; frontend passes none → nulls); USER_SCHEMA as pure data; resolveSubscription = BEM superset (everPaid — WM's copy lacked it); resolveAccount(data, {generators, user}) with the frontend auth-overlay semantic; time context = plain Date (verified powertools.timestamp ≡ toISOString/floor(ms/1000) — package has ZERO runtime deps, browser-safe). Golden master compares against LIVE packages/backend user.js (not a snapshot): frozen Date via node:test mock timers, deterministic randomId, format-assert+sentinel for the 2 non-injectable BEM generators, deepStrictEqual + JSON.stringify (key-order) parity across 7 fixtures (empty, paid, trialing, expired-trial+cancelling, suspended, junk-type coercion, passthrough/template/strip) + resolveSubscription parity per fixture. **31/31 tests green; CI test job gained the account suite.**
    * [x] Task 1.3b: web-manager adopts @omegajs/account — vendor tool GENERALIZED (multi-package: scans dist for ANY `@omegajs/<pkg>` reference and vendors each into dist/vendor/<pkg>/; package roots resolved via require.resolve of the bare name — convention: entry is src/index.js; + ESM forms rewritten: `import…from` / `export…from` / dynamic `import()` / side-effect `import`, alongside the existing require/require.resolve; relative-dep walk + host-dep guard cover both syntaxes; devkit suite 31 passing incl. new ESM multi-package fixture that import()s the rewritten file and proves named-imports-from-vendored-CJS interop). Client flip: auth.js deleted DEFAULT_ACCOUNT + local resolveAccount deep-merge + resolveSubscription body → imports `@omegajs/account` (resolveAccount(rawData, {user}) overlay; resolveSubscription wrapper keeps only WM's storage-fallback glue; everPaid additive); package.json gained @omegajs/{account,devkit} devDeps + the vendor after-hook. GATES GREEN: client suite 74 passing (was 73; +everPaid test); cross-stack e2e 11/11 (doc-created marker flipped from metadata.created.timestamp — which the canonical engine now fills with `now` even for fallback accounts — to api.clientId, non-null ONLY when the read hits the BEM-written doc since the frontend injects no generators; e2e observed a real server-generated v4 uuid + everPaid:false asserted in the browser bundle); pack-smoke: web-manager tarball dist has ZERO raw @omegajs refs, ships dist/vendor/account/{index,engine,schema,subscription}.js, vendored code loads + resolves from a scratch install; extension suite 85 passing (sanity on the changed vendor path); CI pack-smoke grep widened to ESM forms. Watch item: the tarball still ships src/ with the bare `@omegajs/account` import and `"module": "src/index.js"` points there — modern bundlers (webpack 5/esbuild) honor the `exports` map → dist, but an exports-unaware consumer would hit src and fail LOUDLY (unresolvable specifier); revisit shipping src/ at the client cutover.
    * [x] Task 1.3c: BEM adopts @omegajs/account — user.js (408 lines: schema + engine + constructor) → ~50-line thin wrapper, ZERO call-site changes (`new User(Manager, settings).properties` + `User.resolveSubscription` preserved; wrapper injects the real generators: uuid v4, `Manager.Utilities().randomId({ size: 8 })`, uid-generator apiKey — the golden master's mock Manager asserts the exact randomId args). packages/backend gained @omegajs/{account,devkit} devDeps + the vendor after-hook. The src-deep-require problem (corpus tests load `../src/...` in consumers, where the private package can't resolve — flipping ~24 test files incl. _legacy to dist was rejected as too broad) solved INSIDE the wrapper with a try/catch fallback: try the bare specifier (monorepo workspace link — always-fresh for the golden master; rewritten to `../../vendor/account/index.js` in dist by the vendor hook), catch → `../../../dist/vendor/account/index.js` (same file at the same depth from BOTH src/ and dist/ copies, so consumers' src deep-requires land on the vendored copy). 🔥 VENDOR BUG FOUND+FIXED (TDD, failing test first): jetpack.find follows symlinks during traversal regardless of matching exclusions → the host-dist scan recursed through the fixture's circular `backend-manager → package root` symlink until ENAMETOOLONG. vendor.js now scans with a symlink-safe walker (fs.readdirSync withFileTypes; never follows symlinks, never enters node_modules or dist/vendor — none of that is host code to scan/rewrite); devkit suite 32 passing (+1 regression test with a circular-symlink + node_modules fixture proving fixture-dep @omegajs refs are neither vendored-for nor rewritten). GATES GREEN: account suite 31/31 (golden master now validates the wrapper glue — real generators format-asserted, key order byte-identical); BEM standalone 2 passing (exit 0); sandbox corpus 1203 passing (exit 0; middleware logs show real generated clientId/privateKey/affiliate.code flowing through the injected generators); cross-stack e2e 11/11 (the BEM-side api.clientId was generated by the WRAPPED user.js inside the emulator runtime); pack-smoke 8/8 incl. the NEW src-fallback check (scratch-install consumer deep-requires src/manager/helpers/user.js corpus-style → vendored account resolves; v4 clientId + randomId wiring + apiKey + resolveSubscription all asserted). Ships as an old-name v5 release.
  * [x] Task 1.4: BEM structure harmonization (src→dist + generated public/* + defaults parity — plan §6, old-name v5 releases) — COMPLETE (1.4a dist layer, 1.4b generated public/*, 1.4c defaults parity, 1.4d devkit shims)
    * [x] Task 1.4a: dist layer + bin flip — preparePackage un-defused (`./src_`/`./dist_` → `./src`/`./dist`; the config, prepare scripts, and offline-safe disposable-domains before-hook were already wired, just disabled), `main` → dist/manager/index.js, bin/backend-manager requires ../dist/cli/index.js, `start` → prepare:watch (matches the other frameworks), .gitignore gained /dist/, prepublishOnly now prunes the self-test fixture node_modules from BOTH src and dist. Safety verified BEFORE flipping: jetpack.copy preserves symlinks as symlinks (no recursion on the fixture's circular `backend-manager → package root` link; empirically tested) and npm pack excludes symlinks from tarballs. Path-depth audit: dist mirrors src at the same depth, so all `__dirname`-relative escapes (templates/, package root) resolve identically; only src-literal is watch.js's bemSrcDir (BEM dev watch still watches src — correct as the reload trigger since prepare:watch keeps dist synced; noted as dev-flow nuance). Consumers deep-requiring src/ still work (files ships src/ unchanged; the corpus itself requires ../src in ~10 test files). GATES GREEN: standalone suite 2 passing via bin→dist; full sandbox corpus 1203 passing / 1247 total (exit 0, 84s) via consumer `npx mgr`→dist; cross-stack e2e 11/11; pack-smoke: tarball 1.2M with 432 dist files + fixture symlinks excluded, scratch-install resolves main to dist/manager/index.js, installed `mgr version` runs from dist, self-containment grep clean. 🔥 LATENT BUG FIXED (checkpoint-10 wiring): sandbox `test:backend` used `--workspace=apps/backend`, but npm resolves the project root to the MONOREPO (sandbox-brand is itself a root workspace; workspace globs don't recurse) so the flag never resolved — the corpus had only ever been run from inside apps/backend directly. Now `cd apps/backend && npm run test:offline`; CI's sandbox job would have failed on first push without this.
    * [x] Task 1.4b: consumer `public/*` becomes generated + gitignored — COMPLETE, all 4 gates green. Built: `src/cli/utils/public-files.js` (SSOT generator: writePublicFiles overwrite-mode for setup, ensurePublicFiles fill-if-missing reading the consumer's JSON5 backend-manager-config.json for the brand url); setup-tests/public-html-files.js delegates to it; ensure wired into EmulatorCommand.startEmulators() (covers `mgr emulator` AND `mgr test` auto-start — a fresh clone has NO public/ since it's gitignored, and firebase-tools refuses to boot hosting without the folder) and into `mgr deploy` (bare deploy includes hosting); templates/_.gitignore gained `/public/` under "Hosting boilerplate (generated ...)"; standalone fixture pre-tracked its missing public/404.html (generated via the real helper) so the ensure never creates untracked strays; sandbox dogfooded the REAL `mgr setup` (gitignore smart-merge added `/public/` ✓, plus legit consumer updates: functions/.nvmrc created, projectScripts synced into functions/package.json, firestore.rules version stamp, required BEM indexes, emulator-config ui.enabled=true) and sandbox public/* untracked (git rm --cached). GATES ALL GREEN: ✅ fresh-clone corpus (rm -rf public → `mgr test` regenerates → 1203 passing exit 0); ✅ pack-smoke 11/11 (new: shipped gitignore template has /public/; ensurePublicFiles from a scratch consumer install generates from a JSON5 config with url substitution and refuses to overwrite existing files); ✅ standalone suite 2 passing (fixture tree stays clean — pre-tracked 404.html, no ensure strays); ✅ fresh-clone e2e 11/11 (rm -rf public → `mgr emulator` regenerates with the REAL brand url from the JSON5 config — found+fixed mid-checkpoint: the first ensure draft used strict JSON.parse, which silently fell back to url:'' on BEM's JSON5 config). Port-conflict interlude resolved (Ian closed somiibo serve on 5002; this session's 3 orphaned java emulators killed with his go). 🔥 FINDINGS (logged, not fixed): (a) full `mgr setup` CANNOT complete on an emulator-only consumer — firestore-indexes-synced fetches LIVE indexes (403 on demo-sandbox-brand), crashes with no summary, and leaves the raw error text in a stray `_firestore.indexes.json`; setup should skip live-project checks for `demo-*` project IDs (firebase-tools' offline convention) — harmonization candidate; (b) nvmrc-version's fix is deliberately two-phase (writes .nvmrc then throws so the user re-runs) — surprising on first setup of a fresh consumer; (c) `mgr test` teardown can orphan the java emulator grandchildren (firestore/database/pubsub survived with PPID 1).
    * [x] Task 1.4c: defaults expansion to parity — BEM's setup scaffolds through the devkit defaults engine. `copyDefaults`'s hand-rolled walk (archive-skip, `_.` renames, MERGEABLE_BASENAMES merge) → `src/utils/scaffold-defaults.js` (engine + BEM FILE_MAP: `'**/*': {overwrite:false}` + mergeLines for CLAUDE.md / `_.gitignore` / `functions/_.env`; exported for the self-test, mirrors BXM); setup.js prints per-file add/merge from the engine's `{written,merged}` return (quiet logger; no-op merges land in `skipped` so "All defaults up to date" stays honest). TREE 5→7 FILES: `templates/_.env` → `src/defaults/functions/_.env`, `templates/_.gitignore` → `src/defaults/_.gitignore` (git mv; engine `_.` strip + path placement puts them at `functions/.env` / `.gitignore`); env-file.js + gitignore.js setup checks retarget `dist/defaults/...` and KEEP their legacy fix paths (old `# BEM>>>` marker strip, functions/.gitignore removal, no-marker→custom migration). WORKFLOWS EXCLUDED from parity by evidence: zero real BEM consumers have .github/workflows (checked ultimate-jekyll/somiibo/studymonkey/slapform backends). 🔄 REVERSE FLOW INTO DEVKIT (standing permission): BEM's merge-line-files had evolved PAST the ckpt-22 canonical — custom-key PROMOTION (framework newly adopts a key the user had in Custom → promoted UP into Default with their value, Custom copy dropped; no more empty-in-Default + set-in-Custom duplicates) absorbed into @omegajs/devkit/merge-line-files + `hasSectionMarkers` export (BEM's setup validators consume it); one refinement over BEM's copy: keys in BOTH sections keep the Custom copy (dropping it would flip dotenv's last-wins effective value — new devkit test). BEM's src/utils/merge-line-files.js → standard shim; setup-tests helpers wrapper re-exports + marker aliases only. EM/BXM inherit promotion via vendored devkit (their merge tests pin nothing contrary — verified before flipping). BEM vendors devkit 2→4 (defaults-engine + merge-line-files); minimatch ^10.2.5 added to BEM deps (BARE_PATTERNS host-dep guard; lockfile delta = 1 line, already hoisted). NEW test/boot/defaults-scaffold.js (framework self-test layer, 4 tests vs mkdtemp dirs): fresh 7-file tree + renames + markers, value/custom preservation (.env + .gitignore), promote-up through the REAL template, run-2 idempotent + run-3 tree-checksum stable (BEM's template has no pre-marker preamble → instantly idempotent, no converge pass unlike EM). Sandbox dogfooded the LIVE `mgr setup`: defaults merge ran (`Merged functions/.env` — its explanatory preamble comment sat in the framework-owned Default section and was dropped per protocol; relocated to the Custom section where it's preserved, re-merge verified 0/0), checks 1–30 green incl. the retargeted [17] gitignore + [18] env; post-30 `firestore-indexes-synced` live-deploy crash on demo-* = the KNOWN parked 1.4b finding (pre-existing; its stray `_firestore.indexes.json` cleaned). GATES GREEN: devkit 69 (67 + 2 net: promote-up rewrite + both-sections + hasSectionMarkers), BEM standalone 6 passing (2 boot + 4 scaffold), sandbox corpus 1203 passing / 1247 total (exact baseline), cross-stack e2e 11/11, EM 755 passing / 5 designed skips + BXM 92 (both unchanged — promotion regression-neutral), pack-smoke-bem-defaults 5/5 (tarball ships dist/defaults + engine + scaffold-defaults, templates/ clean of moved files, self-containment grep, REAL scaffoldDefaults from the installed tarball: fresh + merge + promote-up + idempotent, helpers shim resolves through vendored devkit).
    * [x] Task 1.4d: BEM devkit shims (deferred from Task 1.1c, done ahead of 1.4b/c since the vendor hook was just proven in BEM) — src/cli/utils/{safe-install,attach-log-file}.js flipped to the standard one-line shims (same pattern as extension/desktop). Pre-flip diff confirmed both BEM copies were functionally identical to devkit's canonical versions (comment-only deltas + the already-accepted `# bem log` → `# omega log` header normalization). BEM has NO framework logger to shim — its console+file route logging is a BEM feature (wonderful-log), per the plan. Scope check: nothing in test/, templates/, or src/defaults requires these src paths, so no consumer ever loads the src shims (CLI always runs bin→dist, where the vendor hook rewrites them) — the user.js try/catch fallback stays a user.js-only special case. Vendor now ships account (4) + devkit (2), rewriting 3 dist files. GATES GREEN: standalone 2 passing (exit 0); sandbox corpus 1203 passing (exit 0); cross-stack e2e 11/11 (mgr emulator = the dist CLI path); pack-smoke 9/9 (new check: vendored shims load from a scratch consumer install with intact export shapes — safeInstall fn + callable attachLogFile with detach/createTee).
  * [ ] Task 1.5: @omegajs/config (Phase-1 step 4 — omega.json5 loader/validator/schema + framework flips). ⚠ PLAN DEVIATION (Ian, 2026-07-06): dual-read is CANCELLED — "no backwards compatible things needed. just implement and document the new way ... so that legacy brands can migrate over." Frameworks flip to omega.json5 outright; legacy configs are no longer read anywhere; migration = one-time file conversion per the mapping tables in docs/config.md.
    * [x] Task 1.5a: packages/config created — the package itself, no framework touches. `loadConfig(projectDir, target, {defaults})`: discovery (config/omega.json5, then functions/config/omega.json5 for standalone backends), brand-monorepo walk-up ({brand}/apps/{app} → brand root config, only when the brand root actually has one), five-layer agnostic deep merge (`defaults ← brand shared ← brand targets[t] ← app shared ← app targets[t]`; target sections overlay the TOP LEVEL, so any shared key inside a target entry overrides per-surface), merged `targets` map kept on the resolved config (key presence = enabled; `enabled` flag + getEnabledTargets()), no-target mode = whole-file merge (the disperse shape), hasOmegaConfig() as the frameworks' dual-read probe. HARD FAILS on raw files pre-merge: secret-shaped keys (plan regex `/(secret|privateKey|apiSecret)$/i`; key-NAME test — publishableKey/clientId/firebaseConfig.apiKey pass by design) in ANY target section throw; legacy brand-config `targets` ARRAY throws loudly (would silently mangle to `{0:'web'}` through the merge); JSON5 parse errors carry the file path. Schema findings return as `errors`, never thrown — EM's proven two-mode usage (audit throws, boot warns). Schema: EM's src/config/schema.js entry format + validator walker ported verbatim (runSchema; dropped EM's dead describe() helper); SHARED_SCHEMA seeded from EM's shared entries (brand/firebaseConfig/analytics/payment/sentry/oauth2/theme + targets sanity incl. unknown-target-name typo protection); TARGET_SCHEMAS.desktop seeded from EM's real schema with the plan's `platforms` rename, .backend seeded from the sandbox's REAL backend-manager-config.json (parent/github/reviews/marketing/blog/dataRequest — read, not guessed), web/extension/mobile empty by design (web fills in Phase 2; mobile reserved, MAM parked). SHARED_SECTIONS export = the disperse enumeration. Deliberately deferred: toSiteGlobal() (needs the real Jekyll site.* usage audit — lands with Phase-2 A0/template-kit), framework dual-read adoption (next slices; EM canary is the plan's gate). GATES GREEN: config suite 34/34 (merge/secrets/validate units + loader fixtures under .temp/ per the devkit convention); devkit 32 + account 31 re-run green after the workspace install refresh (lockfile diff = +15 lines, just the new workspace entry); CI test job gained the config suite step (verification still pending first push, like all of ci.yml).
    * [x] Task 1.5b: EM flips HARD to omega.json5 (first framework on the new config; no dual-read per Ian's deviation above). Load points: `Manager.getConfig()` (build/CLI side — the single choke point webpack snapshots into EM_BUILD_JSON, so packaged apps automatically carry the RESOLVED config) and `main.js initialize()` dev fallback both resolve via @omegajs/config (`loadConfig(dir,'desktop')`; missing file → seeded-empty `{brand:{},app:{}}` so non-consumer cwds still run CLI commands; validation reports what's missing). Boot + `gulp audit` validation → `validateConfig(config, {target:'desktop'})`; EM's src/config/schema.js + src/utils/validate-config.js DELETED (engine lives in @omegajs/config — EM's own schema was its seed). Per-OS `config.targets.*` → `config.platforms.*` everywhere (build.js getWindowsSignStrategy, audit.js MAS/snap warnings, build-config.js entitlements/arch/NSIS/snap reads, sign-windows.js cloud provider — the last one caught by the SUITE, not grep: multi-line `?.` chains hide from path greps). Defaults template rewritten as src/defaults/config/omega.json5 (shared sections top-level; app/platforms/autoUpdate/startup/releases/downloads/remoteConfig/restartManager under targets.desktop; all teaching comments preserved); consumer workflow one-liners (snap gate, windows signing strategy) now use the NEW `electron-manager/config` package export → dist/vendor/config/index.js (canonical resolution, brand-monorepo aware) instead of raw JSON5 reads; boot fixture + audit/get-config/config-schema/validate-config/manager/release-pipeline/build-config suites flipped to the new shape (validate-config.test.js rewritten as the @omegajs/config integration surface — engine semantics live in the config package's own 34 tests). EM docs swept (17 files: filename + platforms renames; config-schema.md + CLAUDE.md + defaults CLAUDE.md restructured for the shared schema); NEW monorepo docs/config.md = format spec + per-framework migration mapping tables (EM done; BEM/UJM/omega-manager rows staged for their flips). GATES GREEN: EM suite 751 passing / 5 designed skips (exactly the pre-flip baseline; 1 intermediate failure was the sign-windows cloud-provider path — fixed, full re-run clean); pack-smoke 9/9 (tarball ships vendored config ×6 + omega.json5 template, NO legacy electron-manager.json, self-containment grep clean, `require('electron-manager/config')` resolves from a scratch install, workflow-style strategy read works, installed getConfig() resolves + derives appId/productName, secret-shaped key hard-fails from the installed package, sync-nvmrc.js still ships); config suite 34/34 re-run. NOTE for EM consumers at publish: this ships as the config-flip release — existing consumers must convert config/electron-manager.json → config/omega.json5 (docs/config.md table) when they update.
    * [x] Task 1.5c: BEM flips HARD to functions/config/omega.json5 (second framework; biggest config surface). LOADER FIRST: @omegajs/config gained the functions-dir normalization — BEM's runtime cwd is the functions/ dir (Cloud Functions + emulator both boot there), so findBrandConfigPath now treats an app root as one level up when basename === 'functions'; new load test proves `loadConfig(functionsDir,'backend')` ≡ `loadConfig(appRoot,'backend')` incl. brand walk-up + firebaseConfig from the brand layer (config suite 34→35). MANAGER: init() resolves consumer config via loadConfig(cwd,'backend', {defaults}) where defaults = templates/config/omega.json5 RESOLVED through the same loader (targets stripped) — the template file was git-mv'd + reshaped (shared top-level, parent/github/marketing/blog/reviews/dataRequest under targets.backend; killed a latent duplicate `parent` key JSON5 was silently last-winning); missing consumer config → defaults only (same tolerance as before); boot warns via formatErrors on schema findings (audit-throws/boot-warns contract); REMOVED: backendManagerConfigPath option, self.project.backendManagerConfigPath (+ its options.log line at :384 — greps missed it, caught by the post-flip residue grep), requireJSON5 helper, lodash mergeWith/isArray imports, JSON5 import, legacy `app.id → brand.id` fallback (no-backwards-compat). CLI: setup.js loads RESOLVED config WITHOUT defaults (so the template comparison stays meaningful) via namespaced `omegaConfig.*` (its own loadConfig method collides), scaffolds templates/config/omega.json5 → functions/config/omega.json5, bemConfigJSON → omegaConfigJSON everywhere; setup-tests: bem-config.js → omega-config.js (git mv; compares resolved-vs-resolved, fix() messages point at targets.backend), project-id-consistency checks the RESOLVED projectId (brand-level firebaseConfig counts) + fixes by writing an app-level override, public-html-files rides the rename; test.js loadProjectConfig + self-test fixture key injection through the loader; firebase-init resolveProjectId + public-files ensurePublicFiles through the loader (hasOmegaConfig-guarded). FIXTURE: src/test/fixtures/firebase-project functions/config/omega.json5 (JSON5, targets.backend enablement, backend_manager admin keys stay top-level custom). SANDBOX DOGFOODS THE HIERARCHY: shared sections (brand/firebaseConfig/analytics/payment/sentry/oauth2) moved UP to apps/sandbox-brand/config/omega.json5 (brand level, targets {web:{}, backend:{}} enablement), backend app file = targets.backend only — the corpus + e2e now exercise the five-layer chain + functions-dir walk-up in a real consumer on every run. package.json: devDeps + @omegajs/config; vendor hook now ships config ×6 into BEM dist (rewrote 10 files); EM dist re-vendored so its copy carries the walk-up fix. Docs: BEM CHANGELOG [Unreleased] BREAKING entry with the 5-step consumer migration recipe; README config section rewritten in the new shape (killed a stale example that showed `googleAnalytics.secret` IN the config — the new loader would hard-fail it; backendManagerConfigPath row dropped); CLAUDE.md, defaults CLAUDE.md (+ sandbox mirror), 13 docs/*.md swept contextually (consent parent under targets.backend, migration.md step 7 = convert-then-edit, directory-structure/key-files templates paths); docs/config.md BEM table → DONE + functions-dir rule documented in Resolution; monorepo CHANGELOG entry. NOT touched: test/_legacy/* (2 refs to the dead backendManagerConfigPath option — off-limits dir, scripts are not in the suite). GATES GREEN: config suite 35 (fail 0), BEM self-test 2 passing (Manager booted in the emulator functions runtime off the new fixture config), sandbox corpus 1203 passing / 44 skipped (EXACT pre-flip baseline, now THROUGH the brand walk-up), cross-stack e2e 11/11, pack-smoke-bem-config 9/9 (vendored config ships, omega.json5 template ships + legacy template ABSENT, CI self-containment grep clean, functions-dir walk-up + secret hard-fail + template-defaults resolution all proven from a scratch install, bin runs from dist).
    * [x] Task 1.5d: BXM flips HARD to config/omega.json5 (third framework; smallest flip — targets.extension near-empty by design; NO loader changes needed, the generic apps/-walk-up already covers extension apps). MANAGER: build.js getConfig() → hasOmegaConfig-guarded loadConfig(cwd, 'extension').config (missing file → {} — EM's tolerance semantic, replacing the old crash-on-missing JSON5.parse; schema findings console.warn ONCE per process via a module flag, because ~9 gulp tasks each call getConfig() at require time and BXM has no separate audit surface). Config flows unchanged to all gulp tasks + the BXM_BUILD_JSON runtime snapshot. SECRET KILLED: the defaults template carried `analytics.providers.google.secret` (the LAST secret-in-config in any framework — the new loader hard-fails it by design); moved to .env as GOOGLE_ANALYTICS_SECRET (BEM/EM convention; _.env defaults template gained the entry; package.js bakes it into build.json/BXM_BUILD_JSON from process.env at build time — same value flow web-manager's GA4 measurement-protocol path expects via `analytics.googleSecret`; dotenv already loaded by cli.js + gulp/main.js). TEMPLATE: src/defaults/config/browser-extension-manager.json git-mv'd → omega.json5 + reshaped (EM-style header comments, shared sections top-level, `targets: { extension: {} }` presence-enablement). DEFAULTS TASK: FILE_MAP key renamed (overwrite+merge semantics kept) + REAL BUG FIXED: mergeConfigs started from the template and only walked template keys, so consumer-only keys (liveReloadPort, anything under targets.extension) were silently DELETED on every `bxm setup` — latent data-loss made contract-breaking by the new shape (targets.extension is DESIGNED to hold consumer keys); mergeNested now copies source-only keys at every level. EXPORTS: package.json gained `./config` → dist/vendor/config/index.js (BXM's exports-map test suite auto-generates a test per entry — that's the +1 in the suite count) + @omegajs/config devDep; vendor hook (already wired) ships config ×6 into dist. TESTS: manager.test.js stages config/omega.json5 — old getConfig test replaced by 3 (target overlay onto top level + custom-key passthrough incl. JSON5 comment, missing → {}, secret hard-fail). Docs: BXM CHANGELOG [Unreleased] BREAKING (5-step migration recipe incl. the secret→.env move + brand-monorepo note) + Added (config export) + Fixed (merge bug); README/CLAUDE.md/9 docs swept (managers.md + hooks.md wording upgraded parsed→RESOLVED); defaults CLAUDE.md line 50 rewritten in the new shape; docs/config.md BXM mapping table added (DONE checkpoint 20) + Consumer-access section lists the BXM export; monorepo CHANGELOG entry. A/B methodology note: baseline 85 verified by stash-roundtrip (HEAD+fresh-dist = exactly 85; flip = 88 = 85 + 2 net new getConfig tests + 1 exports-map auto-test). GATES GREEN: BXM suite 88 passing (fail 0), config suite 35 (fail 0, untouched this checkpoint), pack-smoke-bxm-config 9/9 (vendored config ×6 ships, omega.json5 defaults template ships + legacy file ABSENT from tarball, CI self-containment grep clean, installed Manager.getConfig resolves brand walk-up from apps/{brand}/apps/extension + targets.extension overlay, secret hard-fail, `browser-extension-manager/config` export resolves, shipped template resolves clean+enabled, bin runs from dist).
  * [ ] Task 1.6: devkit remaining slices (plan Phase-1 step 5; risk order cli-router → defaults engine → build-task factories; the defaults engine unblocks Task 1.4c)
    * [x] Task 1.6b: defaults-engine slice — devkit gained `src/defaults-engine.js` (`applyDefaults({ defaultsDir, outputDir, fileMap, files, transform, logger })` → `{ written, merged, skipped }`): minimatch FILE_MAP (dot:true, last-match-wins) with overwrite/skip (bool|fn), name/path (fn), template (tolerant `{{ key.path }}` — EM's renderer is the standard, GitHub `${{ }}` survives), merge (JSON5 defaults merge — BXM's semantics incl. the checkpoint-20 consumer-key fix, exported as `mergeJson5Defaults`), mergeLines (marker protocol); built-ins: `_.` segment strip, archive-dir skip (non-final `_x`, verified safe — BXM tree has NO `_x/` dirs, `test/_init.js` is a FILE and ships), `.gitkeep` → dir-only, `.DS_Store` skip, write-only-if-changed, binary passthrough (BXM's ext list), symlink-safe walk. PLUS `src/merge-line-files.js`: EM's evolved copy verbatim (canonical — .env double-quote normalization, order-safe substitution, pre-marker lines treated as default; BXM's older inline variant DROPPED pre-marker content silently). EM ADOPTED: `copyDefaults(targetDir?)` → engine call owning only the file map (catch-all preserve, 3 marker merges, always-re-render YMLs; `.nvmrc` rule dropped — no such default file, sync-nvmrc.js owns consumer .nvmrc); `utils/merge-line-files.js` → standard shim; exported copyDefaults + NEW build/defaults-scaffold suite (4 tests vs temp dirs: renames/archive/workflow-render, preserve, .env migrate+survive, converge-then-stable — first re-merge normalizes EM's pre-marker `_.env` preamble below the marker, pre-existing protocol behavior). BXM ADOPTED: gulp task keeps shell+watcher, guts → exported `scaffoldDefaults({ outputDir, files })`; FILE_MAP sheds the `_.` name-fns + `.DS_Store` rule (built-ins); global `[ site.x ]` bracket pass on html/md/liquid/json/yml/yaml became the engine `transform` hook (createTemplateTransform util untouched — distribute still streams it); `.nvmrc` template normalized `v{ versions.node }/*` → `v{{ versions.node }}/*` (CAUGHT pre-flip: single-brace node-powertools tokens vs the `{{ }}` engine standard; rendered output byte-identical `v22/*`); NEW build/defaults-scaffold suite (4 tests incl. the omega.json5 consumer-key regression THROUGH the engine + converge — JSON5 comments strip on first merge, pre-existing). UNBLOCKS Task 1.4c. GATES GREEN: devkit 67 (+21: 9 merge-line ported cases + 12 engine), EM 755 passing / 5 designed skips (760 total = 756 + 4), BXM 92 (88 + 4), pack-smoke-defaults-engine 6/6 (both tarballs ship engine+merge vendored — devkit ×9 now; EM's REAL copyDefaults scaffolds AND re-merges from the installed package preserving a custom .env value; BXM's vendored engine scaffolds the shipped defaults tree — brand.id + liveReloadPort survive the omega.json5 merge, theme arrives; self-containment greps clean; both bins dispatch).
    * [x] Task 1.6a: cli-router slice — `createCliRouter({ commandsDir, aliases, defaultCommand='setup' })` (src/cli-router.js + `./cli-router` export) extracted from the drift-identical ×3 UJM/BXM/EM router: positional command → alias table → flag-style alias → default; loads `commands/<name>.js`; missing command file errors cleanly; command errors log with the command name + rethrow to the bin. `Main.config` exposes the resolved dispatch table for structure tests (no more source-scraping). ADOPTED BXM + EM: each src/cli.js → thin wrapper owning only its alias table + commandsDir (dotenv line stays framework-side; EM's bin-side ELECTRON_RUN_AS_NODE strip untouched); the vendor scan auto-picked up the new module (devkit ×7 into both dists). EM's build/cli.test.js alias-integrity test was the ONLY breakage (it regex-scraped `const ALIASES` from dist/cli.js) — rewritten to introspect `Main.config` from the installed class, now also covering defaultCommand; suite count unchanged. Normalization (licensed): EM's error line gains the command name (BXM's variant = the shared standard). BEM DOCUMENTED as NOT this router — colon-style utility commands dispatching stateful command classes is a different design, not duplication (devkit CLAUDE.md modules table says so). GATES GREEN: devkit suite 46 (+10: 9 router tests w/ fixture command modules + Main.config exposure), BXM 88 (unchanged), EM 751 passing / 5 designed skips (756 total unchanged), pack-smoke-cli-router 8/8 (router ships vendored in both tarballs; positional `version` + flag `-v` dispatch from scratch installs of BOTH bins; unknown command exits 1 with `Command "nope" not found`; Main.config introspection from installed dists; both dists self-containment-grep clean).
* [ ] Phase 2: Frontend bake-off → @omegajs/web (plan §4)
  * [x] Task 2.1: A0 baseline + shared corpus — spikes/bakeoff-shared created (private workspace package). **REAL somiibo-website Jekyll baseline MEASURED FIRST (read-only): 332s cold `npm run build`** — jekyll 2.98min (self-reported 178.23s; 1,030 posts + 105 pages + defaults → 2,608 HTML files), sass+PurgeCSS 1.99min, imagemin 1.82min (WARM cache: 1,783/1,936 hits from cache-uj-imagemin), minifyHtml 24s, webpack 12s, translation 0.4s (warm cache), clean+setup ~9s (tasks overlap; M1 Max/10-core/64GB, ruby 4.0.0/jekyll 4.4.1, UJM 1.9.31 = npm latest so no setup self-update; site resolved via omega-manager `npm run brands -- --path=website`; git status delta after run = the SAME 4 pre-existing dirty files, zero new tracked modifications; note: UJM's build pushes its imagemin cache to the site's dedicated `cache-uj-imagemin` branch — the pipeline's own designed behavior). Full method + env + content-shape + targets in BASELINE.md. CONTENT SHAPE MEASURED (corrected the plan's guess): posts live in src/_posts/{2017..2026,other,seo} (1,030 — the "1031" was this ±1); pages are NOT mostly blueprint pages — 59× platform-bot + 32× solution + 5× package CUSTOM-layout data-frontmatter pages (70–120-line YAML: nested card arrays, HTML-in-YAML, 199 `{{ site.* }}` refs = the variable-resolver load) + 6× theme-bracket .html + 3 blueprint singles; collections = 20 _alternatives only (_team/_updates empty). GENERATOR: src/corpus-spec.js = SSOT of every measured number; generate-corpus.js (seeded mulberry32, byte-reproducible, no Date.now/Math.random) emits _posts (exact per-dir counts, YYYY-MM-DD-slug.md convention, word quartiles generated {360,891,1072,1852,7566} vs real {346,875,1017,1797,7522}), pages (exact archetype mix into the real subdir tree), _alternatives, site-data.json (the candidates' site.* source), corpus-manifest.json (deterministic, no timestamps); corpus/ output gitignored — the generator is the artifact. bench.js = hyperfine-style repeated-run harness (zero deps; hyperfine not installed). RESULTS.md = A2 scorecard skeleton (weights, <0.5/5→Eleventy rule, A1 slice checklist incl. HTTPS dev-server decision). Bug caught by the suite: solution-page labels collided (adj/noun pools cycled in lockstep → 32 solutions collapsed to 12 unique paths, files silently overwritten) — fixed with lap-shifted noun index; test asserts 99 md + 6 html exactly. GATES: bakeoff-shared suite 4/4 (spec counts + per-dir distribution, byte-determinism twice + seed sensitivity, filename/frontmatter contract, layout mix + stressor + site-data), devkit 69 re-run green, lockfile delta 14 lines (new workspace only).
  * [x] Task 2.2: @omegajs/template-kit + toSiteGlobal() — COMPLETE. AUDIT FIRST (all read-only): jekyll-uj-powertools = 13 filters (lib/filters/main.rb) + 16 tags (4 registered UNPREFIXED: iftruthy/iffalsy/iffile/urlmatches) + variable_resolver helper; usage ranking across UJM src + somiibo src: uj_icon 612, iftruthy ~370, uj_content_format 97, iffalsy 72, uj_liquify 71, uj_member 62 … (uj_increment_return/uj_random/append_param/iffile near-zero but ported anyway — complete surface); Jekyll-specific filter usage is TINY (only slugify/date_to_xmlschema/jsonify/strip_html/markdownify/push — somiibo uses ZERO); site.* usage: brand 265/url 161/theme 101 + socials/download/translation/extension/analytics/web_manager… = the resolved omega.json5 shape (engine-owned posts/pages/data/time excluded). BUILT packages/template-kit (private, CJS, node:test): src/filters.js (10 pure + 3 factories: createLiquify/createContentFormat/createIncrementReturn), src/tags/{conditionals,content,media,collections}.js + index (engine-neutral defs `{block, render(ctx, markup, renderInner)}`; ctx = lookup/page/site accessors/options; icon+logo SVG roots INJECTABLE — Ruby hardcoded node_modules/ultimate-jekyll-manager; member/post image-tag calls shared buildImageHtml() directly instead of re-parsing a {% uj_image %} string), src/jekyll-compat.js (slugify, date_to_xmlschema, date_to_rfc822, jsonify, strip_html, number_of_words, subset where_exp/group_by_exp + relative_url/absolute_url/markdownify factories), src/register-liquid.js (LiquidJS adapter: getSync-based lookup, getAll-scope async liquify, WeakMap increment counter, block-tag token collection, buildTagCtx), src/data/* MACHINE-EXTRACTED from the Ruby (184 languages incl. escape handling, 43 flag mappings, 15 social patterns — regenerate-don't-hand-edit headers). RUBY PARITY PINNED vs real ruby 4.0.0: uj_hash byte-exact (994@hello/1000, 305@somiibo/360 — BigInt full-hexdigest), title_case confirmed, uj_commaify DELIBERATE divergence (Ruby groups decimal digits '1234.5678'→'1,234.5,678' = latent bug; ours '1,234.5678'; unknown-language echo is DOWNCASED like Ruby). toSiteGlobal(resolvedConfig) in packages/config src/site-global.js (+ ./site-global export): identity map, strips targets/enabled, url := url||brand.url, baseurl default — 3 new tests. Docs: docs/template-kit.md (inventory tables, adapter contract, deliberate-exclusions: generators/hooks are ENGINE features for A1). GATES: template-kit 41/41 (filters incl. parity pins; tag semantics vs fake ctx incl. SVG fixtures for icon brands-fallback/flag-mapping/logo id-prefixing; full adapter vs REAL liquidjs 10.27.1 — one first-run failure was a WRONG TEST expectation, code was Ruby-correct), config 38 (35+3), BXM 92 re-run (fastest vendored-config consumer — new file flows through prepare/vendor cleanly; site-global not required by any framework dist so no re-vendor needed), CI + template-kit & bakeoff-shared steps (bakeoff step was missed in ckpt 24 — wired now).
  * [x] Task 2.3: A1 candidate spikes — bakeoff-eleventy + bakeoff-astro build the identical slice against the corpus (checklist in spikes/bakeoff-shared/RESULTS.md) — **BOTH CANDIDATES COMPLETE**
    * [x] Task 2.3a: spikes/bakeoff-eleventy — the COMPLETE A1 slice on Eleventy 3.1.6. **COLD FULL BUILD 3.60s mean (σ0.025, ×3+warmup) vs 332s Jekyll = ~92×**; 1,279 HTML files (1,030 posts + 105 pages + 20 alternatives + 103 blog-index + 18 taxonomy + 3 defaults); phases: esbuild+sass 0.60s / Eleventy 2.64s / PurgeCSS 0.42s; watch reload ~1.9–2.0s (CAVEAT recorded: --incremental doesn't narrow under collection-paginated templates — all 1,279 re-render; B-phase optimization). LOAD-BEARING BETS ALL VALIDATED (probed FIRST, before the slice): v3 virtual templates as LAYERED layouts (zero copying; cross-layer layout chaining; extensionless Jekyll names resolve against virtuals — TemplateLayoutPathResolver consults virtualTemplates), jekyllInclude + root:-array layered includes, registerLiquid via amendLibrary, data cascade IS page.resolved (layout frontmatter deep-merges under page frontmatter natively; eleventyComputed.resolved alias + mechanical `page.resolved.`→`resolved.` rewrite covers UJM's 967 refs — `layout.*` refs measured ZERO). Symlink FARM = the DEV mode (virtual content is captured at config time → not watchable; farm must live OUTSIDE the input dir or Eleventy processes the symlinks as content). ENGINE CORE src/omega-web.js: layered theme chain (active→classy→core), frontmatter {{ site.* }} preprocessor (cached per raw string; skips permalink/pagination + machinery keys — walking the full cascade crashes on other templates' raw content; WeakSet cycle guard), legacy `[ site.theme.id ]` bracket-layout via addLayoutAlias TABLE (layout values resolve BEFORE preprocessors — a data transform can never catch them), Jekyll conventions (dated filenames → /blog/:slug/ via fileSlug; /about → /about/; timezoneOffset 0 = CI-parity dates), collections + taxonomy from post.categories/tags, consumer-suppressible virtual DEFAULT pages (signin/signup/404/blog/blog-category/blog-tag/about; suppression = cheap permalink scan — corpus about.md verified to beat the default). SLICE: blueprint index/pricing (Liquid resolve-plan math: $8/mo billed annually, save 33%), auth pages bundling the REAL @omegajs/client via `web-manager` esbuild alias (3.3MB IIFE, bundles clean), dusk theme (overrides head include + 404 layout + _variables.scss; classy fills gaps), 3-layer page modules (site>theme>core, content-hashed manifest, pageModule frontmatter cascades from layouts), sass layering via `omega:` SCHEME importer (REAL FINDING: bare @use resolves file-relative BEFORE loadPaths — loadPaths can never layer), PurgeCSS post-pass (unused selectors stripped, verified). HTTPS DEV DECIDED: dev-server native https:{key,cert} (+http/2) w/ mkcert — no BrowserSync proxy. GATES: spike suite 17/17 (13 slice invariants through REAL Eleventy incl. dusk layering + farm-vs-virtual byte-parity — needed setUseTemplateCache(false), Eleventy's module-level layout cache leaks across same-process builds; 4 asset tests incl. real-client bundle + purge), full corpus build + spot-checks (plans/icons/readtime/taxonomy/manifest wiring), CI + bakeoff-eleventy step, RESULTS.md Eleventy column + checklist filled, spike README = architecture + 8 migration findings.
    * [x] Task 2.3b: spikes/bakeoff-astro — the COMPLETE A1 slice on Astro 5.18.2. **COLD FULL BUILD 5.56s mean (σ0.40, ×3+warmup) vs 3.60s Eleventy / 332s Jekyll (~60×)**; 1,279 HTML files — **URL SET IDENTICAL to the Eleventy build** (find-diff verified); phases: assets 0.15s / Astro 4.60s / PurgeCSS 0.36s (wiping the persistent `.astro/` store: no material difference at 5.11s; the first-ever build after install ran ~17s = cold fs caches, one-time). LOAD-BEARING BETS ALL VALIDATED (the first mini-fixture probe build validated everything at once): `omega:` Vite resolveId plugin = cross-layer component imports (classy base picks up dusk's head override, zero copying) + import.meta.glob layout dispatch for Jekyll `layout:` names (bracket hack normalizes in plain JS — no alias table, WE own dispatch); content collections over the consumer dir; `.astro` layouts `export const defaults` + manual chain merge (package→solution→base 3-deep proven) + computeResolved() deep-merge = page.resolved equivalent; template-kit as DIRECT imports (filters as plain functions; tags via a thin ctx builder mirroring registerLiquid's ctx); default pages in TWO shapes (content-shaped: defaults collection + catch-all getStaticPaths suppression — mini about.md beats the default; route-shaped blog/pagination/taxonomy: integration injectRoute, suppression = don't inject); SHARED asset pipeline staged then copied post-build (astro clears its outDir). HONEST FINDINGS (README, 9): layouts port Liquid→.astro BY HAND (~200 custom files / 49 sites — THE dominant migration cost) while CONTENT stays Liquid (full liquidjs+template-kit engine ships in the build regardless — asymmetry recorded); glob loader has NO .html entry type (custom frontmatter loader); linked CJS workspace pkgs need vite.ssr.external; SSR chunks carry the CHUNK's import.meta.url (paths anchored at config time via OMEGA_SPIKE env — this silently swallowed the asset manifest AND real icon SVGs until fixed); NO incremental static rebuild (rebuild = full ~5.6s; dev on-demand: ~16.5s boot over the corpus, then <50ms warm renders, 0.7–1.3s single-page re-renders after touches — slow boot/instant navigation vs Eleventy's fast boot/~2s full re-render); HTTPS native (vite server.https). DRY EXTRACTION landed with it: assets.js + layers.js (collectLayered) + frontmatter-liquid.js + consumer-scan.js + the mini-site FIXTURE moved into bakeoff-shared — both candidates build IDENTICAL consumer content through the IDENTICAL pipeline. GATES: astro 13/13 (each test a REAL `astro build`; suite ~25s vs eleventy ~1s — recorded as A2 data), eleventy 17/17 re-gated post-extraction, bakeoff-shared 4/4, template-kit 41/41, corpus spot-checks (pricing math, 103 blog pages, 6 categories + 12 tags, 3-deep chain, real @omegajs/client bundle in signin, icon BYTE-PARITY with eleventy output), CI + bakeoff-astro step, RESULTS.md Astro column + checklist filled.
  * [x] Task 2.4: A2 scoring + decision memo — **DECISION: ELEVENTY WINS, 4.70 vs 3.55 (margin 1.15; the <0.5 default rule never fired)**. Memo: spikes/bakeoff-shared/DECISION.md; scorecard + raw data: RESULTS.md. THE MIGRATION-COST DIMENSION IS REAL DATA: ported 3 REAL files into BOTH candidates (UJM classy contact 337L, sweet-saucy recipe 412L + the real adsense include 65L, somiibo index 456L + hero-demo include 77L = 1,347 lines) against a new shared fixtures/ports-site (real UJM team doc alex-raeburn, real recipe doc verbatim; mirrored 5+4 tests incl. full JSON-LD Recipe schema parse, uj_member vs real team collection, adsense include params). RESULT: Eleventy = 43 single-line MECHANICAL edits (100% regex-able, 0 hand-rewrites) vs Astro = ~700 lines HAND-REWRITTEN (.astro) — content files 0 edits in both. CENSUS (46 site repos): consumer 132 custom layout/include files (18.9k lines) + framework 115 UJM theme files (19.7k lines) → Astro bill ≈ 247 files/38.6k lines by hand; Eleventy bill = codemod pass. 8 CODEMOD RULES extracted (the B4 'omega migrate' conversion-plan seed — page.resolved→resolved, bracket layouts, tag-arg interpolation hoist [found a LIVE somiibo production bug: class="fa text- display-4"], forloop.parentloop hoists [LiquidJS gap], include leading-slash, page.<key> forms, canonical, per-collection defaults]. OUTPUT-PARITY SWEEP: **1,279/1,279 corpus pages normalized-identical ACROSS candidates** after fixing: Astro date zero-padding (%d), Astro auto-escaping vs Liquid's never-escapes (set:html audits — XSS-policy finding), same-date post tie-breaks (both engines now date desc + slug), taxonomy meta titles + tag #name headings. ENGINE FEATURES BOTH needed (→ @omegajs/web requirements regardless): team collection for uj_member, consumer _includes in Liquid roots (EXISTING dirs only — a nonexistent root cost +1.05s/corpus in LiquidJS stat probes, caught by A2 re-bench + worktree-baseline bisect), page-scoped frontmatter Liquid ({{ page.recipe.title }} in layout meta defers to per-page COW render — preprocessor sees the FULL cascade and layout data is SHARED across pages; renderData() treats Eleventy template objects as opaque), UTC date filters (date_to_xmlschema/rfc822 flipped to CI-parity UTC in template-kit, test tightened), deterministic post sort. RE-BENCHED post-changes: Eleventy 3.52s (σ0.035), Astro 5.31s (σ0.176). GATES: eleventy 22/22 (17 + 5 ports), astro 17/17 (13 + 4 ports; suite serialized — parallel test files collide on the shared .astro store), bakeoff-shared 4/4, template-kit 41/41. DISPOSITION: Phase B builds @omegajs/web on Eleventy 3 promoting the spike engine core; astro spike stays as reference; template-kit/config/shared-pipeline/codemod rules carry regardless.
  * [x] Task 2.5: **B1 engine core — packages/web (@omegajs/web@0.1.0) EXISTS.** Promoted per DECISION.md disposition: the spike engine (omega-web.js) split into focused modules — src/engine.js (configureOmega: Liquid opts + template-kit registration, layered layouts, legacy aliases, frontmatter preprocessor, `resolved` computed, default pages, globals), src/collections.js (posts/alternatives/team + taxonomy, shared byDateThenSlug order), src/layouts.js (virtual/farm delivery, ex-themes.js) — PLUS the four SSG-agnostic modules moved in from bakeoff-shared (frontmatter-liquid.js, layers.js, consumer-scan.js, assets.js) and NEW src/build.js (`buildSite()`: assets → Eleventy → PurgeCSS orchestration with per-phase timings — the `omega build` seed) + src/paths.js (packaged content locations; configureOmega/buildSite now DEFAULT themesDir/coreDir/defaultsDir to the package's own themes/ defaults/ core/, all moved in from the spike as the B2 seed). Full test suite moved with the package: 22/22 (13 slice + 4 assets + 5 real-layout ports) + fixtures (mini-site, ports-site, site-assets) under packages/web/test/fixtures — test glob narrowed to test/*.test.js (node --test treats EVERY .js under test/ as a test file; it executed the site-assets fixture). Package private:true until first publish (B3), subpath exports for every module. SPIKES now thin: bakeoff-eleventy = bench harness only (build.js wires corpus → buildSite, prints OMEGA_TIMINGS; bench touch-targets point at packages/web/themes; eleventy.config.js dev-serve via @omegajs/web; test script removed), bakeoff-astro = retained reference importing @omegajs/web/{frontmatter-liquid,consumer-scan,assets} + the moved fixtures (ssr.external += @omegajs/web), bakeoff-shared = corpus generator + bench + records only (deps slimmed to fs-jetpack). CI: bakeoff-eleventy step → web suite step. GATES ALL GREEN: web 22/22, astro 17/17 (serialized), shared 4/4, template-kit 41/41, config 38/38; corpus build through the promoted package = **1,279 HTML files, complete 4-module manifest, site-assets layer wins ('consumer wins' in bundle), ~3.6–3.9s** (vs recorded 3.52s — machine under load; zero hot-path changes, pure module split); dev config smoke-loads.
  * [x] Task 2.6: **B2 — the REAL UJM content is IN packages/web.** Port scope (521 files copied, 127 Liquid-transformed by the scripted codemod — rules 1/2/5 + new: layout `themes/[…]`+`themes/<id>` prefix strips, `layout: none` drop, body-only `page.<key>`→`resolved.<key>` (83 refs), `page.canonical.*`→`site.url`/`page.url`): core layer gains `_layouts/{blueprint(45),core/root,modules}` + `_includes` chrome (head/body/foot manifest-adapted; css/js bundle blocks → assetManifest + engine `pageAssets` computed) + real UJM css tree (main.scss ex-ultimate-jekyll-manager.scss via `omega:theme`) + real UJM js runtime (main.js ex-ultimate-jekyll-manager.js w/ static conditional imports; core/libs/modules/pages trees) + default nav/footer/account data JSONs; themes/ = classy FULL (frontend+backend+admin) + neobrutalism + newsflash (layer-fallback to classy) + bootstrap (vendored sibling scss/js) + _template; defaults/pages = ~60 real pages (404 at literal /404.html, paginated blog size 6 `/blog/page/N.html`, categories/tags index + per-term generator pages over aggregateTaxonomy). ENGINE: consumer `_layouts` top layer + content-ignore (cwd-relative glob — `../` input gotcha), core as layout layer, generalized alias table (every layout × bracket/hardcoded spellings), `resolved` = inject-properties parity (site seed w/ exclusions, deepMerge, `resolved`+`page.resolved` in COW context), site.posts/team/updates/alternatives as in-place-synced arrays (Eleventy captures key set at data-init — getters/reassignment invisible), site.data._includes from layered include-root JSONs, paginator compat (posts/page/total_pages/prev/next), jekyll.environment + site.uj defaults, updates collection + /team/:title //updates/:title permalinks (explicit permalink wins; '' = computed-proxy artifact counts as absent). ASSETS: layer-root conventions (js|css main + pages, _theme.*), entry rule (index.* or ≤2 segments below pages/), boot stubs (import + call default fn w/ webManager), __main_assets__→core|themes, __theme__→active theme, web-manager→client DIR (subpaths), manifest v2 {js:{main,pages},css:{main,pages,themePages}}, per-page css compiled w/ owner-root loadPaths. FOUR REAL BUGS: template-kit lazy block bodies (falsy iftruthy ran inner {% assign %} — silently emptied every title; adapter now probes, renders body only on request), jsonify(undefined)→'null', permalink '' proxy artifact, templateContent-getter walks + missing LiquidJS `cache:true` (corpus eleventy 40.6s→16.7s, profiled). uj_post title/description now prefer post.post.* (UJM posts carry the real title there; Jekyll only had filename-derived data.title). TESTS: web 30/30 — slice rewritten vs real content (neobrutalism layering via real fallback, real markers), ports fixture owns contact/recipe as consumer-local _layouts, assets suite on new API, NEW contract.test.js (3 themes × full default set = 66 pages each, key URLs, taxonomy/collection pages, md legal layouts, manifest injection, zero unresolved Liquid, redirect module) + contract-site fixture; astro 17/17 (B1 pipeline frozen+vendored at src/assets-pipeline.js); template-kit 41/41 (lazy-tag + jsonify + uj_post pins updated); corpus harness: replica consumer layouts checked into bakeoff-shared/fixtures/consumer-layouts + generator copies them into corpus/_layouts, bench touch targets → real paths. B2 corpus baseline: 1,397 files, cold ×3 mean 20.40s σ0.97 (real chrome; the 3.52s A2 record was the synthetic slice — engine per-page ~12.7ms vs Jekyll ~68.6ms ≈ 5.4×).
  * [x] Task 2.7: **B3 — the `omega` CLI + scaffolding + ESM boot runtime + Ruby-free CI are IN packages/web.** CLI: bin/omega (yargs, boolean-flag fix, yargs version/help disabled so `-v` routes to our command) → src/cli.js (devkit createCliRouter — SSOT, no new router; dotenv from consumer root) → src/commands/: setup (node-version warn, scaffoldDefaults, package.json scripts sync {setup,start,build,test,clean,deploy}→omega + engines.node), dev (dev-mode assets: stable UN-HASHED names + no minify + expanded css → manifest → fs.watch(recursive) on consumer assets + theme/core layers rebuilding IN PLACE — URLs stable so HTML stays valid — + programmatic Eleventy init/watch/serve, --port; found: elev.watch() requires elev.init() first, write() self-inits but watch() doesn't), build (loadConfig→toSiteGlobal→buildSite, environment production, manifest to .omega/), test (production build + smoke: pages>0, 404.html w/ data-theme-id — 404 is the GUARANTEED default, consumers own their home page — manifest js.main on disk; then consumer test/ via node --test), deploy (UJM parity: refuse file: deps → npm run build → npu sync), clean (dist/+.omega/), version, translate/audit = EXPLICIT not-ported-yet stubs (exit 1 + pointer; the subsystems ride later checkpoints). SCAFFOLD (scaffold/ tree + src/scaffold.js FILE_MAP over devkit defaults-engine, BEM-mirror): `**/*` overwrite:false; marker merges CLAUDE.md/_.gitignore/_.env; config/omega.json5 merge:true (JSON5 defaults-merge, consumer wins — validated seed: brand/theme/targets.web passes loadConfig clean); build.yml+.nvmrc re-template every run ({{ versions.node }}=24; `${{ secrets.* }}` survives the tolerant renderer); src/{pages,assets/{js/pages,css/pages,images}} seeded via .gitkeep (NO _posts seed — devkit archive-dir rule skips `_`-prefixed non-final segments); NO pages copied, NO Gemfile/_config.yml. Ruby-free CI template: checkout→setup-node→sfw npm install (retry×3)→`npx omega setup && npm run build`→peaceiris gh-pages publish_dir ./dist. BOOT RUNTIME (runtime/boot.js + runtime/manager.js, replaces B2 stubs): assets.js switched IIFE→**ESM + splitting** (chunkNames chunks/) — web-manager + boot runtime land in ONE shared chunk evaluated once per page, so `import webManager from 'web-manager'` in ANY of the 48 page modules is the SAME initialized singleton (module-scoped client singleton would otherwise split-brain across separate bundles — webpack's single graph reproduced with `<script type=module>` document-order-deferred semantics; foot.html emits both as type=module); handshake bootMain(mod)=initialize(window.Configuration)→dev lib (development only, code-split dynamic import)→global module (errors logged, don't block pages), bootPage(mod)=await main boot→mod({manager,options}) w/ frontend Manager (webManager + mode helpers off Configuration.environment) + options.paths.pagePath from data-page-path; page bundles now ~330-byte stubs (signin 329B vs ~1MB inlined client before). LIVE-VERIFIED end-to-end in a bare temp consumer: setup scaffolds 6 files + dirs + scripts sync → **build renders 56 default pages in 1.65s** → test smoke passes → dev serves /404.html + /pricing/ (brand from omega.json5) on --port → clean; marker merges preserve custom lines; rerun 0 written (idempotent; omega.json5 stabilizes after one merge cycle). GATES: web **38/38** (13 slice + 6 assets incl. ESM-splitting singleton-in-ONE-chunk + dev-mode stable names + graph-reader over dynamic chunks + 7 NEW cli/scaffold incl. real-bin e2e spawn + 5 ports + 7 contract w/ type=module pin), template-kit 41/41, config 38/38, devkit 69/69, shared 4/4, astro 17/17; corpus spot-check post-ESM: 1,397 files 18.6s (assets 0.91s — thin stubs are FASTER; within B2 baseline range). packages/web exports += consumer/scaffold; index += resolveClientEntry/consumerPaths/loadSiteData/scaffoldDefaults; deps += devkit/dotenv/fs-jetpack/yargs.
  * [x] Task 2.8: **B4 — `omega migrate` + liquid-lint are IN packages/web, live-proven on real consumers.** PIPELINE (src/migrate/: index orchestrator, config-convert, rules, codemod walker, lint, consumer-assets + commands/migrate.js, aliases migrate[-m,migration], bin booleans += check/dry-run; `--check` = full report in memory, ZERO writes): (1) CONFIG — src/_config.yml + config/ultimate-jekyll-manager.json → config/omega.json5 (js-yaml + JSON5 deps added): url→brand.url, brand/theme/oauth2 verbatim, flat analytics→providers.<p>.id, web_manager.firebase.app.config→firebaseConfig, web_manager.payment→payment (credential `false` = legacy disabled → dropped w/ note — REAL somiibo finding, stripe.publishableKey:false failed the string schema), web_manager rest→targets.web.web_manager WHOLE (client blob), meta/socials/download/extension/favicon/manifest/icons/recaptcha/cloudflare/translation + permalink/pagination/collections/defaults/generators→targets.web.* (rule 8's home), UJM-json distribute/purgecss/imagemin/workflows→targets.web.*, webpack/gems/Jekyll-machinery dropped w/ notes, findSecretKeys pre-strip; written config validated via loadConfig(root,'web') into the report; mapping table → docs/config.md. (2) CODEMOD — the 8 DECISION.md rules EXECUTABLE (rules.js, pure text→text, ordered: page.resolved→resolved; bracket/hardcoded theme layout prefixes (layout-line-scoped); interpolated quoted tk-tag args → {% capture %} hoist SCOPED TO THE TAG SPAN (HTML-attr `{{ }}` on the same line untouched; tk tag list from template-kit registry); forloop.parentloop → depth-scoped uj_parentloop<d>_<prop> assigns via TOKEN-AWARE for/endfor stack (same-line opens/ref ambiguity + parent-and-inner-share-a-line → error findings, never mis-hoisted; raw/comment spans inert); include leading-slash strip; page.canonical.url→{{ site.url }}{{ page.url }} (before rule 6); page.content→content, page.slug→page.fileSlug, page.<key>→<key> w/ negative lookbehind `(?<![\w./-])` (words-on-a-page.png false positive found on REAL somiibo posts) + Eleventy-valid props pass + next/previous/collection flagged-no-fix; flat site|resolved.analytics.<p> → providers spelling). (3) LINT — known filter/tag names derived AT RUNTIME from the real Liquid+registerLiquid path (SSOT, no static list), + end-forms + Eleventy extras; Jekyll-only tags (post_url/link/highlight/…) = errors w/ hints; unknown filters/tags = warnings (REAL find: sweet-saucy `| limit: 3` — broken upstream too, Jekyll has no limit filter); raw/comment spans inert; quoted-string pipes not filters. (4) CONSUMER ASSETS — seed-identical src/assets/js/main.js DELETED (comment/ws-insensitive vs the UJM scaffold; core main + boot runtime replace it; customized → error w/ port recipe `import coreMain from '__main_assets__/js/main.js'` + export default wrapper); main.scss `@use 'ultimate-jekyll-manager' with (…)` → `@use 'omega:main' with (…)` (assets.js layeredFileImporter now SKIPS the requesting file via containingUrl, so a consumer main.scss configures the layers below; with() flows through core main's `@forward 'omega:theme'` — consumer $primary PROVEN in compiled css); page-css self-`@use 'pages/<key>'` DROPPED (theme page css ships separately as pageAssets.themeCss — the @use was a module self-loop AND a double-load). (5) HYGIENE — src/_config.yml, config/ultimate-jekyll-manager.json, Gemfile, Gemfile.lock, .ruby-version removed; rerun reports already-migrated, edits idempotent. CHROME NORMALIZATION (best-implementation-wins): foot.html gtag/fbq/tiktok read analytics.providers.<p>.id; Configuration.analytics = flat bridge for the client (extension's package.js pattern); engine.js composes firebaseConfig→web_manager.firebase.app.config + payment→web_manager.payment (ONE home per value in config, runtime contract unchanged). TWO REAL-DATA ENGINE BUGS: taxonomy terms now dedupe by SLUG w/ most-frequent spelling as display name (somiibo ships "Marketing"×673 + "marketing"×11 → duplicate permalink hard-error; Jekyll silently last-write-won) + the LIVE somiibo icon bug `class="fa text- display-4"` — 12 latent copies of the interpolated-arg no-op found in the PACKAGED classy layouts by our own rule 3 and fixed (0 residual; capture hoist proven byte-identical render vs literal args through real registerLiquid). LIVE-VERIFIED: deployment-playground copy — migrate (config + seed main.js delete + 2 scss edits + 4 legacy files) → build **57 pages/2.33s** → verified $primary #5B47FB in compiled css, gtag id, firebaseConfig in Configuration; somiibo copy — migrate (**73 edits/23 files**, 1 honest error = customized main.js, ported per the recipe) → build **2,556 pages in 96–104s vs the 332s Jekyll baseline (~3.2× whole-pipeline, REAL site)** → marketing category built ONCE, pricing plans from migrated payment, broken icon class 0 occurrences (live Jekyll site still ships it). Dry-run on read-only sweet-saucy: 219 edits/23 files hitting EVERY rule family + the limit-filter find. GATES: web **52/52** (38 + 14 migrate incl. semantic render proofs, false-positive guards, check-mode zero-writes, real-loader config validation, composition-through-chrome), config 38, tk 41, devkit 69, shared 4, astro 17; corpus post-B4 **18.14s** (no regression). packages/web exports += ./migrate; index += runMigration. B5 note: somiibo 2,556 vs Jekyll's 2,608 files — the URL-set diff is `omega verify`'s first job.
  * [ ] Task 2.9 (PAUSED — Ian: no more migrators until asked): B5 — `omega verify --against <jekyll-dist>` parity harness (URL-set diff, DOM diff on key pages, redirect map) = the per-site migration gate; first target: the somiibo 2,556-vs-2,608 file-count gap. Also queued behind the pause: translate/audit subsystem ports, UJM-setup extras, engine consumption of targets.web.collections/defaults/generators, dev-loop live-reload + re-render narrowing.
* [ ] @omegajs/manager — omega-manager functionality in the monorepo (Ian's checkpoint-33 directive: idempotent all-services runs so a brand monorepo is fully working)
  * [x] Task M1 (checkpoint 33): **packages/manager core + first services.** Service-runner ported with the strict return contract (existence-checked handler loading — broken handlers now throw loudly instead of being message-sniffed away; runner returns firstError), run-summary ported (update steps + testing check drill-downs, retry command → `npx omega-manager`), brand loader (loadConfig(root) whole-file + manager DEFAULTS layer + `{ domain }` templateObject port; app discovery: declared targets in app omega.json5 — incl. BEM's functions/config — else website*/backend*/… dir conventions; resolveBrandRoot mirrors @omegajs/config's is-app rule so apps/sandbox-brand under the monorepo's own apps/ resolves — REAL bug found by the first live run), `.omega/` state store (three-bucket principle relocated: omega.json5 = choices, state.json = durable IDs, runs/{ts}.json = transients), manage orchestrator (per-service state persistence, stop-on-error w/ --continue-on-error, exit 1 on errors), CLI via devkit router (bin omega-manager; manage default). Services: workspace (apps/* workspaces + app-per-enabled-target w/ create-this-dir guidance + brand config = hard gate/app configs = warnings + idempotent .omega/ gitignore), update (ONE resolution-gated npm install at brand root — node_modules-climb probe per declared dep, so nested brands never grow stray installs — then per-app `npm run build`, no-script = honest skip; omega-manager-compatible steps output), testing (web → dist/index.html, backend → firebase.json + functions/package.json, all → package.json parses). GATES: manager suite **27/27** (runner contract incl. leak-key violation + warned-stickiness, brand loading incl. nested-brand + secret-key failure + declared-beats-convention, e2e fixture manage: full loop w/ real npm-run-build, idempotent rerun w/ single gitignore entry, dry-run honesty — testing reports the unbuilt site, missing-app error, --service filter); LIVE on apps/sandbox-brand: dry-run + 2 real runs — backend→backend + website→web mapped, install skipped (deps resolve through the monorepo), website builds via its own build.js, 5/5 health checks, exactly one .omega/ gitignore entry after 3 runs. CI += manager suite step.
  * [ ] Task M2: port external provisioning services in omega-manager's order. Each port: service dir + schema/defaults migration + state in .omega/state.json + suite coverage; schema-prompting (ensureSchemaFields) rides these ports.
    * [x] Task M2a (checkpoint 34): **github service** — org/repo/pages reconciled to omega.json5 `github: {}` via the gh CLI (gh auth login or GH_TOKEN in brand .env). org: diff-then-patch profile (name, support@{domain} + billing email, 160-cap description, blog; `location` only when configured — omega-manager's hardcoded country now belongs to config; owner-not-an-org = note not error; `shared: true` filters the op so one brand never rewrites a shared org). repo: THE brand-monorepo repo (github.repo, default brand id) — created EMPTY when missing (user pushes; no clone/template — onboarding's job) w/ visibility/description/homepage, existing = exact-drift patch (`-F` typed booleans, fixing omega-manager's string-"true" PATCH), identity → .omega/state.json. pages: gh-pages source + cname from brand.url for web brands (no gh-pages branch = deploy-first note; subdomain Pages repos don't map to one-repo brands — parked for the cloudflare port). --dry-run = reads only, all drift reported as `planned`, ZERO mutations. DEFAULTS.github { shared:false, private:true }; SERVICE_ORDER = workspace → github → update → testing; no github.org = clean skip. Old-world clone/GitHub-Desktop/setup-command behaviors ride onboarding/company-mode. GATES: manager suite 27→**42** (recording fake API: converged-brand zero-mutation no-op, exact-drift patches, description truncation, shared filtering, repo-name override, deploy-first pages, dry-run zero-mutation; manage e2e proves the no-config skip in the full loop); LIVE ×2: sandbox-brand full run (github skips w/ guidance, 3 passed/1 skipped, zero repo changes) + read-only dry-run vs REAL itw-creative-works/ultimate-jekyll (real diff public→private withheld, shared-org filter, real branch probe, zero writes).
    * [x] Task M2b (checkpoint 35): **cloudflare service** — all 12 operations ported (zone, dns-records, email-routing, zone-settings, cache-rules, rules-managed-transforms, rules-redirect, rules-configuration, rules-response-headers, rules-security, speed-scheduled-tests, workers) with the read→diff→write pattern; reads cache to .omega/cache/cloudflare/{op}.json (the .output read-cache relocated). Non-interactive by design: pending zones report nameservers and move on, unverified email destinations get a verification email + warned (browser-open/poll flows ride the prompting port). BEST-IMPL FIXES: zone created THIS run visible to later ops via serviceData (omega-manager read the setup-time null zoneId on first runs — getZoneId helper); custom TXT records are ADDITIVE + required-TXT existence matches by kind (spf/dmarc/bimi/exact-content) — caught by the suite: the de-ITW'd Ahrefs-as-custom-record would otherwise have silently suppressed the apex SPF default (type+name override), and route MX/A two-run convergence is pinned as faithful behavior. DE-ITW'D (hardcode → config): dns.dmarcReports {rua,ruf}, dns.bimiLogo, dns.sendgrid {id,whitelabel}, dns.spfIncludes, verification TXTs via dns.records, CSP default without itwcreativeworks.com hosts, org location already config-gated — Ian's values land in company/brand config at migration. Route templating via manager templateObject (node-powertools dropped); psl dep for apex/subdomain detection; subdomain brands filter to zone+dns-records on the parent zone. DEFAULTS gained cloudflare (143-line settings/rules block) + domain (provider/email) sections; SERVICE_ORDER: workspace → github → cloudflare → update → testing. --dry-run = reads only across all 12 ops. GATES: manager suite 42→**62** (converged-zone zero-mutation no-op across all 12 ops, DNS drift + obsolete-MX cleanup + additive-TXT regression, ruleset create-vs-update, read-only settings skip, email converged/unverified, subdomain filtering, per-op dry-run); manage e2e proves no-token skip + scrubs shell CLOUDFLARE_TOKEN so fixtures can never reach a real account; LIVE sandbox run with token scrubbed (clean skip, 3 passed/2 skipped, zero repo changes — standing rule honored, no real Cloudflare touched).
    * [x] Task M2c (checkpoint 36): **domain service** — the registrar side of DNS, one operation (`nameservers`): the registrar's nameservers reconcile to the Cloudflare zone's assigned pair (closing the loop cloudflare's pending-zone report left open). namecheap via the Namecheap XML API (NAMECHEAP_USERNAME + NAMECHEAP_API_KEY in brand .env; no creds → clean skip w/ guidance; domain not in the account → warned, not failed; converged = zero-mutation no-op). Manual registrars (squarespace/unknown) BEST-IMPL upgrade: an ACTIVE zone is proof the nameservers are already set (success, no instructions — omega-manager printed them unconditionally even when converged); only a pending zone prints the values + returns warned. BUG FIXED in port: omega-manager's SLD/TLD split popped one dot-label (mybrand.co.uk → "mybrand.co"+"uk"; Namecheap wants "mybrand"+"co.uk") — now split via psl, pinned by test. --dry-run = reads only (drift → `planned`). Email half of `domain` config unchanged (cloudflare dns-records/email-routing consume it); registrar/forwarding schema prompts ride the prompting port. SERVICE_ORDER: workspace → github → cloudflare → domain → update → testing. GATES: manager suite 62→**76** (recording fakes for BOTH APIs: converged no-op, drift update w/ sorted NS + correct SLD/TLD, multi-part-TLD pin, not-in-account warn, manual active-vs-pending, no-zone/no-NS warns, credential-skip variants, dry-run zero-mutation); manage e2e scrubs NAMECHEAP_* alongside CLOUDFLARE_TOKEN + proves the no-provider skip in the full loop; LIVE sandbox run with all creds scrubbed (clean skip "no domain.provider configured", 3 passed/3 skipped, zero repo changes — standing rule honored, no real registrar or Cloudflare touched).
    * [x] Task M2d (checkpoint 37): **firebase service** — all 13 operations (billing, services, project-settings, oauth-consent, service-account, hosting, firestore, database, authentication, storage, functions, cloud-messaging, sdk-config). Auth: GOOGLE_CLIENT_ID/SECRET in brand .env via the ported GoogleOAuth2Client (src/lib/google-auth.js; tokens → .omega/auth/, auth URL printed, no `open` dep); no creds → clean skip; no firebase.projectId → skip (project creation rides onboarding). DE-ITW'D: firebase.billingAccount (omega-manager silently linked a HARDCODED company billing account — Spark + unconfigured now warns), firebase.supportEmail (googlegroup default → support@{domain} fallback), firebase.organizationId. BEST-IMPL idempotency: services lists-then-enables only missing APIs (was batch-enable every run), project name + web app diffed (was blind PATCH), Identity Platform initialized only when config absent (was POST every run), IAM grants diff-aware everywhere → converged project = zero-mutation no-op. NON-INTERACTIVE: hosting = one-pass converge (create domain, TXT/ACME via Cloudflare, CNAME unproxied till verified, pending → warned, reruns converge); Google sign-in + OAuth redirect URIs (no Google APIs exist) = instructions + warned, wrong-project client detected by project-number prefix; VAPID missing = console URL + warned. SECRETS: SA key → .omega/secrets/service-account.json + copied to backend app functions/ (BEM's spot); google-oauth.json ditto; never state/omega.json5. sdk-config: state + omega.json5 firebaseConfig drift check prints paste-able block + warns (auto-write awaits the config-serializer port). shared:true → service-account + sdk-config only. --dry-run = reads only across all 13. GATES: suite 76→**93** (method-level recording fake; converged zero-mutation no-op across all 13 ops, shared filter, billing warn-vs-link, missing-only enablement, key lifecycle incl. backend copy, auth diffs + manual-flow warns, firebaseConfig drift, hosting one-pass with A-records skipped, fully-drifted dry-run = zero mutations); manage e2e scrubs GOOGLE_*; LIVE sandbox with all creds scrubbed (clean skip, 3 passed/4 skipped, zero repo changes — standing rule honored).
    * [x] Task M2e (checkpoint 38): **recaptcha service** — one op (site-key). BEST-IMPL: omega-manager only printed a checkmark outside onboarding; the port PROVES the shared secret via the documented siteverify endpoint (throwaway-token probe: invalid-input-response = good secret, invalid-input-secret = bad → service errors with .env guidance). Classic reCAPTCHA has no key-management API → domain list stays printed guidance ({domain} + www.{domain} + console link); onboarding add-domain browser poll rides the prompting port. DE-ITW'D: hardcoded `?project=itw-creative-works` console link → `recaptcha.project` config (null default → generic classic admin console). Auth: RECAPTCHA_SITE_KEY + RECAPTCHA_SECRET_KEY in brand .env; missing → clean skip naming exactly the missing key. Never mutates — dry-run ≡ normal run by construction. GATES: suite 93→**102** (siteverify recording fake: valid = one read call, invalid errors, skip precision, both console URLs, dry-run parity); manage e2e scrubs RECAPTCHA_*; LIVE sandbox all creds scrubbed (clean skip, 3 passed/5 skipped, zero repo changes). CI manager-step label compacted to "external-service reconciliation suites".
    * [x] Task M2f (checkpoint 39): **analytics service** — all 4 ops (google-streams, google-firebase-link, meta-pixel, tiktok-pixel). One GA4 web stream per target (web=root, backend=api., desktop/extension/mobile=virtual subdomains; found by URI, renamed on drift); enhanced measurement DIFFED vs live v1alpha settings (omega-manager blind-PATCHed every run); clean MP secret per stream (regenerated until dash/underscore-free); acknowledgement gate → warned + settings URL (was browser poll); propertyId-not-found warn. Firebase link: wrong-property links moved to configured property (config = SSOT); different-project links warn; project NUMBER from brandState.firebase.sdkConfig.messagingSenderId (no second API client); sleep-retry ladder + Firebase-API fallback → warned-rerun-converges; "Web App" auto-stream normalized diff-gated; cross-link detector (firebase measurementId absent from property streams). Pixels: META_ACCESS_TOKEN/TIKTOK_ACCESS_TOKEN presence in brand .env (BEM's names) + guidance — prompts ride prompting port. Auth: GOOGLE creds, tokens → .omega/auth/google-analytics-tokens.json (separate scope from firebase). DELIBERATE NON-FEATURE: no property auto-create (would mint a new property every run until config-serializer writeback exists) — propertyId is required config; kills ITW shared-account default + LA/USD hardcodes. No propertyId/creds → google ops filtered, pixels still run; nothing configured → skip. GATES: suite 102→**119** (converged zero-mutation no-op across 4 ops, exact create/rename/patch/move assertions, fully-drifted dry-run = zero mutations); manage e2e scrubs META/TIKTOK; LIVE sandbox 9 creds scrubbed (clean skip, 3 passed/6 skipped, zero repo changes).
    * [x] Task M2g (checkpoint 40): **search-console service** — all 3 ops (property, ga-link, sitemaps). Property: sc-domain:{domain} existing = converged proof; missing = ONE-pass create (DNS_TXT token [idempotent] → TXT via Cloudflare, stale google-site-verification records replaced/SPF untouched → verify ONCE; propagating → warned + rerun converges — was an interactive spinner poll); no CF token → manual record + warned; subdomain brands verify at their label in the apex zone. ga-link: NO API exists on either side → firebase-oauth-redirect pattern (warned + associations URL until state gaLinked; confirm rides prompting port); no GA propertyId → silent nothing-to-do. Sitemaps: submit only MISSING (omega-manager resubmitted every run — converged brand mutated forever); searchConsole.sitemapPaths in DEFAULTS (SSOT, was in-code); no web target / submitSitemap:false → skip before read. Auth: GOOGLE creds, own token cache (.omega/auth/google-search-console-tokens.json, webmasters+siteverification scopes). DEAD CONFIG DROPPED: searchConsole.subdomains (read by nothing). GATES: suite 119→**135** (converged no-op, exact TXT POSTs @ apex + subdomain label, verify→add ordering, stale replacement, pending warn w/ downstream ops waiting, already-verified proceeds, manual fallback, ga-link warn/silence, missing-only submission, dry-run mints not even a token); manage e2e no-creds skip; LIVE sandbox all creds scrubbed (clean skip, 3 passed/7 skipped, zero repo changes).
    * [x] Task M2h (checkpoint 41): **adsense service** — the 1 op (sites). The Management API v2 is READ-ONLY (no site add/config writes exist — omega-manager itself only ever requested adsense.readonly), so the service is a pure verifier: domain present + READY = converged success; GETTING_READY/REQUIRES_REVIEW/NEEDS_ATTENTION → warned with what Google is waiting on; missing → warned + the exact add-site console deep-link, rerun converges once it appears (was an interactive openBrowserAndPoll loop — rides the prompting port). www.{domain} entries match the bare domain. DE-ITW'D: adsense.accountId required config, null default (omega-manager's schema defaulted every brand to the company's shared pub-7372939085773564). Dropped unused API methods: listAccounts (fed only the schema account-picker → prompting port), getSite (dead even in omega-manager). Auth: GOOGLE creds, own token cache (.omega/auth/google-adsense-tokens.json, adsense.readonly scope). Dry-run ≡ normal run by construction (the API has zero writes). GATES: suite 135→**145** first run (skips incl. null-default proof, READY one-read, warn ladder, www matching, missing-site deep-link w/ zero mutations, dry-run parity); manage e2e no-accountId skip; LIVE sandbox all creds scrubbed (clean adsense.accountId skip, 3 passed/8 skipped, zero repo changes).
    * [x] Task M2i (checkpoint 42): **sendgrid service** — all 6 ops (domain-auth, sender-identity, list, custom-fields, segments, event-webhook). domain-auth: valid = converged; missing = ONE pass (authenticate w/ automatic_security → 3 DKIM/bounce CNAMEs diff-synced into the CF apex zone [match untouched / wrong content PATCH / missing POST] → validate ONCE; propagating → warned + rerun converges — was a 10s poll-with-spinner); no CF token → manual records AND validation still attempted (manual fix converges). sender-identity: offers@{contact domain}; unverified leftovers recreated; DE-ITW'D: brand.address REQUIRED for CAN-SPAM (omega-manager silently stamped the company's Redondo Beach address on every brand) → warned when missing; dropped the i18n-iso-countries dep (country passes through). list: config listId → state listId → name lookup → create; id lands in STATE (config writeback rides the serializer port). custom-fields/segments: backend-manager's marketing SSOT required through the WORKSPACE package (src/lib/bem-marketing.js w/ fieldsFor/segmentsFor — kills omega-manager's ../.. filesystem climb into the sibling repo); type mismatch = delete+recreate; segment query_dsl rebuilt per run (lib/segment-query.js port) + diffed vs live detail → stale PATCH in place w/ delete+recreate fallback; __temp_ orphans swept; non-BEM entries never touched; verify reads → warned on persist failures. event-webhook: account-global → parent BEM forwarder; DE-ITW'D: `parent` default null (was the company URL), 'self' = this brand is the parent; 5 consent toggles managed, tracking toggles untouched, min-diff PATCH; BACKEND_MANAGER_WEBHOOK_KEY missing → warned. NOTE: BEM's SSOT still carries the company email in the test_admin segment — company data in backend-manager's file, fix belongs to the BEM side, the manager just provisions the dict. DEFAULTS: parent + marketing.campaigns. GATES: suite 145→**166** first run (converged zero-mutation across all 6, exact CNAME bodies + validate-once, manual fallback, sender payload + CAN-SPAM warn, 4-source list resolution, field/segment reconciliation incl. PATCH-rejection fallback + orphan sweep, webhook min-diff, fully-drifted dry-run = zero mutations on BOTH APIs); manage e2e scrubs 11 vars; LIVE sandbox all creds scrubbed (clean skip, 3 passed/9 skipped, zero repo changes).
    * [x] Task M2j (checkpoint 43): **beehiiv service** — all 4 ops (publication, custom-fields, segments, webhook). Publication: config id → state id → auto-match by brand name/id; inaccessible configured id → warned + GATES downstream ops (config is SSOT — never silently pick another publication); no create API → dashboard values printed (name/description/subdomain) + warned; select/create browser poll rides prompting port; id → state until serializer writeback. Custom-fields: bem-marketing SSOT bridge's SECOND consumer; diffed by DISPLAY (Beehiiv matches subscriber values by display); kind mismatch = delete+recreate; verify read → warned on persist failures. Segments: VERIFY-ONLY — Beehiiv has no segment-create API; missing → warned + human-readable per-condition dashboard instructions (formatCondition port); omega-manager's Chrome-extension browser automation (~500 lines of DOM driving) rides the extension port; op cannot mutate by construction. Webhook: per-publication but publications shared across siblings → always parent BEM forwarder; matched by managed description first (survives parent moves) then URL; min-diff PATCH (url/event_types/enabled); 3 consent events. DEFAULTS: marketing.newsletter (enabled/platform/publicationId) — omega-manager's newsletter.content generator blob NOT ported (BEM-side data, rides config-hierarchy dispersal). GATES: suite 166→**182** first run (converged zero-mutation across all 4, inaccessible-pub downstream gating proven w/ zero reads, auto-match into state, exact field creation payload, segments never-mutate even when drifted, description-matched min-diff webhook + exact create payload, dry-run parity); manage e2e scrubs 12 vars; LIVE sandbox all creds scrubbed (clean skip, 3 passed/10 skipped, zero repo changes).
    * [x] Task M2k (checkpoint 44): **payment service** — all 11 ops across three processors (paypal-account/webhook/products, stripe-account/radar/disputes/webhook/products, chargebee-account/webhook/products). Stripe: business-profile min-diff (unsupported account types warn; updateAccountInfo opt-out); products config → state → METADATA MATCH ({brandId, productId} stamped on creation — lost state self-heals without duplicates) → create; prices by interval+amount, stale actives archived + recreated; webhook URL-matched, re-enabled when auto-disabled, enabled_events diffed; radar + disputes have NO API → ga-link pattern (rules/action printed + Dashboard deep-links from state accountId, warned until radarConfirmed/disputesConfirmed; confirms ride prompting port). PayPal: parallel live/sandbox auth probe reports environment; products config → state → EXACT-NAME match → create; plans by interval+amount+trial with duplicates AND stale deactivated; legacyProductIds plans deactivated; webhook JSON-Patch event diff. Chargebee: DETERMINISTIC IDs (family=brandId, item={brandId}-{productId}, price=…-{interval}) → no state needed; prices updated in place; legacy plans read-only report; webhook &brand= URL, events create-only (API returns none); 'once' prices visibly not-managed (omega skipped silently). De-ITW: brand.images.brandmark replaces the hardcoded company CDN; company Stripe organizationId default GONE; interactive account setups → prompting port (creds: STRIPE_SECRET_KEY / clientId+PAYPAL_CLIENT_SECRET / site+CHARGEBEE_API_KEY; processor `false` = off). Best-impl: FULL dry-run guards (omega's stripe ops had none) + archived-product skip everywhere. --processor filter ported free (yargs). New dep: stripe SDK via npu. GATES: suite 182→**216** first run (converged zero-mutation across all 11, both self-heals, exact create payloads ×3 processors, drift reconciliation, webhook create/diff/re-enable ×3, warned-until-confirmed, fully-drifted dry-run = zero mutations on all 3 APIs); manage e2e scrubs 15 vars; LIVE sandbox all creds scrubbed (clean skip naming all three cred paths, 3 passed/11 skipped across 14 services, zero repo changes; the packages/backend disposable-domains auto-refresh triggered by the stripe install was reverted to keep the checkpoint surgical).
    * [x] Task M2l (checkpoint 45): **slapform service** — both ops (form, user) + the NEW shared `src/lib/firestore-rest.js` (Firestore REST + RS256 service-account JWT via node:crypto — NO firebase-admin dep, omega used the whole admin SDK; plain-JS docs, typed-value codec internal, getDoc/patchDoc surface faked method-for-method; chatsy/replyify reuse it next). form: `slapform.formId` doc diff-synced on name + settings.enabled (omega merge-wrote EVERY run, no dry-run guard, and silently created a name-only orphan doc when the id was wrong → now read-first, patch-on-drift, missing form = visible error). user: form.owner UID set to `slapform.plan` as internal comp — DEFAULT = grandmaster top tier; omega resolved the plan from `.brands/slapform/config.json` (company-mode read, doesn't exist standalone) → now config with default; diffed on the 8 subscription leaf fields, patched with leaf updateMask so Slapform-written siblings (trial, cancelled) survive like the admin-SDK merge did; missing user doc created by the same patch. Auth: SLAPFORM_SERVICE_ACCOUNT in .env = path to SA JSON (absolute or brand-root-relative; omega read company-mode .output/slapform/secrets/) → missing = clean skip. Skips: enabled false (incl. scalar `slapform: false`), no web target, no formId (dashboard-created; interactive setup flow + config writeback rides prompting port). DEFAULTS.slapform (enabled/formId/plan); SERVICE_ORDER … payment → slapform → update (omega's slot; 15 services). GATES: suite 216→**234** (converged zero-mutation with exact read paths, extra-sibling-fields-not-drift, exact leaf-mask patches, form-not-found error + stop-on-error proof, missing-user-doc create with all 8 paths, plan override, no-owner error, 5 skips, fully-drifted dry-run = zero mutations + null state, typed-value codec round-trip); manage e2e scrubs 16 vars + no-formId skip; LIVE sandbox all creds scrubbed (clean skip w/ guidance, 3 passed/0 failed/12 skipped across 15 services, zero repo changes).
    * [x] Task M2m (checkpoint 46): **chatsy service** — both ops (chat, user) on the ckpt-45 Firestore REST client (CHATSY_SERVICE_ACCOUNT in .env). chat: agent doc diff-synced on name/welcome/language/autoTranslate/brand/knowledge with a 9-leaf mask (Chatsy-owned owner/id/metadata survive; omega merge-wrote every run unguarded + orphan-created on bad id → visible error now). Knowledge = packaged baseline (policies + pricing GENERATED from payment.products: free/limits/unlimited/trials, archived skipped) + brand repo `config/chatsy.md` appended ({website} replaced; omega read .brands/{id}/chatsy.md). REAL OMEGA BUG FIXED: {website} was replaced BEFORE pricing insertion → every live agent's knowledge carries a literal `{website}/pricing`; port replaces last + pins it. user: owner account → `chatsy.plan` (default max/Max; company-mode .brands/chatsy read → config) via NEW shared `src/lib/owner-plan.js` (comp-plan reconciliation extracted from slapform's user op — chatsy 2nd consumer, replyify 3rd; slapform refactored onto it, 18/18 green pre-chatsy). De-ITW: agent image from brand.images.brandmark (omega hardcoded company CDN color-x.svg; absent → unmanaged), sponsorshipsUrl config (default {website}/contact; omega hardcoded itwcreativeworks.com/sponsorship in the shared template). Skips: enabled false (incl. scalar), updateAgentInfo false (shared agent), no web target, no agentId (prompting port), no SA. DEFAULTS.chatsy; SERVICE_ORDER … slapform → chatsy → update (16 services). GATES: suite 234→**255** first run (converged zero-mutation + exact reads, owned-fields-not-drift, exact 9-leaf mask, image gating both ways, chatsy.md merge vs real temp brandRoot, knowledge pins incl. the {website}/pricing fix + anti-company-URL assert, agent-not-found + stop-on-error, no-owner, owner-plan 8-path patch, plan override, 6 skips, drifted dry-run = zero mutations + null state); manage e2e 17 vars + no-agentId skip; LIVE sandbox all creds scrubbed (clean skip w/ guidance, 3 passed/0 failed/13 skipped across 16 services, zero repo changes).
    * [x] Task M2n (checkpoint 47): **replyify service** — both ops (agent, user) on the shared Firestore REST client (REPLYIFY_SERVICE_ACCOUNT in .env), gated on the BACKEND target (email agent answers the brand's support mail). agent: doc diff-synced on name + Gmail FILTER QUERY (brand filter from config/replyify.md `---filter---` or auto `to:(@domain)`, ANDed with baseline transactional exclusions) + brand details + knowledge (packaged baseline + file `---knowledge---` appended; all 4 file formats; omega read .brands/{id}/replyify.md) with a 6-leaf mask; missing agent = visible error. user: owner → `replyify.plan` (default max/Max; company-mode read → config) via the shared owner-plan lib (3rd consumer as designed). DE-ITW HARD — biggest company-content strip of the trio: omega's baseline shipped ITW's ENTIRE sponsorship business block (guest-post rules, itwcreativeworks.com/sponsorship, WELCOME10) + hardcoded GIFT15 discount to EVERY brand's agent → sponsorship block GONE from the package (company prose belongs in config/replyify.md; original text preserved in omega-manager for migration), discount section renders ONLY when `replyify.discount { code, label }` configured (spliced between subscription and refund; no code → no section → agents can't invent one). Templater = manager's own templateObject with flat { brand.url }/{ domain } keys (node-powertools stays dropped). Skips: enabled false (incl. scalar), updateAgentInfo false, no backend target, no agentId, no SA. DEFAULTS.replyify; SERVICE_ORDER … chatsy → replyify → update (17 services). GATES: suite 255→**276** first run (converged zero-mutation + exact reads, owned-fields-not-drift incl. unmanaged settings.signature, exact 6-leaf mask with the full composed filter string pinned, replyify.md filter+knowledge merge vs real temp brandRoot, 4-format parser units, anti-company pins, config-gated discount both ways, agent-not-found + stop-on-error, no-owner, owner-plan 8-path patch + override, 6 skips, drifted dry-run = zero mutations + null state); manage e2e 18 vars + no-backend-target skip; LIVE sandbox all creds scrubbed (clean skip w/ guidance, 3 passed/0 failed/14 skipped across 17 services, zero repo changes).
    * [x] Task M2o (checkpoint 48): **server service** — the company-server brand registry. brands op: `brands/{brand.id}` on the company server's Firestore replace-synced to the whitelisted config sections (brand/github/sponsorships; absent sections omitted; payment/analytics never cross) — full-document replace (omega's merge:false, stale keys removed) but DIFF-SYNCED: read first, write only on drift incl. a leftover stale key (omega overwrote blindly every run, no dry-run guard). FirestoreREST gained **setDoc** (PATCH without updateMask = full replace + upsert), the merge:false counterpart to patchDoc. DE-ITW: SERVER_SERVICE_ACCOUNT in .env (path to the company server's SA JSON) replaces `getFirestore('itw-creative-works')` + company-instance `.output/` secrets; doc id from brand.id (not the .brands/ dir name); the legacy-brand `_` prefix skip dropped (company-instance convention). Skips: enabled false (incl. scalar), no SA. DEFAULTS.server = { enabled: true } (project comes from the SA); SERVICE_ORDER … replyify → server → update (18 services). GATES: suite 276→**290** first run after one fixture fix (converged zero-mutation + exact read path, key-order-insensitive deepEqual proof, whitelist pinned by exact full-doc write args, absent-sponsorships omission, drift on brand data + sponsorship price, stale-key-is-drift proof, skips, dry-run parity create+update with null state); manage e2e 19 credential vars + no-SA skip through the full loop; LIVE sandbox all creds scrubbed (clean skip w/ .env guidance, 3 passed/0 failed/15 skipped across 18 services, zero repo changes).
    * [x] Task M2p (checkpoint 49): **assets service** — 5 of omega's 8 ops ported, all LOCAL image processing (no external API). Sources = brand-repo `assets/logo/*.svg` (committed; omega read .brands/{id}/assets/), derived = gitignored `.omega/assets/` (omega's .output/{id}/assets/). Ops: logo-gen (wordmark+combomark from brandmark + brand.font via opentype.js text-to-path, MISSING-ONLY — hand-tuned files never overwritten, pinned vs newer brandmark), process (color+black SVG variants + PNG ladders 9/5 sizes), icons (icns/ico via png2icons; PSD-composited icon.png when templates ports, brandmark meanwhile), social-icons (brandmark-on-white 15% padding), favicons (5 PNGs + multi-size ico + site.webmanifest content-diffed vs config). BEST-IMPL: every op mtime-diffed → omega's --onboarding gate on write ops DIES (it regenerated blindly, no dry-run guard); converged brand = zero-work no-op; dry-run = zero writes + exact planned counts. DE-ITW: omega PACKAGED commercial fonts (CromaSans/NowAlt, licensed) + defaulted every brand to CromaSans via alias table → port ships NO fonts, brand.font resolves from brand assets/fonts/ or system dirs (unset → noted; configured-but-missing → error). PARKED (onboarding/prompting ports): MrLogo AI brandmark (interactive prompt + company-mode admin token), PSD trio templates/social-images/store-images (ag-psd + node-canvas + company binary PSDs). New deps via npu: sharp, png2icons, opentype.js. DEFAULTS.assets; SERVICE_ORDER … server → assets → update (19 services). GATES: suite 290→**306** first run through the REAL pipeline on tmp roots (33-file set with black-conversion + magic-byte pins, converged rerun = all 33 mtimes unchanged, touched brandmark regenerates all-but-webmanifest, renamed brand rewrites ONLY webmanifest, dry-run 20/2/4/7 planned + zero writes, wordmark/combomark from a font BUILT in-test with opentype.js, never-overwrite pin, missing-font error, svg-to-black units); manage e2e no-brandmark skip (19 services); LIVE sandbox: committed generic brandmark added, run 1 = 33 real files, run 2 = fully converged, summary **4 passed/0 failed/15 skipped** — first external port that fully PASSES live. (Side effect noted: backend's own update-disposable-domains script refreshed its data list during the npu install.)
    * [x] Task M2q (checkpoint 50): **certificates service** — Apple code-signing for desktop/mobile targets via the App Store Connect API, all 4 ops (api-key, certificates, bundle-ids, profiles). api-key: APPLE_API_ISSUER/APPLE_API_KEY_ID/APPLE_TEAM_ID from the brand .env + auto-detected AuthKey_*.p8; CSC_KEY_PASSWORD auto-generated + persisted to the brand .env on first REAL run (never dry-run; rotating orphans existing .p12s). certificates: download-if-missing or create via CSR (CSR private key PRESERVED — pairs with the issued cert), .p12 exports mtime-diffed vs .cer via the now-SHARED src/lib/stale.js, non-interactive keychain import (trusted-tool ACLs + partition list; recorder-injected in tests); manual DEVELOPER_ID_*_G2 validate the local .cer via openssl → warn with portal guidance. bundle-ids: `${bundleIdPrefix}.${brand.id}`, platforms from targets, capability reconciliation incl. the exact APPLE_ID_AUTH consent payload. profiles: per platform × cert type → .omega/certificates/apple/profiles/; device list fetched LAZILY (omega fetched every run). DE-ITW: hardcoded com.itwcreativeworks → REQUIRED config `certificates.apple.bundleIdPrefix` (clear error when unset); company-shared .output/_shared/ cert tree → brand-local gitignored .omega/certificates/apple/. DE-INTERACTIVATED: browser auto-opens (agreements 403, manual certs, Keychain Access GUI fallback) → printed URLs/guidance. BEST-IMPL: full dry-run guard (omega had NONE — reads only, plans all, writes zero files), download-if-missing idempotency (omega re-downloaded everything every run), operations{} toggles + --force-recreate dropped. ZERO new deps: NEW shared src/lib/jwt.js (node:crypto ES256 ieee-p1363 + RS256; firestore-rest refactored onto it per DRY), fetch replaces wonderful-fetch, jsonwebtoken never added. GATES: suite 306→**329** first run — URL-level recording fakeApple + REAL local pipeline (openssl fixture key/CSR/DER cert = Apple's actual format, real CSR gen + .p12 export, keychain always a recorder): converged zero-mutation across all 4 ops, preserved-CSR create with the POSTed CSR pinned to the paired on-disk key, first-time CSR gen, manual-cert warn ladder + real openssl validation, exact bundle-ID/capability/profile payload pins + no-device-fetch-for-distribution, local-file-only-cert guard, CSC .env persistence + dry-run-never-writes, dry-run zero-write (not even a directory), ES256/RS256 signer verification; manage e2e 24 credential vars + targets-gate skip (20 services); LIVE sandbox all creds scrubbed: **4 passed/0 failed/16 skipped** across 20 services, certificates skips honestly (web+backend sandbox has nothing to sign).
    * [x] Task M2r (checkpoint 51): **seo service** — parasite SEO GitHub repos, the github-repos op. Per seo.github.content[] item: repo created if missing (public, auto-init; misplaced-owner poll fails loudly — gh repo create can silently land under the authenticated user), developer-tool template pushed via Contents API CONTENT-COMPARED (unchanged never rewritten; 6 files auto-discovered incl. dotfiles: generated README/package.json + static download script, daily-cron maintenance workflow, .gitignore, .nvmrc; discovery now cwd-safe — omega's was cwd-relative), stale non-template files deleted, description/homepage/topics reconciled with NORMALIZED compares (omega re-PATCHed empty descriptions every run: null !== ''), starred as author. GUARDRAIL VERBATIM: >MAX_STALE_FILES(10) non-template files = real-repo name collision → item errors touching NOTHING. Per-item author identity: author.token env:VAR override per call + author.git as commit author/committer (sock-puppet repos). Content: config/omega.json5 or config/seo.json5 sidecar (chatsy.md/replyify.md convention; omega's .brands/{id}/seo.json + per-brand auto-create dropped — port never writes config; no content = clean skip). org defaults to github.org (omega: orgMain). BEST-IMPL: full dry-run guard (omega had none), honest created/updated/synced/planned item statuses, item failures = LOUD service error (omega swallowed into success; loop still runs all items). GATES: suite 329→**349** first run (converged zero-mutation, creation full-push with exact Contents-API body pins, surgical drift + stale delete, guardrail touches nothing while other items reconcile, author token on every call + git identity on commits, sidecar merge, defaults, dry-run zero-mutation both paths, template/generator pins); manage e2e no-content skip (21 services); LIVE sandbox all creds scrubbed: **4 passed/0 failed/17 skipped** across 21 services, zero repo changes.
    * [x] Task M2s (checkpoint 52): **account service** — required Firebase Auth accounts, the users op, running AFTER update/deploy (signup calls hit the live backend). Per account.admins entry ({domain} templated, space-tolerant): account:true → exists? sign-in PROBE verifies the password, updated only on mismatch : created + real POST /user/signup (welcome email, marketing lists; failure = warned not vanished — omega swallowed); roles.admin + highest payment.products plan LEAF-MASK-patched onto users/{uid} (siblings survive; no-products = converged — omega issued an empty merge write every run); marketing:true → PUT /marketing/contact. AUDIT: runQuery users where roles.admin==true → anyone outside the account:true list (incl. admin docs with NO auth record) = service ERROR with email+uid. DE-ITW ×2: hardcoded company ADMIN_EMAILS (personal Gmails) → account.admins config (DEFAULT: support@{domain} only); the personal keyboard-column password formula in code → HMAC-SHA256(email + apex domain) keyed by ACCOUNT_PASSWORD_SEED (auto-generated + persisted to brand .env on first REAL run — the CSC pattern; apex-based so subdomain changes never rotate; rotation self-heals). NO firebase-admin: Identity Toolkit REST (accounts:lookup/update/create) + FirestoreREST (gained **runQuery**) over the brand's own .omega/secrets/service-account.json; custom tokens signed locally via shared jwt.js. DRY ×2: google-token.js extracted from firestore-rest (per-scope cached token providers — identitytoolkit + datastore scopes); env-secret.js extracted from certificates (replace-or-append .env writeback; fresh-file leading-newline quirk fixed). SKIP ORDER: SA gate BEFORE seed resolution — a skipped brand never gets a seed written as a side effect. Skips: enabled false (incl. scalar), no backend target, firebase.shared (owning brand manages accounts), no brand.url, empty admins, no SA. GATES: suite 349→**379** first run (30 tests: deterministic HMAC + apex stability + complexity pins, RS256 custom-token claims verified vs signing key, seed writeback with the pushed password pinned to the just-persisted seed + dry-run never writes it, converged zero-mutation, exact create/patch/query payload pins, unauthorized + ghost admin errors, marketing-only both ways, {domain} templating, no-products no-op, warned signup outage, dry-run zero-mutation ×3, env-secret units); manage e2e 26 vars + no-backend-target skip (22 services); LIVE sandbox all creds scrubbed: **4 passed/0 failed/18 skipped** across 22 services — account passes the backend-target gate, skips at the missing SA with firebase-service guidance, .env untouched.
    * [x] Task M2t (checkpoint 53): **migrations service** — Firestore data migrations, gated on `--migration` (bare = all, `--migration=<name>` = one, unknown name = skip with the available list) so a normal manage run never touches collection data; between account and testing (23 services). FRAMEWORK ported on REST: per-collection fix pipelines (fixes get `(data, doc {id, createTime, updateTime ISO}, db)`, return dot-path updates / `__delete__` / `__logs__`), all of a doc's fixes merged into ONE leaf-masked patch — ancestor/descendant path conflicts resolved first, deletes = masked paths absent from the body, `FieldValue.delete()` = local identity Symbol (no firebase-admin); before/after snapshots + `_summary.json` under `.omega/migrations/{collection}/{ts}/`; `--limit`/`--ids`/`--verbose` CLI pass-throughs; schema validator ported; fix/write failures + invalid docs → **warned** (omega printed ⚠ but reported success). MIGRATIONS ported = the two canonical BEM-schema ones: notifications (uid→owner, owner.uid flatten, legacy→metadata.*, url→context.client.url + full context build, attribution backfill, trim) + users (orphan docs deleted via authAdmin.getUser null — replaced omega's error-code sniffing; plan→subscription; subscription.id/name→product; trial.activated→claimed; deprecated-field removal; referrer-UID→attribution.affiliate.code via DB lookup; metadata.created reconciled to Auth's creationTime; auth.uid/email backfill; consent backfill; full DEFAULT_USER backfill; affiliate.code/api credential generation; sentinel+null normalization; usage period→monthly + zero-total cleanup). DE-ITW: omega's other 25 registered migrations are company one-offs (15-brand usage-rename table, old-middleware/bad-run damage repairs, somiibo keep-plan carve-out — dropped from users) and stay in the company instance. SHARED LIBS: FirestoreREST + listDocs (pageToken pagination) / countDocs (aggregation) / getDocWithMeta / deleteDoc + shared colon-endpoint helper (runQuery refactored onto it); auth-admin.js PROMOTED account/lib → src/lib (2nd consumer) + metadata.creationTime in toUser. Skips: no flag, no backend target, firebase.shared, missing SA (firebase-service guidance). GATES: suite 379→**407** (28 new, first-run green: gating ×6, pinned single-patch payloads for both legacy transforms incl. absorbed dotted paths, orphan delete + snapshot, converged zero-mutation ×2, warned on write-fail + invalid, --ids/--limit/pagination pins, dry-run zero-mutation ×2, validator/trim/metadata/sentinel units); manage e2e 23 services + flag-skip assert; LIVE sandbox 26 vars scrubbed: **4/0/19 across 23 services**, `--migration` run skips at the missing SA with zero disk side effects (no .omega/migrations created).
    * [x] Task M2u (checkpoint 54): **testing-service live checks** — omega-manager's service order fully drained. target-checks grew from local-only into the full health surface, grouped per app + a repo section. NEW: per-app FRAMEWORK VERSION (TARGET_FRAMEWORKS in config.js — web:@omegajs/web, backend:backend-manager, extension:browser-extension-manager, desktop:electron-manager; names flip at each rename cutover, mobile reserved) — installed resolved via node_modules climb so file:/workspace refs read their REAL version (omega parsed the declared range), backend deps from functions/; outdated→warned, unpublished/no-registry→dim note; web HOMEPAGE fetch on brand.url (3 attempts, linear backoff, retries network errors AND non-2xx — restores the original website-check behavior omega's consolidated target-checks dropped; undici's opaque `fetch failed` unwrapped to error.cause, e.g. getaddrinfo ENOTFOUND); backend API HEALTH on https://api.{host}/backend-manager/test/health (mirrors @omegajs/backend getApiUrl + route mount) + deployed bemVersion vs npm latest (shared version cache), firebase.shared→dim skip (owning brand deploys); repo-level WORKING TREE (git status --porcelain `-- .` — nested brand sees only its own files; not a repo→silent; first 10 files listed) + GITHUB ACTIONS latest run via gh (github.org/github.repo||brandId — DE-ITW: omega defaulted org to itw-creative-works; no org→silent matching the github gate; success✓/in-progress⚠/failure✗). STAYS BEHIND: build.json check (UJM artifact — @omegajs/web has no build manifest), stash check (nothing stashes in the new update service). DRY-RUN: local checks run, every remote action (fetch/npm view/gh) prints `⊘ would …` — zero network. INJECTION: fetch/exec/retryDelayMs ride runManage OPTIONS (the seam manage.test needs — full-loop e2e now exercises live checks against a canned 200). GATES: suite 407→**429** (22 new, first-run green: exact all-green pass set + one-fetch-per-URL + one-npm-view-per-package pins, ENOTFOUND retry ×3 pin, 500→200 recovery, version states ×4, shared-Firebase/no-URL gates, working tree ×3, exact gh command + repo override + outcome ladder, dry-run zero-network with exact exec pin, missing-build error); LIVE sandbox creds scrubbed: dry run all-would-lines zero network; live run resolves the file:-installed backend-manager via the climb and matches REAL npm latest (5.11.7=5.11.7 ✓ green), homepage/API honestly fail on the never-resolving .example.com domains → full run **3/1/19 across 23 services** — the one failure is testing correctly reporting the sandbox isn't deployed (cp53's 4/0/19 had testing local-only).
    * omega-manager service-order port COMPLETE (checkpoints 33–54). Still riding later ports: onboarding/prompting (all interactive flows), config-serializer writebacks, disperse remnants (cert files, .env composition), extension port (beehiiv segments), PSD template trio, company mode (M3).
  * [x] Task M3 (checkpoint 55): **company mode BUILT** — the settled design implemented. Company root = config/omega.json5 with `brands: { roots: [...] }` (relative-only, absolute = config error; Ian's shape `roots: ['..']` proven); discovery = immediate children of each root with their own config/omega.json5, company-self + nested companies skipped, sorted by id, deduped by realpath. **runCompany = one manage CHILD PROCESS per brand** (cwd=brand root; sequential streams live + tees .omega/logs/{id}.log at the company root; --parallel buffers per-brand blocks + TTY progress, concurrency = min(--concurrency||cores, brands); --brand=<id[,id]> filter; all other argv forwarded verbatim via filterChildArgs). CHILDREN ARE THE DESIGN: per-brand .env secrets (STRIPE_SECRET_KEY etc.) can never cross brands — each child chains shell > brand .env > company .env itself (omega's in-process ALS console-patching approach dropped: unsafe with per-brand .env, and pipe tees give the log files for free). Aggregate = ONE RunSummary fed from each brand's own .omega/runs/ file (child always writes it); child dies before writing → synthetic error entry. STAMP: runs idempotently write .omega/company.json {root} into every discovered brand (gitignored bookkeeping, dry-run-exempt like run files); brand-local runs read it → company config layer (minus `brands`, secret-scanned) between DEFAULTS and brand via the standard deepMerge + company .env under brand .env + `Company:` header; stale stamp → warn + standalone. Disabled brands stamped, never spawned. Company .omega/ gitignore ensured by the run (gitignore ensure extracted to src/lib/gitignore.js — 2nd consumer with the workspace service). Original design line: self-similar hierarchy company → brands/ → brand → apps/ → app; a brand behaves IDENTICALLY everywhere; merge chain manager DEFAULTS ← company ← brand ← brand targets ← app ← app targets; brands stay their own git repos (remotes manifest rides onboarding); company-scoped services ride their ports; legacy split-repo layout never supported. GATES: suite 429→**447** (18 new first-run green: detection/discovery incl. the ['..'] shape + nested-company skip, absolute/missing root errors, loadCompanyConfig strip+secret-fail, DEFAULTS←company←brand chain pins, stamp idempotency (mtime-pinned no-rewrite) + stale fallback, env-precedence through a real stamped runManage, filterChildArgs pins, and 5 REAL-child e2es: sequential (stamps+logs+run files+aggregate+company gitignore), --brand filter + unknown-brand error, --parallel, disabled-brand, synthetic-error path + collectChildResult unit); LIVE scratch fixture company via the REAL CLI (creds scrubbed): sequential streaming w/ layered Company: headers, --parallel real builds in both brands, --brand filter, stamps, company logs, brand-local run inheriting company sentry.dsn + company .env var through the stamp alone. Self-similar hierarchy: company → `brands/` → brand → `apps/` → app, each level with its own config/omega.json5; the checkpoint-33 walk-up rule generalizes one rung. A brand behaves IDENTICALLY everywhere (standalone / nested / loose) — the company layer only changes what defaults it inherits; company runs = the same per-brand manage loop iterated (+ `--brand` filter, `--parallel` one-per-core with the brand-prefixed logger, aggregate RunSummary). Brand discovery: company omega.json5 `brands: { roots: ['./brands'] }` — dirs to scan (anything with config/omega.json5 = a brand), paths RELATIVE to company root, never absolute; Ian's shape = company workspace as a sibling repo inside the org folder with `roots: ['..']` (no repos move). Reverse direction without nesting: company runs idempotently stamp `.omega/company.json` (path to company root) into each managed brand; brand-local runs read it and layer company defaults, warn if stale; no stamp = standalone. Merge chain: manager DEFAULTS ← company ← brand ← brand targets.<type> ← app ← app targets.<type>; secrets: company .env ← brand .env. Brands are their own git repos (company gitignores brands/*, keeps a remotes manifest for re-clone; NOT submodules, NOT npm workspaces — each brand owns its install). Company-scoped services (shared promo-server etc.) run once at company level when they port. The new engine never supports the legacy split-repo layout — legacy brands stay on old omega-manager until their consolidation moment.

## ✅ Completed Task List
