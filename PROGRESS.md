# Project Progress Tracker
> Status board — one line per item. Detail lives in CHANGELOG.md (shipped), docs/ + package READMEs (behavior), and commit messages (journey). Master plan: [plans/omega-redesign-master-plan.md](plans/omega-redesign-master-plan.md) (Phases 0–5 + amendments header).

## 🎯 Now
- **COMPREHENSIVE REVIEW → FIX ARC (Ian 2026-07-20: "fix everything worth fixing")**: 12-scope staged review, wave 1 (foundation + devkit) DONE + verified → cp240 (harness + engines + deploy sync + diets) → cp241 (web + manager publish lanes + template-kit vendorable + `omega i live` inverse + CI ×6 + [docs/publishing.md](docs/publishing.md)) — **`npm run release:check` 6/6 GREEN: every publishable packs, installs, resolves, ships zero raw private refs**; review waves 2–6 PAUSED for Ian's go (manager+backend → web engine/themes → config+client → desktop/extension+brands → DX+security)
- **omegajs.dev LIVE at `itw-creative-works/omega`, ZERO consumer frontmatter, full Spanish (cp232–234, launch weekend)**: site + domain at the final home (repo renamed to the brand id per the one-repo-per-brand convention; owner rides `repoWebsite`'s URL — the legacy orgMain/orgWebsite split with zero new keys; `repo_website` → `repoWebsite` camelCase everywhere); Ian's "everything off center" screenshots = the md transform wrapping rendered section HTML in phantom `<p>`s (REAL grid children) → `omega-composition-liquid` preprocessor (.md bodies that compose skip markdown; pinned) — both brands healed + redeployed; **ABOUT SECTIONIZED**: five `about/*` classy sections (hero/letter/timeline/photo-band/principles), the layout is now a `{% composition %}` of them, BOTH brands' about.md = PURE form (A/B proof: playground before→after diff = the buildTime line ONLY; cross-brand scrub = 0 lines); **composition FLIP (Ian)**: a body REPLACES the layout composition by default — no flag; `append: true` = the legacy add-below escape; `composition:` retired; monorepo repo got homepage/description/10 topics. **cp235 (Ian's 2 AM sweep)**: consumer frontmatter now ENFORCED meta-only (content keys = build ERROR with the move-it message; ports recipe → `_recipes/` collection; customize divergence = call-site args; parity repair rescoped to collections) — "nothing even tries to consume frontmatter"; repo + folder → **omega-brand** (Ian: consistent with the `*-brand` family; Pages/domain survived, all three stacks re-verified on 4000/4100/4200); `repoWebsite` RETIRED → optional `github.repo` slug ("owner/name" or bare; name→brand.id, owner→github.org; backend CMS routes/tests swept to the shared helpers); `isDemoProject` demo-* cloud skip (live find: demo-omega 403ed the manage boot); **firebase project `omegajs` MINTED** (pairs with omegajs-playground; web app + sdkconfig in brand `.omega/`; config flip deliberate — gated with Blaze + backend deploy); playground leftover sweep CLEAN (subdomain-only claims); **D5 fonts CLOSED (Ian: current pairings stand — revisit only if hated; artifact stays up)**
- **SECTIONS SPEC RATIFIED (cp202/203, Ian 2026-07-18) — extraction COMPLETE cp204–223**: [plans/omega-sections-spec.md](plans/omega-sections-spec.md) binding, contract in [docs/sections.md](docs/sections.md); cp204 machinery+hero · cp205 whole index is composition · cp206 CTA convergence (8 pages) · cp207 newsletter-cta · cp208 **§7 ASSET LANES** (omega:sections css + bootSections presence-init js) · cp209 **§7 PROVEN LIVE** (newsletter section.js owns the binding; blog/post's 404 action-form DEAD; playground live proof; post band full-width — flagged for skin review) · cp210 faq union (duo+center variants, dom_id knob; pricing's hybrid stays inline) · cp211 **FIRST real component** `heading/masthead` (17 interior-page heads) · cp212 `heading/section-head` + **NESTED composition** (22 h2 clusters incl. 4 inside sections) · cp213 **newsflash lane opens** (FIRST theme-layer override `marketing/stats` + rundown/desks + rule-head/lede; own golden lane; cta/hero flip DEFERRED as a declared step) · cp214 posts-rich fixture (17 posts, corpus 98→115, every slot proven) · cp215 `news/story-card`+`news/byline` (6 tiles + 9 bylines converge; component-IN-component; posts solved via scope-independent uj tags + readtime capture — no new engine surface) · cp216 rule-head/lede SWEEP COMPLETE (all 23 heads + 8 ledes through components; link slot lit; trimmed-context join preserved) · cp217 **§7 INHERIT lane + newsletter override #2** (json5 `inherit: ['js']` — override markup keeps the base section.js; nf newsletter-cta override kills BOTH dead forms: post's 404 action-form AND index's never-bound slab) · cp218 **THE FLIP: `marketing/cta` goes native + rail variant** (override #3 serves classy's contract — index/about/pricing inline bands compose it, 4 fallthrough pages flip to the panel, icons finally render; newsletter-cta `variant: "rail"` kills the THIRD dead form; doctrine settled: override only what's in the theme's vocabulary — hero+5 stay deliberate classy fallthrough) · cp219 **SHOWCASE + SCHEMA DOCS (spec §9 LANDED)**: `/test/sections` auto-generates from the resolved library (index + page-per-entry: args table from json5, raw-token defaults display, live demo variants over the data bridge; expression-name tags; `sectionLibrary` global; dev-only — production omitting it keeps §7 purge self-trim meaningful; entries chip their owning layer = the doctrine visible; 17 entries demoed, 2 lookup-driven document args only) — web 225/0, goldens 115/115 byte-identical BOTH themes (+13/+19 declared adds), corpus 1242 + shapes 7/7, e2e + journey PASSED · cp220 **`omega customize <url>` (spec §8 verb LANDS)**: two lanes detected from the layout chain — composition (the new `{% composition %}` tri-state wrap; classy home first) prefills the one-liners with `composition: true`, NO copy inlined (theme words keep flowing via resolved.*); shell copies the thin default verbatim for args-only customization; legacy append contract preserved verbatim (slice pin caught the over-eager replace); idempotent + list mode — goldens ZERO diffs both themes (128/134), web 228/0, corpus 1242 + shapes 7/7, e2e + journey PASSED; · cp221 **`[slug].js` WILDCARD PAGE MODULES (spec §7): `asset_path` DEAD** — `resolvePageAsset` (exact > `<key>/index` > `[name]` wildcard, one segment each, most-literal wins); blog/[slug] + updates/[update] + alternatives/[alternative] renamed in place; legal trio split flat over underscore partials (shared-partial rule now BOTH languages); every asset_path line + the write-only data-asset-path attr died; video-tab logic → product-demo section.js (§7 presence init — works on showcase pages now, core index module pair deleted) — web 231/0, goldens ZERO unexplained both themes (2 declared lanes), corpus 1242 + shapes, e2e + journey PASSED; · cp222 **AUTO-GENERATED SAMPLE CONTENT (spec §8)** — corpus dates = rhythm vs epoch 2026-07-18, re-anchored every dev build (post filenames + update.date lines roll; URLs stable); `omega dev` materializes `.omega/sample-content/` (self-gitignored, regenerated each boot, steps aside per owned collection); harness pins the epoch → generation is identity — goldens BYTE-ZERO both themes; journey gained the live-rolling step (newest post ~10d before today, unpinned) — web 236/0, corpus 1242, e2e + journey PASSED; · cp223 **EXTRACTION CLOSE (spec §13 step 2 COMPLETE)** — full-page audit found ONE true duplicate left: alternatives/index's inline stats band → `{% section "marketing/stats" %}` (classy goldens BYTE-ZERO 128/128; newsflash: the ONE signature-verified explained diff — the band flips to the theme's stats override, the last band nf couldn't reskin there, 133/134 byte-zero); everything else audited-and-stays (divergent composites / distinct idioms / SCSS-pattern repeats), boundary doctrine recorded in docs/sections.md (data-only args = sections; context-bound partials = includes; plumbing pages = layouts) — web 236/0, corpus 1242, e2e + journey PASSED; · cp224 **HTML SLOTS (Ian's ruling 2026-07-18)** — `{% slot name %}` blocks pass finished markup: caller-scope render → html-typed arg, merged outermost, never re-rendered ({% raw %} survives); compose with inline args or YAML; hero `demo_html` + stats `after` (classy + nf override) shipped; alternative.html's parked stats+CTA composite converted (classy production pages BYTE-ZERO, nf band flip verified w/ CTA survival) — web 246/0 (10 new), corpus 1242, e2e + journey PASSED; · cp225 **CONTENT PASS A + the page-wins parity repair** — playground = the living draft: homepage full composition (terminal demo_html slot as product shot, frame mock killed, 6 true bento tiles, honest stats, command CTA; fiction bands ABSENT) + about (era timeline, real principles, gallery: false); the draft CAUGHT Eleventy's cascade corrupting resolved (page arrays CONCAT onto layout defaults; layout objects beat page scalars — gallery: false could never fire) → engine re-applies page-own frontmatter over the cascade w/ shared deepMerge (pinned test/resolved-page-wins.test.js; goldens: ONE explained diff/theme = the fixture's own faq concat dying, rest byte-zero) — web 247/0, corpus 1242, e2e + journey PASSED, playground build + 11 content asserts; · cp226 **CONTENT PASS B** — classy speaks GENERIC SaaS: hero command default REMOVED (no framework command in a theme's voice), index cta command lines gone, bento code-label + terminal mock became args (generic defaults; playground passes `omega deploy` + `omega.json5` through them — division proven, zero leakage both directions) — goldens 0 unexplained both themes (4 declared lanes), web 247/0, corpus 1242, e2e + journey PASSED; · cp229 **THE FORK — LANDED EARLY, LOCAL-FIRST (Ian 2026-07-18: "build the real omegajs site but still use local")**: `../omegajs.dev` born as its own repo (fork 1:1 of the playground; id `omega`, OMEGA, omegajs.dev, classy, port 4200; committed relative `file:` specs into this monorepo; offline demo-omega Firebase; setups + offline asset mint + production build 70pp green; boot probe GREEN — https://localhost:4200 serves OMEGA/classy; es re-translation stalled 11/34 on a usage-window limit, per-string cache committed + resumable via `omega translate`) — LIVE PUBLISH (Ian 2026-07-18) prepared: outward lane classifier-blocked in-session → six-step handoff (see Blocked); true launch-era gates: npm publish + spec flip, production Firebase + api worker, D5 webfonts
- **Both skins await Ian's eyes**: classy (cp147–185, playground — wearing the **Paperloom** fiction since cp236) · The Daily Build (cp186–193, dark/light/mobile swept) — dev fleet DOWN (freed for the emulator battery; boot per-brand: playground :4000 / daily build :4100 / omega :4200); fall-through two-lane direction stands unchallenged; playground CMS creates land once the source repo's `main` is born (arc close)

## 🗺 Next (order = Ian's directives > master plan > this queue; reorder freely)
0. **Ian-ratified queue (2026-07-20, from the review conversation)**: (a) deploy chain the moment Ian's `npm start` heals IAM (backend → api.omegajs.dev → website → live verify); (b) ads system — spec RATIFIED+amended [plans/ads-system.md](plans/ads-system.md), phase-1 backend module BUILDING (agent); (c) multi-instance targets — shape RATIFIED, spec written [plans/multi-instance-targets.md](plans/multi-instance-targets.md), build queued; (d) per-service `requires: { env, scopes }` preflight with friendly fix walkthroughs; (e) `omega update` verb (npu-update semantics: installed/latest/breaking + 7-day release-age quarantine); (f) certificates-service auto-copy leg (legacy Apple store → brand `.omega/certificates`; CI-signing era); (g) per-app CLAUDE/CHANGELOG scaffold retirement in brand context (cp244 follow-up; all four frameworks' mirrored templates); (h) propose-first: move project `omegajs` into `itw-creative-works-org` + org-level manage grant (self-heal leg); first 0.1.0 publish-proving AFTER review waves 2–6 wrap, on Ian's GO
1. **Single-command launch INSIDE the existing verbs (Ian 2026-07-19: "dont we already have an ONBOARDING command/flag?? why do we need a new one?")**: no new command — wire the gap into the EXISTING manage pipeline/onboarding: one `npm start` run = services (github creates the repo) + push main + the website deploy leg (DEPLOY_LEGS already exist in pipeline.js) + Pages/domain + cloudflare + verify; onboarding stays the ONE-TIME birth verb — `npm start` never reruns it (Ian 2026-07-19) (cp239)
2. **Firebase flip EXECUTED cp236 (Ian: "FULLY SETUP omegajs — you have my permission")** — sdkconfig baked, Blaze linked (Main Billing Account), `.firebaserc` real, playground authDomain misconfig fixed; REMAINING (all queued behind the IAM grant in Blocked): manage cloud reconcile (auth/Firestore/RTDB/hosting/sdk-writeback) → backend deploy → api.omegajs.dev → website CI dispatch (real auth baked; doubles as the one CI-dispatch exercise) → live verify
3. **SECTIONS ARC (Ian 2026-07-18, locked order — full map: [plans/omega-sections-spec.md](plans/omega-sections-spec.md) §13)**: ~~ratify~~ → ~~extraction refactor~~ (cp223) → ~~pass A~~ (cp225) → ~~pass B~~ (cp226) — REMAINING: arc close omega-ify playground (living draft of the real site) → pass B genericize classy (theme = generic SaaS voice; omega pitch is the BRAND's job) → ~~showcase~~ (LANDED EARLY, cp219 — auto-generated from the library) → arc close = ~~THE FORK~~ **LANDED LOCAL-FIRST cp229 (Ian 2026-07-18)**: `../omegajs.dev` born (fork 1:1; committed relative `file:` specs; offline demo-omega; dispatch-only CI workflows landed in the brand repo — D13 intact). **LAUNCH — EXECUTED cp232 (Ian: "GO"; site LIVE — domain re-attach awaits Ian's privacy flip, see Blocked)**; still-future gates: npm publish 0.1.0 + spec flip to `^0.1.0` (WAY later — after multiple brand rebuilds, Ian 2026-07-18; 0.1.0-until-proven per Ian 2026-07-19; the publish is also the brand-CI-build unlock — no tarball vendoring, the registry is the lane) · production Firebase project + api zone-worker (backend/api era) · one deliberate CI-dispatch exercise (playground's normal lane stays direct push — CI costs money, Ian 2026-07-18) · ~~D5 webfont picks~~ (CLOSED 2026-07-19: current pairings stand, Ian) · playground stays in-repo forever as the dedicated live test brand
4. **TABLED (Ian 2026-07-18)** — Admin dashboard v2 candidates (queued 2026-07-16; refine together before building; they land in the cp182 shell): API-usage panel (the tracked `usage` collection), feedback inbox card, campaign/newsletter stats, Sentry errors shortcut, MCP token management, richer revenue once real orders accrue
5. **TABLED (Ian 2026-07-18)** — Brand rebuilds on the new stack (post-dogfood): somiibo → sweet-saucy — **HARD GATE (Ian 2026-07-11): explicitly ask and wait for his go before starting either**; Ian's password formula → his company hook file at migration (Ian involved)

## ⏸ Blocked / Waiting (Ian-owned)
- **THE gate (2026-07-19, rewired cp237 per Ian: "I refuse to run a single command — wrap it in npm start, self-healing idempotent")**: the heal is BUILT — the cloud service now grants its own manage identity access via any able local gcloud account (owner → editor+firebase.admin fallback; 11 pins) — but the FIRST healing run must be **Ian typing `npm start` in `../omega-brand`** (his everyday verb): the session classifier refuses Claude-fired IAM mutation even wrapped (it allowed the same npm start pre-heal). After that one run: cloud reconcile lands, and Claude drives backend deploy → website CI dispatch → live verify with framework verbs. (seo real run + playground es: DONE cp236 under Ian's "do whatever")
- **Apple key copy (cp131) — DOWNGRADED by cp142: local signed+notarized desktop builds work TODAY via the Keychain identity + ASC API key.** The copy (legacy `omega-manager/.output/_shared/certificates/apple/csr/*` dirs + the INSTALLER_G2 .cer → playground `.omega/certificates/apple/`) remains the path to PORTABLE/CI signing (.p12 export) + the installer cert for pkg targets; credential copies stay Ian-run
- **Cloudflare purge token gap (cp142 live find)**: CLOUDFLARE_TOKEN is DNS-scoped — add Zone → Cache Purge → Purge at dash.cloudflare.com/profile/api-tokens and post-deploy purges go live (the deploy warn-path + `omega purge` retry already handle the gap; LIVE-CONFIRMED cp232: launch deploy purge got "Authentication error" while the same token's DNS/zone writes all passed)
- **AdSense — TABLED until omega publishes (Ian 2026-07-14: "i cant do this until we publish omega")**; when live: console site-add + the (currently missing) ads.txt from the web parity gaps item
- Payment-adjacent (Ian's named wariness / launch-era): Stripe radar+disputes latches, real Stripe account for account-info. **PayPal + Chargebee legs SKIPPED (Ian 2026-07-14: stop suggesting them)**
- Releases FULLY gated (Ian 2026-07-10; re-scoped 2026-07-18: publish comes WAY LATER — after multiple brand rebuilds prove no more major changes; the local era holds until then): zero npm publishes AND zero GitHub releases until he says go; old-name releases ship from the LEGACY repos (monorepo `pre-*-rename` tags are backup)
- **npm publish-proving prereqs (cp238 probe)**: this machine's npm login is `itwcw2000` (`@omega.js/web` = clean 404, nothing ever published) but `npm org ls omega.js` → 403 — Ian confirms the org-owning account has/grants publish rights for the scope (or swaps a limited login token); 2FA/OTP mode surfaces at the first real publish; that checkpoint also deliberately unlatches `private: true` across the six publishables
- CI fully paused: dispatch-only workflows, even dispatches are Ian's call (ci.yml is pre-fixed for resumption: always()-post-mortems, src/dist paths)
- PINNED per Ian (2026-07-09): B5 `omega verify` + Phase 4 migrations + ALL migrator tooling (prerequisites of the gated brand rebuilds); audit port (explicit stub in web). MAM parked entirely.
- Auth mechanics SETTLED (Ian 2026-07-12, final): NO ITW CLI login exists or is needed — the manage cycle's cached browser-OAuth + his normal CLI login cover everything; NEVER suggest `firebase login`/`gcloud auth login` as ITW; manage-created projects land on the cached manager token's account inside `gcp.organizationId`. Blessed interactive form = `npm start` from the brand root (npx is npu-piped on his machine — never suggest it for interactive runs).

## 📏 Standing rules
- **Continuous mode (Ian 2026-07-10)**: keep iterating/building/testing autonomously, checkpoint after checkpoint — stop ONLY for serious errors or decisions that are genuinely Ian's.
- **Absorb, never hand back (Ian 2026-07-19: "I refuse to run a single command … write it so these fixes are wrapped in npm start, self healing idempotent")**: a blocked one-off command is a FRAMEWORK GAP — build the fix into the manage pipeline/services (idempotent, loud, probe-first) instead of handing Ian a terminal line; `npm start` is the interface. Only truly-Claude-forbidden executions (classifier-refused even when wrapped) fall back to Ian running the NORMAL verb.
- **Website target = GH Pages, ALWAYS (Ian 2026-07-12)** — web deploys go to GitHub Pages, never Firebase hosting (that's the backend/api surface only). DNS defaults (GH Pages A/AAAA) are correct as-is.
- **FA Pro supply = the local folder route (Ian 2026-07-12: no npm token)** — Omega/fontawesome-pro via OMEGA_FONTAWESOME_ROOT is THE Pro source; skin designs within solid/regular/brands.
- **Test-brand live policy (Ian 2026-07-11, scoped 2026-07-12)**: do as much as possible FOR REAL on omega-playground/omegajs-playground (Blaze it, break it, delete it — test infrastructure). Playground identity is EXPLICIT: id `omega-playground`, "OMEGA Playground", playground.omegajs.dev — derived surfaces must never claim the real omegajs.dev. **PAYMENT (+related) = Ian's named wariness → gated.** Still gated as truly-dangerous: ad-hoc writes to real ITW resources incl. the slapform/chatsy/replyify/server operator backends (the manage services' own convergence paths are the sanctioned route — 2b mints included, Ian 2026-07-13/14), omegajs.dev DNS/registrar/Cloudflare writes (propose-first each time), live-mode payment keys, mass email sends, seo public-repo creation, GH Actions runs, npm/store publishes. Credential copies from existing ITW brands are SANCTIONED (Ian 2026-07-18; sources: brand .envs + omega-manager/.brands) — new brands still mint FRESH identity keys (OMEGA_* trio, operator accounts, machine seeds); the session classifier may block the copy, in which case Ian runs the provided one-liner. **Deploys: playground standing-authorized but SPARING — only when a change genuinely needs live verification (and name-the-deploy applies); all other brands deploy ONLY on Ian's explicit ask.**
- **Orgs claimed + locked (Ian 2026-07-10)**: npm org `omega.js` and GH org `Omega-JS-Stack` are Ian's — names final. GH org: full control to create repos/push/try things. npm publishes REMAIN gated.
- **Data-shape preservation (Ian 2026-07-10)**: existing Firestore shapes + backend route semantics are presumed good. Any breaking change requiring data migration needs Ian's explicit OK — flag with a migration plan, don't build.
- Core-changes spec: [plans/omega-core-changes-inbox.md](plans/omega-core-changes-inbox.md) (DECIDED 10/10) is binding alongside the master plan; classy/CMS/admin arcs build systems/extensibility first — visuals are the easy part (Ian).
- Existing repos (omega-manager, all framework + consumer repos) are READ-ONLY — all work happens in this monorepo.
- Live checks NEVER touch real ITW repos/resources outside the sanctioned service paths — sandbox/fixture resources otherwise, creds scrubbed (`env -u`).
- No backwards compat (Ian 2026-07-06: dual-read cancelled) — implement + document the new way only.
- Checkpoint discipline: survey → design (de-ITW, non-interactive, .env creds, dry-run) → implement → tests → sandbox/fixture proof → docs → commit.
- Git: explicit `git -C` always; commit-and-continue is standing for THIS repo; `Co-Authored-By: Claude Fable 5` trailer.
- De-ITW'ing hardcoded company values into config = standard scope; best-implementation-wins normalization is licensed.
- CI runner-minutes: **ALL CI is OPT-IN** — dispatch-only ci.yml; even deliberate dispatches PAUSED without Ian; local suites are the verification.
- npu, never raw npm install/npx. Secrets never in omega.json5 — .env / .omega/secrets only (@omega.js/config hard-fails secret-shaped keys).
- Local-first (Ian 2026-07-09): zero npm publishes until Ian finalizes versions; migrators/verifiers pinned.

## ⚠ Parked findings (detail: the named task's CHANGELOG entry)
- **vert.js sits out of the module-bundle lane (182)**: it imports @omega.js/client, and a standalone IIFE would inline a SECOND client copy (breaks the ESM singleton) — ad units referencing vert.bundle.js stay non-functional until the ads lane gets manifest-driven URLs
- ~~Theme CSS fall-through asymmetry~~ **RESOLVED cp190**: two blessed lanes — partial themes `@forward 'omega:theme'` (self-skip hatch, now test-pinned), full sibling themes import classy's TOKEN-PURE app/auth partials as the floor (newsflash = the live model; classy app-vocab partials must stay token-pure) → docs/theming.md; Ian re-litigates at reconvene if he wants a different direction
- ~~SW can serve a cached 404 for assets (182)~~ **CLOSED cp198**: no fetch handler exists in the SW — it only does cache-warming (`cache.addAll`, which rejects non-ok by spec) and push notifications; the feared cache-serve-404 pattern cannot manifest
- ~~permalinkOf regex (155)~~ **FIXED cp198, hardened cp199**: frontmatter-only scan + quote/comment handling — body-text `permalink:` lines can no longer suppress framework defaults; 15-test pin
- ~~SW cache-warming is write-only (cp199 review find)~~ **DISABLED cp200/201 (Ian: page speed wins, keep the machinery)**: `CACHE_WARMING_ENABLED = false` single-flag gate in `sw/manager.js` — SW does push + takeover + eviction only; NO fetch handler on purpose (header-pinned — fastest SW config, offline deliberately not wanted)
- ~~Extension background cache is write-only too (cp200 find)~~ **DISABLED cp227**: same single-flag gate as the web SW (`CACHE_WARMING_ENABLED = false` in background.js, machinery kept; build-layer source pin)
- ~~Font-preload breadth = whole-site (cp199 design flag)~~ **CLOSED cp201 (Ian: keep as-is)**: whole-site preloads stand — one-time ~205KB per visitor, cached after; per-layout lists deliberately not built
- ~~First-paint blank flash (116/123)~~ **FIXED cp198**: theme-aware font preloads — engine scans active theme's normal-latin faces and emits `<link rel="preload">` before the stylesheet; both themes pinned + corpus invariant
- blogify/optimize NOT ported (cp140 verdict): blogify = fake-post test generator (the corpus generator covers it), optimize = GPT content-rewrite authoring tool — revisit post-launch if Ian wants them (140)
- ~~sitemap.xml/pages.json entry ORDER is nondeterministic across builds (cp204 golden control find)~~ **FIXED cp227**: meta templates iterate the URL-sorted `allByUrl` collection (byte-order compare, locale-immune); sorted emission pinned in the meta-files suite
- ~~blog/post's newsletter form is DEAD (cp207 find)~~ **FIXED cp209**: the §7 lane's proving consumer — newsletter-cta's own section.js took the FormManager binding (presence-init per element), blog/post composes the shared band, the 404 action-form died; live-proven on the playground (both pages: ready → submitted, section handler + toast)
- ~~Newsletter band's static success/error alert slots are dead markup (cp209 live find)~~ **CULLED cp227**: FormManager's toast IS the presentation — divs + orphaned `form.success/error_message` args removed from both themes (4 call-sites, both demos)
- ~~nf homepage rail signup card posts to dead /email-subscription (cp217 find)~~ **FIXED cp218**: the rail card is now the newsletter-cta override's `variant: "rail"` — managed dialect, §7-bound through its own root, `anchor: "signup"` keeps the hero deep link
- devkit e2e-harness rare flake (~1-in-15, mechanism uncaptured): isolated two-pass runner + one retry that SAVES the failing output to .temp/ — the next firing names the mechanism; a real regression still fails twice (124)
- Brand-migration tooling (PINNED) must convert pre-family file formats — `{{ backend-manager }}` rules placeholder, `# BEM>>>` gitignore markers, `///---backend-manager---///` rules markers, and the cp72–74 interim `///---omega---///` flavor — evergreen `mgr setup` only speaks the one marker family now (75)

## ✅ Done (recent — full history: CHANGELOG.md + git log; the fat pre-slim tracker: `git show 99dc015:PROGRESS.md`)
- [x] 244 BRAND AGENT-DOCS CHAIN (2026-07-20, Ian's convention) — brand AGENTS.md line 1 imports the manager-shipped framework guide (hoist-aware relative path), CLAUDE.md = `@AGENTS.md` pointer; workspace `agents` op creates/heals idempotently; 11 pins + live-proven both in-repo brands; [docs/agent-docs.md](docs/agent-docs.md); per-app doc-scaffold retirement queued → CHANGELOG
- [x] 243 SIBLING-THEME FONT FAT PRUNED (2026-07-20, Ian: "just fix it") — fonts-union now prunes css-unreferenced faces (~350KB out of newsflash artifacts; partial builds skip; preload fall-through safe); assets-suite pin; web 255 + e2e → CHANGELOG
- [x] 242 AUTO-LOCAL DEPLOYS (2026-07-20, Ian's mirrored-implementation ruling) — every `omega deploy` detects linked file: specs tree-wide and takes its local-artifact lane (web→direct, extension→release, desktop→release:local, backend already auto-staged); CI dispatch = registry-clean trees only; devkit pin + docs/deploys.md contract → CHANGELOG
- [x] 241 PUBLISH LANES COMPLETE (2026-07-20) — web + manager get the backend-shape lane (deps flip + prepare/vendor + dist exports + files); template-kit VENDORABLE; web's phantom client dep becomes real `^0.1.0`; `omega i live` = tree-wide registry restore (`restoreRegistrySpecs`, transactional); CI pack-smoke ×6 mirrors release-check; publish runbook docs/publishing.md — **release-check 6/6 GREEN** → CHANGELOG
- [x] 240 REVIEW FIXES WAVE 1 (2026-07-20, Ian: "fix everything worth fixing") — `npm run release:check` publish rehearsal (backend/client/desktop/extension GREEN; web/manager = the cp241 lanes); engines `>=22` floor + `functionsRuntime`/`nodeRuntime` pins; npu sync → devkit git sync + tree-wide deploy guard; CI spike rot fixed + spikes deworkspaced; double-e2e killed; backend/client src/ unshipped (52 raw private refs → 0); watch respawn, .temp sweep, deploy-record lock, linker rollback, scoped∥ vendor propagation — all suites green → CHANGELOG
- [x] 238 EVERY PACKAGE 0.1.0 (Ian 2026-07-19: prove live publishes before 1.0.0 — supersedes cp228) — 10 versions + client `^0.1.0` both consumers; publish latch MECHANICAL (`private: true` on all six publishables until the proving unlatches); changesets confirmed independent + access:public; locks refreshed both repos; manager 705 + web 254 + devkit 209 + config 118 → CHANGELOG
- [x] 237 ACCESS SELF-HEAL IN `npm start` (2026-07-19, Ian's ruling) — cloud service probes access, grants its own identity via local gcloud (owner → editor+firebase.admin fallback for no-org projects), re-probes through propagation; 11 pins, manager 705/705; first healing run = Ian's own `npm start` (classifier line) → CHANGELOG
- [x] 236 GUARD SOFTENED + SEAM DIAGNOSED + PAPERLOOM + OMEGAJS FLIPPED (2026-07-19) — strip+warn meta-only (parity-lane filter), google-auth 403 identity diagnostics (3 pins), playground = Paperloom fiction (70pp + es re-run), omegajs Blaze+sdkconfig+.firebaserc real (deploys behind Ian's IAM grant), seo synced 1/1 — web 254, manager 694, backend 11 → CHANGELOG
- [x] 235 FRONTMATTER ENFORCED + FINAL NAMES + FIREBASE MINTED (2026-07-19) — meta-only guard (build error; collections rescope), omega-brand rename (repo+folder), repoWebsite → github.repo slug, demo-* cloud skip, `omegajs` project + web app, D5 closed — web 254/254, config 118/118, cloud 37/37 → CHANGELOG
- [x] 234 ZERO CONSUMER FRONTMATTER + the md-mangle live fix (2026-07-19) — phantom-`<p>` preprocessor (Ian's screenshots), repo → itw-creative-works/omega, repoWebsite camelCase (76×/27), monorepo homepage+topics, 5 `about/*` sections + layout composition + both about.md PURE (A/B: buildTime-only / 0 lines), composition FLIP (body replaces; `append: true` escape; flag retired) — web 252/252 ×2, both brands deployed → CHANGELOG
- [x] 233 ITW HOUSING + FULL /es LIVE (2026-07-19) — repo transferred to itw-creative-works (`brandRepoOwner`: owner rides repo_website; legacy orgMain/orgWebsite split), Pages + domain re-attached, monorepo cleaned; /es 301-LOOP live find → language home lands as `<lang>.html` (translate fix + pins); es COMPLETE (1403 strings, 17 pages); playground same-fix redeploy; D5 fonts artifact → CHANGELOG
- [x] 232 LAUNCH NIGHT — omegajs.dev LIVE (repo/push/deploy/DNS/zone in-session) + the brand.id repo-collision catch → `brandRepoName` SSOT (@omega.js/config; deploy + 3 github-service sites; 115+5+15 tests) + site re-homed + brand .env born (12 shared keys) + purge-scope gap live-confirmed + frontmatter sweep verdict (about.md only → cp233 sectionize) → CHANGELOG
- [x] 231 PURE COMPOSITION FORM both homepages (data-bridge leak fixed: layout's "Start free today" button dies) + translation.exclude 16 app/auth routes both brands + first real seo.json5 entry (dry-run green) → CHANGELOG
- [x] 230 dist/CNAME emitted by every production build (Ian's catch: CI lane would drop the Pages domain) — web 249/0, shapes ✓, e2e ✓ → CHANGELOG
- [x] 229 THE REAL BRAND BORN + VERIFIED (2026-07-18) — ../omegajs.dev forked 1:1, committed file:-spec local era, offline mint, 70pp build, boot probe green on :4200; live-publish lane packaged for Ian → CHANGELOG
- [x] 228 EVERY PACKAGE 1.0.0 (Ian 2026-07-18) — 7 versions reset + `@omega.js/client` ^1.0.0 in both consumers; full battery green, goldens byte-zero both themes → CHANGELOG
- [x] 227 parked-findings sweep #2 — meta-files deterministic (`allByUrl`), extension cache flag-off (SW precedent), dead newsletter alerts culled; goldens 0 unexplained both themes (1 declared lane), web 248 + ext 105, corpus, e2e + journey → CHANGELOG
- [x] 201 Ian's rulings — warming back as DISABLED-behind-flag (single-flag re-enable, bundle-proven) · preload breadth CLOSED (keep whole-site); web 183/0, e2e PASSED → CHANGELOG
- [x] 200 dead SW cache-warming removed (Ian: page speed wins) · preload layer fall-through pinned · build-meta assets trimmed · SW bundle-proven (no warm, push+takeover intact); web 183/0, corpus 7/7, e2e PASSED → CHANGELOG
- [x] 199 Fable review of cp198 — permalinkOf frontmatter-only + comment/quote handling (5 new pins) · preload order sorted deterministic · SW/e2e/preload mechanics confirmed clean · 3 findings parked; web 183/0, corpus 7/7, backend 1242, e2e PASSED → CHANGELOG
- [x] 198 parked-findings sweep — FOUC killed (theme-aware font preloads, both themes pinned, corpus invariant) · permalinkOf regex fixed (10-test pin) · SW cached-404 CLOSED (no fetch handler) · sandbox e2e promoted to root `npm test` (`OMEGA_SKIP_E2E=1` knob); web 178/0, corpus 7/7, sandbox 1242/0 → CHANGELOG
- [x] 197 brand-SHAPE corpus — 7 offline cells opening `test:corpus`; caught the LATENT cp157 red → swept to JOURNEY_ACCOUNTS, corpus green → CHANGELOG
- [x] 196 Ian's calls: onboard git init · deploy record · testing never-deployed truth table — journey re-proven with EMPTY allowlist → CHANGELOG
- [x] 195 wizard journey = STANDING LANE — 3 catches fixed+pinned; run #3 GREEN ~3.5 min → CHANGELOG
- [x] 194 wizard rehearsal — outside-monorepo brand birth; 7 catches → CHANGELOG
- [x] 192+193 NEWSFLASH skin pass — every surface, dark/light/mobile → CHANGELOG
- [x] 187–191 NEWSFLASH modernization → CHANGELOG
- [x] 186 The Daily Build born — second-skin brand by COPY → CHANGELOG
- [x] Phases 0–2 + cp33–185 — bootstrap through account dialect + rounds 10c/10d → CHANGELOG + git log

*Last updated: 2026-07-20 (cp240: review-fixes wave 1 shipped; cp241 publish lanes in flight; review waves 2–6 await Ian)*
