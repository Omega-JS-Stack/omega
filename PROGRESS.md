# Project Progress Tracker
> Agents and maintainers should update this file regularly to reflect the current state of the project.

## 🎯 Current Focus
* **Goal:** Phase 1 — foundation packages. Sandbox complete (1.2); @omegajs/account COMPLETE (1.3); BEM dist layer LIVE (1.4a) + devkit shims adopted (1.4d — all four frameworks now vendor devkit). Next: 1.4b (generated public/*), then 1.4c (defaults expansion), then @omegajs/config (Phase-1 step 4).
* **Current Phase:** Phase 1 — Foundation packages (Phase 0 complete except the publish gate, which Ian owns)
* **Priority:** High
* **Last Updated:** 2026-07-06 1:40 PM
* **Notes:** Devkit vendoring proven on extension (585d7cc) and desktop (7c75970); runner-core adopted by extension (eaae359, checkpoint 7) and desktop (checkpoint 8, this change). Backend adoption waits for BEM harmonization (no dist layer yet). INCIDENT (resolved): checkpoint-5 commit initially ran in omega-manager by accident (stray shell cwd) — undone via mixed reset, Ian's working tree fully restored, nothing pushed; all git commands now use explicit `git -C`. Pending Ian: electron-manager@1.12.1 publish (his session), @omegajs org claim, GitHub remote creation.

## 📌 Active Task List
* [ ] Phase 0: Bootstrap the monorepo
  * [x] Task 0.1: Create repo skeleton (packages/, spikes/, apps/, docs/, package.json workspaces, .nvmrc, .gitignore, README, CLAUDE.md, PROGRESS.md, CHANGELOG.md)
  * [x] Task 0.2: Probe @omegajs npm scope (no packages exist; Ian to claim org on npmjs.com)
  * [x] Task 0.3: Initial commit (b9d9aed) + per-checkpoint commits on Ian's go
  * [x] Task 0.4: Install + configure changesets (@changesets/cli ^2.31.0, .changeset/ initialized)
  * [x] Task 0.5: Plain-copy backend-manager, web-manager, browser-extension-manager, electron-manager into packages/{backend,client,extension,desktop} (rsync minus .git/node_modules/.env/logs/lockfiles; secret scan clean — only BEM's demo-project test fixture; MAM excluded as parked)
  * [x] Task 0.6: Workspace install + suites GREEN in-place — client 73 passing; extension 85 passing; desktop 751 passing / 5 designed extended-mode skips (required 2 hoisting fixes: src/test/runners/electron.js + boot.js now resolve electron via require.resolve(paths:[projectRoot]) instead of hardcoded <root>/node_modules/electron — behavior identical for consumers); backend 2 passing = its complete standalone suite (real BEM corpus runs in consumers; emulator stack self-orchestrated successfully from the monorepo, proving java+firebase-tools env)
  * [x] Task 0.7: CI workflow written (.github/workflows/ci.yml — 4 suites w/ xvfb+java, pack→scratch-install smoke; CI verification pending first push to GitHub) + pack-smoke run LOCALLY: all 4 packages pack, scratch-install, and resolve OK
  * [x] Task 0.7b: 🔥 FOUND+FIXED production bug via pack-smoke — electron-manager@1.12.0 ON NPM fails every fresh consumer install (postinstall runs scripts/sync-nvmrc.js but `files` didn't ship scripts/). Fixed in monorepo copy (files += "scripts/"); verified: tarball ships script, install succeeds, postinstall syncs consumer .nvmrc. NOTE: live npm package still broken → 1.12.1 publish is the natural Task 0.8 gate candidate (Ian's go required).
  * [ ] Task 0.8: GATE — publish one old-name patch from the monorepo (electron-manager@1.12.1 with the files fix). **Ian is handling this himself in another session**; Phase 1 proceeds in parallel per his go ("commit and continue").
* [ ] Phase 1: Foundation packages
  * [x] Task 1.1a: @omegajs/devkit first files — logger (identical ×4, EM's cleaned copy canonical), safe-install (byte-identical ×4), attach-log-file (functionally identical ×4; header normalized to `# omega log`). Private workspace package, 17 unit tests green (node:test until the shared runner slice lands).
  * [x] Task 1.1b: Vendor-on-prepare mechanism (packages/devkit/tools/vendor.js) — copies devkit src → dist/vendor/devkit + rewrites requires to relative paths + guards that hosts declare vendored modules' runtime deps. Proven on extension: 3 shims adopted, suite 85 passing (unchanged), pack→scratch-install resolves + runs the vendored logger, shipped dist has zero raw @omegajs requires. CI: devkit suite added; pack-smoke gained the self-containment grep (the hard gate, since prepare-package after-hooks are non-blocking).
  * [x] Task 1.1c: Adopt devkit shims in packages/desktop — logger + safe-install + attach-log-file shimmed (Electron-specific logger-lite stays EM-owned); suite 751 passing / 5 designed skips (unchanged); pack-smoke resolves + self-contained + vendored logger runs in a bare consumer + scripts/sync-nvmrc.js still ships. **Backend deferred to its harmonization step (Task 1.3-adjacent)** — BEM has no dist layer yet, so there's nothing for the vendor hook to rewrite (its bin requires src/ directly).
  * [x] Task 1.1d-part1: devkit test-runner CORE extracted (src/test/runner-core.js — discovery, suite/group/standalone execution, filtering, skip semantics, _init hooks, reporting; UJM's canonical, parameterized by title/packageName/targetAlias/suitesDir/middleLayers/boot config) + test/assert.js (was byte-identical ×3) + extended-mode-warning factory. Extension adopted: runner.js is now ~90 lines of BXM-specific config + layer glue; suite 85 passing (unchanged); runner loads from packed tarball. Vendor tool upgraded: SELECTIVE vendoring (only modules the host's dist requires + transitive relative requires — a safe-install-only host ships 1 file and needs only node-powertools) + rewrites `require.resolve()` forms (chromium runner inlines devkit assert SOURCE into the browser harness). Devkit suite 30 passing. CI grep widened to catch require.resolve forms.
  * [x] Task 1.1d-part2: desktop adopts runner core — runner.js 529→89 lines of EM config + layer glue (main+renderer share one Electron spawn via runners/electron.js with harnessEntry; boot glue passes emDistRoot; bootDefaultTimeout 15000; runners now lazy-loaded like BXM so a missing electron doesn't block build-layer tests); assert.js + extended-mode-warning.js shimmed (EM's harness needed NO require.resolve inlining — main-entry requires ../assert.js which chains through the shim, boot-entry resolves it via runtime path-join, renderer-entry keeps its self-contained mini-expect). Suite 751 passing / 5 designed skips (unchanged); prepare vendors 6 modules / rewrites 6 files; pack-smoke: resolves, self-contained, runner+assert+warning all load from tarball, sync-nvmrc.js still ships. Framework-specific runners (chromium/electron/boot harnesses) stay framework-side — revisit sharing UJM↔BXM chromium plumbing when @omegajs/web is built.
  * [x] Task 1.2a: Sandbox brand monorepo + backend app — apps/sandbox-brand in the day-one brand-monorepo format (package.json workspaces apps/*; README documents the fresh-by-construction + demo-everything rules); apps/backend = real BEM consumer built from packages/backend's own scaffolding sources (templates/ + src/defaults/), demo-sandbox-brand emulator-only project, fresh fake service-account key, committed demo .env (re-included via .gitignore Custom Values), backend-manager linked via file: to packages/backend. **FULL BEM CORPUS GREEN vs the sandbox: 1203 passing / 0 failing / 44 designed skips (exit 0)** — the framework's complete routes/events/rules/email/mcp corpus now runs in the monorepo against a fresh consumer (previously only 2 boot smokes ran here). 🔥 DOGFOOD CATCHES (3): (a) REAL BEM BUG FIXED — rules tests ignored firebase.json's firestore emulator port (runner never passed it to the rules client; every rules test times out on a custom-port consumer); src/test/runner.js now passes emulatorPorts.firestore. (b) FINDING (logged): BEM doesn't truly support custom emulator ports — Manager.getApiUrl()/getFunctionsUrl() hardcode localhost:5002/5001 in dev/testing (contract-asserted by test/helpers/environment.js), test/mcp/* hardcodes 5002; payment/cron self-calls break on custom ports. Sandbox uses DEFAULT ports like every real consumer; harmonization candidate. (c) REAL BEM BUG FIXED — content/post 404 tests hit LIVE GitHub search in normal (non-extended) mode; route 500s without GH_TOKEN so any token-less consumer fails. Gated behind GH_TOKEN presence (mailbox-key static-skip pattern) in test/routes/content/post.js (operative) AND the test/functions/general/fetch-post.js legacy copy (test/functions/ only runs with --legacy). Minor parked: BEM's test path filter matches project tests but not corpus paths.
  * [x] Task 1.2b: Sandbox website app + cross-stack e2e harness + CI wiring — apps/sandbox-brand/apps/website is a minimal web-manager consumer (esbuild bundles src/assets/main.js embedding packages/client; deps resolve from the workspace root, no per-app install needed in-monorepo); `npm test` at the brand root runs e2e/run.js: builds the site, boots BEM's full emulator suite (`npx mgr emulator` in apps/backend/functions), serves the site, and drives real Chromium (puppeteer) through signup → BEM auth onCreate creates the Firestore user doc → signout → signin via web-manager → session persistence across reload → subscription resolution (plan basic/inactive). **11/11 steps green, 2 consecutive runs, ~90s each; clean teardown (all ports freed).** The `omega e2e` CLI grows from this harness later (kept sandbox-local until a second consumer earns the devkit extraction). CI: new `sandbox` job (setup-java + root npm ci + functions npm ci + `npm run test:backend` corpus + `npm test` e2e); verification pending first push like the rest of ci.yml. 🔥 DOGFOOD CATCHES (2 REAL web-manager BUGS FIXED in packages/client): (a) NO auth-emulator support — Firestore had a FIREBASE_EMULATOR_CONNECT path but auth never called connectAuthEmulator, so signin/signup vs an emulated backend silently hit LIVE Firebase Auth; (b) the firestore emulator connect lived only in the lazy firestore module while the auth module reads accounts via `manager.firebaseFirestore` directly, so emulator-mode account reads hit LIVE Firestore unless page code happened to touch `manager.firestore()` first. Fix for both: single connect point in `_initializeFirebase()` right where the auth/firestore instances are created (auth :9099 + firestore :8080, same gate `isDevelopment() && env.FIREBASE_EMULATOR_CONNECT`); duplicate block removed from firestore.js. Client suite 73 passing (unchanged). Design note (Ian can veto): the website app embeds packages/client directly rather than being a UJM consumer — UJM is external/read-only + Ruby, and @omegajs/web replaces this app's internals in Phase 2 anyway; the brand-monorepo slot + e2e contract are what's being locked in now.
  * [x] Task 1.3: @omegajs/account extraction (golden-master gate) — COMPLETE: extracted (1.3a), adopted by web-manager (1.3b) and BEM (1.3c); both drifted schema copies are gone
    * [x] Task 1.3a: packages/account created + golden master GREEN — schema engine extracted verbatim from BEM user.js with `$uuid`/`$randomId`/`$apiKey` generators INJECTED via ctx.generators (BEM will inject uuid4/Utilities().randomId(size 8)/uid-generator; frontend passes none → nulls); USER_SCHEMA as pure data; resolveSubscription = BEM superset (everPaid — WM's copy lacked it); resolveAccount(data, {generators, user}) with the frontend auth-overlay semantic; time context = plain Date (verified powertools.timestamp ≡ toISOString/floor(ms/1000) — package has ZERO runtime deps, browser-safe). Golden master compares against LIVE packages/backend user.js (not a snapshot): frozen Date via node:test mock timers, deterministic randomId, format-assert+sentinel for the 2 non-injectable BEM generators, deepStrictEqual + JSON.stringify (key-order) parity across 7 fixtures (empty, paid, trialing, expired-trial+cancelling, suspended, junk-type coercion, passthrough/template/strip) + resolveSubscription parity per fixture. **31/31 tests green; CI test job gained the account suite.**
    * [x] Task 1.3b: web-manager adopts @omegajs/account — vendor tool GENERALIZED (multi-package: scans dist for ANY `@omegajs/<pkg>` reference and vendors each into dist/vendor/<pkg>/; package roots resolved via require.resolve of the bare name — convention: entry is src/index.js; + ESM forms rewritten: `import…from` / `export…from` / dynamic `import()` / side-effect `import`, alongside the existing require/require.resolve; relative-dep walk + host-dep guard cover both syntaxes; devkit suite 31 passing incl. new ESM multi-package fixture that import()s the rewritten file and proves named-imports-from-vendored-CJS interop). Client flip: auth.js deleted DEFAULT_ACCOUNT + local resolveAccount deep-merge + resolveSubscription body → imports `@omegajs/account` (resolveAccount(rawData, {user}) overlay; resolveSubscription wrapper keeps only WM's storage-fallback glue; everPaid additive); package.json gained @omegajs/{account,devkit} devDeps + the vendor after-hook. GATES GREEN: client suite 74 passing (was 73; +everPaid test); cross-stack e2e 11/11 (doc-created marker flipped from metadata.created.timestamp — which the canonical engine now fills with `now` even for fallback accounts — to api.clientId, non-null ONLY when the read hits the BEM-written doc since the frontend injects no generators; e2e observed a real server-generated v4 uuid + everPaid:false asserted in the browser bundle); pack-smoke: web-manager tarball dist has ZERO raw @omegajs refs, ships dist/vendor/account/{index,engine,schema,subscription}.js, vendored code loads + resolves from a scratch install; extension suite 85 passing (sanity on the changed vendor path); CI pack-smoke grep widened to ESM forms. Watch item: the tarball still ships src/ with the bare `@omegajs/account` import and `"module": "src/index.js"` points there — modern bundlers (webpack 5/esbuild) honor the `exports` map → dist, but an exports-unaware consumer would hit src and fail LOUDLY (unresolvable specifier); revisit shipping src/ at the client cutover.
    * [x] Task 1.3c: BEM adopts @omegajs/account — user.js (408 lines: schema + engine + constructor) → ~50-line thin wrapper, ZERO call-site changes (`new User(Manager, settings).properties` + `User.resolveSubscription` preserved; wrapper injects the real generators: uuid v4, `Manager.Utilities().randomId({ size: 8 })`, uid-generator apiKey — the golden master's mock Manager asserts the exact randomId args). packages/backend gained @omegajs/{account,devkit} devDeps + the vendor after-hook. The src-deep-require problem (corpus tests load `../src/...` in consumers, where the private package can't resolve — flipping ~24 test files incl. _legacy to dist was rejected as too broad) solved INSIDE the wrapper with a try/catch fallback: try the bare specifier (monorepo workspace link — always-fresh for the golden master; rewritten to `../../vendor/account/index.js` in dist by the vendor hook), catch → `../../../dist/vendor/account/index.js` (same file at the same depth from BOTH src/ and dist/ copies, so consumers' src deep-requires land on the vendored copy). 🔥 VENDOR BUG FOUND+FIXED (TDD, failing test first): jetpack.find follows symlinks during traversal regardless of matching exclusions → the host-dist scan recursed through the fixture's circular `backend-manager → package root` symlink until ENAMETOOLONG. vendor.js now scans with a symlink-safe walker (fs.readdirSync withFileTypes; never follows symlinks, never enters node_modules or dist/vendor — none of that is host code to scan/rewrite); devkit suite 32 passing (+1 regression test with a circular-symlink + node_modules fixture proving fixture-dep @omegajs refs are neither vendored-for nor rewritten). GATES GREEN: account suite 31/31 (golden master now validates the wrapper glue — real generators format-asserted, key order byte-identical); BEM standalone 2 passing (exit 0); sandbox corpus 1203 passing (exit 0; middleware logs show real generated clientId/privateKey/affiliate.code flowing through the injected generators); cross-stack e2e 11/11 (the BEM-side api.clientId was generated by the WRAPPED user.js inside the emulator runtime); pack-smoke 8/8 incl. the NEW src-fallback check (scratch-install consumer deep-requires src/manager/helpers/user.js corpus-style → vendored account resolves; v4 clientId + randomId wiring + apiKey + resolveSubscription all asserted). Ships as an old-name v5 release.
  * [ ] Task 1.4: BEM structure harmonization (src→dist + generated public/* + defaults parity — plan §6, old-name v5 releases)
    * [x] Task 1.4a: dist layer + bin flip — preparePackage un-defused (`./src_`/`./dist_` → `./src`/`./dist`; the config, prepare scripts, and offline-safe disposable-domains before-hook were already wired, just disabled), `main` → dist/manager/index.js, bin/backend-manager requires ../dist/cli/index.js, `start` → prepare:watch (matches the other frameworks), .gitignore gained /dist/, prepublishOnly now prunes the self-test fixture node_modules from BOTH src and dist. Safety verified BEFORE flipping: jetpack.copy preserves symlinks as symlinks (no recursion on the fixture's circular `backend-manager → package root` link; empirically tested) and npm pack excludes symlinks from tarballs. Path-depth audit: dist mirrors src at the same depth, so all `__dirname`-relative escapes (templates/, package root) resolve identically; only src-literal is watch.js's bemSrcDir (BEM dev watch still watches src — correct as the reload trigger since prepare:watch keeps dist synced; noted as dev-flow nuance). Consumers deep-requiring src/ still work (files ships src/ unchanged; the corpus itself requires ../src in ~10 test files). GATES GREEN: standalone suite 2 passing via bin→dist; full sandbox corpus 1203 passing / 1247 total (exit 0, 84s) via consumer `npx mgr`→dist; cross-stack e2e 11/11; pack-smoke: tarball 1.2M with 432 dist files + fixture symlinks excluded, scratch-install resolves main to dist/manager/index.js, installed `mgr version` runs from dist, self-containment grep clean. 🔥 LATENT BUG FIXED (checkpoint-10 wiring): sandbox `test:backend` used `--workspace=apps/backend`, but npm resolves the project root to the MONOREPO (sandbox-brand is itself a root workspace; workspace globs don't recurse) so the flag never resolved — the corpus had only ever been run from inside apps/backend directly. Now `cd apps/backend && npm run test:offline`; CI's sandbox job would have failed on first push without this.
    * [ ] Task 1.4b: consumer `public/*` becomes generated at build/setup time + gitignored (today setup-tests/public-html-files.js writes tracked framework boilerplate into consumers)
    * [ ] Task 1.4c: defaults expansion to parity with the other frameworks (5 files today vs UJM's 310) — likely rides on the devkit defaults-engine slice
    * [x] Task 1.4d: BEM devkit shims (deferred from Task 1.1c, done ahead of 1.4b/c since the vendor hook was just proven in BEM) — src/cli/utils/{safe-install,attach-log-file}.js flipped to the standard one-line shims (same pattern as extension/desktop). Pre-flip diff confirmed both BEM copies were functionally identical to devkit's canonical versions (comment-only deltas + the already-accepted `# bem log` → `# omega log` header normalization). BEM has NO framework logger to shim — its console+file route logging is a BEM feature (wonderful-log), per the plan. Scope check: nothing in test/, templates/, or src/defaults requires these src paths, so no consumer ever loads the src shims (CLI always runs bin→dist, where the vendor hook rewrites them) — the user.js try/catch fallback stays a user.js-only special case. Vendor now ships account (4) + devkit (2), rewriting 3 dist files. GATES GREEN: standalone 2 passing (exit 0); sandbox corpus 1203 passing (exit 0); cross-stack e2e 11/11 (mgr emulator = the dist CLI path); pack-smoke 9/9 (new check: vendored shims load from a scratch consumer install with intact export shapes — safeInstall fn + callable attachLogFile with detach/createTee).

## ✅ Completed Task List
