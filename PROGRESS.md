# Project Progress Tracker
> Agents and maintainers should update this file regularly to reflect the current state of the project.

## 🎯 Current Focus
* **Goal:** Phase 1 — foundation packages — is **functionally COMPLETE**: sandbox (1.2), @omegajs/account (1.3), BEM harmonization (1.4a–d — **1.4c defaults parity DONE**: BEM scaffolds via the devkit engine, `_.env`/`_.gitignore` moved into defaults with marker merges; BEM's merge-line-files custom-key promotion absorbed INTO devkit as canonical), @omegajs/config (EM+BEM+BXM flipped, 1.5a–d), devkit slices (1.6a cli-router, 1.6b defaults-engine). Remaining Phase-1 stragglers: build-task factories (1.6c) deliberately deferred to ride @omegajs/web's gulp-ectomy; Ian-owned publish gates. **Phase 2 (frontend bake-off) is UNDERWAY**: A0 baseline + corpus done (Task 2.1 — **the real somiibo Jekyll baseline is 332s cold**, recorded in spikes/bakeoff-shared/BASELINE.md; deterministic corpus generator mirrors somiibo's exact shape). **A0 groundwork is COMPLETE** (Task 2.2 — @omegajs/template-kit ships the full uj_* surface as engine-neutral JS + registerLiquid() adapter proven vs real liquidjs; @omegajs/config gained toSiteGlobal()). **Task 2.3a — the Eleventy spike — is DONE and the candidate is STRONG: cold full build 3.60s vs 332s Jekyll (~92×), all load-bearing bets validated (virtual layered layouts, jekyllInclude, cascade-as-page.resolved), 17/17 tests, RESULTS.md Eleventy column filled.** Next: **Task 2.3b — spikes/bakeoff-astro** (the identical slice per the RESULTS.md checklist; template-kit consumed as direct helper imports instead of registerLiquid; theme layering via import aliases is ITS load-bearing bet), then Task 2.4 — A2 scoring + decision memo (margin <0.5/5 → Eleventy).
* **Current Phase:** Phase 2 — Frontend bake-off (A0 done; A1 candidate spikes next); Phase 1 functionally complete (Ian-owned publish gates outstanding)
* **Priority:** High
* **Last Updated:** 2026-07-06 7:30 PM
* **Notes:** Devkit vendoring proven on extension (585d7cc) and desktop (7c75970); runner-core adopted by extension (eaae359, checkpoint 7) and desktop (checkpoint 8, this change). Backend adoption waits for BEM harmonization (no dist layer yet). INCIDENT (resolved): checkpoint-5 commit initially ran in omega-manager by accident (stray shell cwd) — undone via mixed reset, Ian's working tree fully restored, nothing pushed; all git commands now use explicit `git -C`. Pending Ian: electron-manager@1.12.1 publish (his session), @omegajs org claim, GitHub remote creation.

## 📌 Active Task List
* [ ] Phase 0: Bootstrap the monorepo
  * [x] Task 0.1: Create repo skeleton (packages/, spikes/, apps/, docs/, package.json workspaces, .nvmrc, .gitignore, README, CLAUDE.md, PROGRESS.md, CHANGELOG.md)
  * [x] Task 0.2: Probe @omegajs npm scope (no packages exist; Ian to claim org on npmjs.com)
  * [x] Task 0.3: Initial commit (b9d9aed) + per-checkpoint commits on Ian's go
  * [x] Task 0.4: Install + configure changesets (@changesets/cli ^2.31.0, .changeset/ initialized)
  * [x] Task 0.5: Plain-copy backend-manager, web-manager, browser-extension-manager, electron-manager into packages/{backend,client,extension,desktop} (rsync minus .git/node_modules/.env/logs/lockfiles; secret scan clean — only BEM's demo-project test fixture; MAM excluded as parked)
  * [x] Task 0.6: Workspace install + suites GREEN in-place — client 73 passing; extension 85 passing; desktop 751 passing / 5 designed extended-mode skips (required 2 hoisting fixes: src/test/runners/electron.js + boot.js now resolve electron via require.resolve(paths:[projectRoot]) instead of hardcoded <root>/node_modules/electron — behavior identical for consumers); backend 2 passing = its complete standalone suite (real BEM corpus runs in consumers; emulator stack self-orchestrated successfully from the monorepo, proving java+firebase-tools env)
  * [x] Task 0.7: CI workflow written (.github/workflows/ci.yml — 4 suites w/ xvfb+java, pack→scratch-install smoke; CI verification pending first push to GitHub) + pack-smoke run LOCALLY: all 4 packages pack, scratch-install, and resolve OK
  * [x] Task 0.7b: 🔥 FOUND+FIXED production bug via pack-smoke — electron-manager@1.12.0 ON NPM fails every fresh consumer install (postinstall runs scripts/sync-nvmrc.js but `files` didn't ship scripts/). Fixed in monorepo copy (files += "scripts/"); verified: tarball ships script, install succeeds, postinstall syncs consumer .nvmrc. NOTE: live npm package still broken → 1.12.1 publish is the natural Task 0.8 gate candidate (Ian's go required).
  * [ ] Task 0.8: GATE — publish one old-name patch from the monorepo (electron-manager@1.12.1 with the files fix). **Ian is handling this himself in another session**; Phase 1 proceeds in parallel per his go ("commit and continue").
* [ ] Phase 1: Foundation packages
  * [x] Task 1.1a: @omegajs/devkit first files — logger (identical ×4, EM's cleaned copy canonical), safe-install (byte-identical ×4), attach-log-file (functionally identical ×4; header normalized to `# omega log`). Private workspace package, 17 unit tests green (node:test until the shared runner slice lands).
  * [x] Task 1.1b: Vendor-on-prepare mechanism (packages/devkit/tools/vendor.js) — copies devkit src → dist/vendor/devkit + rewrites requires to relative paths + guards that hosts declare vendored modules' runtime deps. Proven on extension: 3 shims adopted, suite 85 passing (unchanged), pack→scratch-install resolves + runs the vendored logger, shipped dist has zero raw @omegajs requires. CI: devkit suite added; pack-smoke gained the self-containment grep (the hard gate, since prepare-package after-hooks are non-blocking).
  * [x] Task 1.1c: Adopt devkit shims in packages/desktop — logger + safe-install + attach-log-file shimmed (Electron-specific logger-lite stays EM-owned); suite 751 passing / 5 designed skips (unchanged); pack-smoke resolves + self-contained + vendored logger runs in a bare consumer + scripts/sync-nvmrc.js still ships. **Backend deferred to its harmonization step (Task 1.3-adjacent)** — BEM has no dist layer yet, so there's nothing for the vendor hook to rewrite (its bin requires src/ directly).
  * [x] Task 1.1d-part1: devkit test-runner CORE extracted (src/test/runner-core.js — discovery, suite/group/standalone execution, filtering, skip semantics, _init hooks, reporting; UJM's canonical, parameterized by title/packageName/targetAlias/suitesDir/middleLayers/boot config) + test/assert.js (was byte-identical ×3) + extended-mode-warning factory. Extension adopted: runner.js is now ~90 lines of BXM-specific config + layer glue; suite 85 passing (unchanged); runner loads from packed tarball. Vendor tool upgraded: SELECTIVE vendoring (only modules the host's dist requires + transitive relative requires — a safe-install-only host ships 1 file and needs only node-powertools) + rewrites `require.resolve()` forms (chromium runner inlines devkit assert SOURCE into the browser harness). Devkit suite 30 passing. CI grep widened to catch require.resolve forms.
  * [x] Task 1.1d-part2: desktop adopts runner core — runner.js 529→89 lines of EM config + layer glue (main+renderer share one Electron spawn via runners/electron.js with harnessEntry; boot glue passes emDistRoot; bootDefaultTimeout 15000; runners now lazy-loaded like BXM so a missing electron doesn't block build-layer tests); assert.js + extended-mode-warning.js shimmed (EM's harness needed NO require.resolve inlining — main-entry requires ../assert.js which chains through the shim, boot-entry resolves it via runtime path-join, renderer-entry keeps its self-contained mini-expect). Suite 751 passing / 5 designed skips (unchanged); prepare vendors 6 modules / rewrites 6 files; pack-smoke: resolves, self-contained, runner+assert+warning all load from tarball, sync-nvmrc.js still ships. Framework-specific runners (chromium/electron/boot harnesses) stay framework-side — revisit sharing UJM↔BXM chromium plumbing when @omegajs/web is built.
  * [x] Task 1.2a: Sandbox brand monorepo + backend app — apps/sandbox-brand in the day-one brand-monorepo format (package.json workspaces apps/*; README documents the fresh-by-construction + demo-everything rules); apps/backend = real BEM consumer built from packages/backend's own scaffolding sources (templates/ + src/defaults/), demo-sandbox-brand emulator-only project, fresh fake service-account key, committed demo .env (re-included via .gitignore Custom Values), backend-manager linked via file: to packages/backend. **FULL BEM CORPUS GREEN vs the sandbox: 1203 passing / 0 failing / 44 designed skips (exit 0)** — the framework's complete routes/events/rules/email/mcp corpus now runs in the monorepo against a fresh consumer (previously only 2 boot smokes ran here). 🔥 DOGFOOD CATCHES (3): (a) REAL BEM BUG FIXED — rules tests ignored firebase.json's firestore emulator port (runner never passed it to the rules client; every rules test times out on a custom-port consumer); src/test/runner.js now passes emulatorPorts.firestore. (b) FINDING (logged): BEM doesn't truly support custom emulator ports — Manager.getApiUrl()/getFunctionsUrl() hardcode localhost:5002/5001 in dev/testing (contract-asserted by test/helpers/environment.js), test/mcp/* hardcodes 5002; payment/cron self-calls break on custom ports. Sandbox uses DEFAULT ports like every real consumer; harmonization candidate. (c) REAL BEM BUG FIXED — content/post 404 tests hit LIVE GitHub search in normal (non-extended) mode; route 500s without GH_TOKEN so any token-less consumer fails. Gated behind GH_TOKEN presence (mailbox-key static-skip pattern) in test/routes/content/post.js (operative) AND the test/functions/general/fetch-post.js legacy copy (test/functions/ only runs with --legacy). Minor parked: BEM's test path filter matches project tests but not corpus paths.
  * [x] Task 1.2b: Sandbox website app + cross-stack e2e harness + CI wiring — apps/sandbox-brand/apps/website is a minimal web-manager consumer (esbuild bundles src/assets/main.js embedding packages/client; deps resolve from the workspace root, no per-app install needed in-monorepo); `npm test` at the brand root runs e2e/run.js: builds the site, boots BEM's full emulator suite (`npx mgr emulator` in apps/backend/functions), serves the site, and drives real Chromium (puppeteer) through signup → BEM auth onCreate creates the Firestore user doc → signout → signin via web-manager → session persistence across reload → subscription resolution (plan basic/inactive). **11/11 steps green, 2 consecutive runs, ~90s each; clean teardown (all ports freed).** The `omega e2e` CLI grows from this harness later (kept sandbox-local until a second consumer earns the devkit extraction). CI: new `sandbox` job (setup-java + root npm ci + functions npm ci + `npm run test:backend` corpus + `npm test` e2e); verification pending first push like the rest of ci.yml. 🔥 DOGFOOD CATCHES (2 REAL web-manager BUGS FIXED in packages/client): (a) NO auth-emulator support — Firestore had a FIREBASE_EMULATOR_CONNECT path but auth never called connectAuthEmulator, so signin/signup vs an emulated backend silently hit LIVE Firebase Auth; (b) the firestore emulator connect lived only in the lazy firestore module while the auth module reads accounts via `manager.firebaseFirestore` directly, so emulator-mode account reads hit LIVE Firestore unless page code happened to touch `manager.firestore()` first. Fix for both: single connect point in `_initializeFirebase()` right where the auth/firestore instances are created (auth :9099 + firestore :8080, same gate `isDevelopment() && env.FIREBASE_EMULATOR_CONNECT`); duplicate block removed from firestore.js. Client suite 73 passing (unchanged). Design note (Ian can veto): the website app embeds packages/client directly rather than being a UJM consumer — UJM is external/read-only + Ruby, and @omegajs/web replaces this app's internals in Phase 2 anyway; the brand-monorepo slot + e2e contract are what's being locked in now.
  * [x] Task 1.3: @omegajs/account extraction (golden-master gate) — COMPLETE: extracted (1.3a), adopted by web-manager (1.3b) and BEM (1.3c); both drifted schema copies are gone
    * [x] Task 1.3a: packages/account created + golden master GREEN — schema engine extracted verbatim from BEM user.js with `$uuid`/`$randomId`/`$apiKey` generators INJECTED via ctx.generators (BEM will inject uuid4/Utilities().randomId(size 8)/uid-generator; frontend passes none → nulls); USER_SCHEMA as pure data; resolveSubscription = BEM superset (everPaid — WM's copy lacked it); resolveAccount(data, {generators, user}) with the frontend auth-overlay semantic; time context = plain Date (verified powertools.timestamp ≡ toISOString/floor(ms/1000) — package has ZERO runtime deps, browser-safe). Golden master compares against LIVE packages/backend user.js (not a snapshot): frozen Date via node:test mock timers, deterministic randomId, format-assert+sentinel for the 2 non-injectable BEM generators, deepStrictEqual + JSON.stringify (key-order) parity across 7 fixtures (empty, paid, trialing, expired-trial+cancelling, suspended, junk-type coercion, passthrough/template/strip) + resolveSubscription parity per fixture. **31/31 tests green; CI test job gained the account suite.**
    * [x] Task 1.3b: web-manager adopts @omegajs/account — vendor tool GENERALIZED (multi-package: scans dist for ANY `@omegajs/<pkg>` reference and vendors each into dist/vendor/<pkg>/; package roots resolved via require.resolve of the bare name — convention: entry is src/index.js; + ESM forms rewritten: `import…from` / `export…from` / dynamic `import()` / side-effect `import`, alongside the existing require/require.resolve; relative-dep walk + host-dep guard cover both syntaxes; devkit suite 31 passing incl. new ESM multi-package fixture that import()s the rewritten file and proves named-imports-from-vendored-CJS interop). Client flip: auth.js deleted DEFAULT_ACCOUNT + local resolveAccount deep-merge + resolveSubscription body → imports `@omegajs/account` (resolveAccount(rawData, {user}) overlay; resolveSubscription wrapper keeps only WM's storage-fallback glue; everPaid additive); package.json gained @omegajs/{account,devkit} devDeps + the vendor after-hook. GATES GREEN: client suite 74 passing (was 73; +everPaid test); cross-stack e2e 11/11 (doc-created marker flipped from metadata.created.timestamp — which the canonical engine now fills with `now` even for fallback accounts — to api.clientId, non-null ONLY when the read hits the BEM-written doc since the frontend injects no generators; e2e observed a real server-generated v4 uuid + everPaid:false asserted in the browser bundle); pack-smoke: web-manager tarball dist has ZERO raw @omegajs refs, ships dist/vendor/account/{index,engine,schema,subscription}.js, vendored code loads + resolves from a scratch install; extension suite 85 passing (sanity on the changed vendor path); CI pack-smoke grep widened to ESM forms. Watch item: the tarball still ships src/ with the bare `@omegajs/account` import and `"module": "src/index.js"` points there — modern bundlers (webpack 5/esbuild) honor the `exports` map → dist, but an exports-unaware consumer would hit src and fail LOUDLY (unresolvable specifier); revisit shipping src/ at the client cutover.
    * [x] Task 1.3c: BEM adopts @omegajs/account — user.js (408 lines: schema + engine + constructor) → ~50-line thin wrapper, ZERO call-site changes (`new User(Manager, settings).properties` + `User.resolveSubscription` preserved; wrapper injects the real generators: uuid v4, `Manager.Utilities().randomId({ size: 8 })`, uid-generator apiKey — the golden master's mock Manager asserts the exact randomId args). packages/backend gained @omegajs/{account,devkit} devDeps + the vendor after-hook. The src-deep-require problem (corpus tests load `../src/...` in consumers, where the private package can't resolve — flipping ~24 test files incl. _legacy to dist was rejected as too broad) solved INSIDE the wrapper with a try/catch fallback: try the bare specifier (monorepo workspace link — always-fresh for the golden master; rewritten to `../../vendor/account/index.js` in dist by the vendor hook), catch → `../../../dist/vendor/account/index.js` (same file at the same depth from BOTH src/ and dist/ copies, so consumers' src deep-requires land on the vendored copy). 🔥 VENDOR BUG FOUND+FIXED (TDD, failing test first): jetpack.find follows symlinks during traversal regardless of matching exclusions → the host-dist scan recursed through the fixture's circular `backend-manager → package root` symlink until ENAMETOOLONG. vendor.js now scans with a symlink-safe walker (fs.readdirSync withFileTypes; never follows symlinks, never enters node_modules or dist/vendor — none of that is host code to scan/rewrite); devkit suite 32 passing (+1 regression test with a circular-symlink + node_modules fixture proving fixture-dep @omegajs refs are neither vendored-for nor rewritten). GATES GREEN: account suite 31/31 (golden master now validates the wrapper glue — real generators format-asserted, key order byte-identical); BEM standalone 2 passing (exit 0); sandbox corpus 1203 passing (exit 0; middleware logs show real generated clientId/privateKey/affiliate.code flowing through the injected generators); cross-stack e2e 11/11 (the BEM-side api.clientId was generated by the WRAPPED user.js inside the emulator runtime); pack-smoke 8/8 incl. the NEW src-fallback check (scratch-install consumer deep-requires src/manager/helpers/user.js corpus-style → vendored account resolves; v4 clientId + randomId wiring + apiKey + resolveSubscription all asserted). Ships as an old-name v5 release.
  * [x] Task 1.4: BEM structure harmonization (src→dist + generated public/* + defaults parity — plan §6, old-name v5 releases) — COMPLETE (1.4a dist layer, 1.4b generated public/*, 1.4c defaults parity, 1.4d devkit shims)
    * [x] Task 1.4a: dist layer + bin flip — preparePackage un-defused (`./src_`/`./dist_` → `./src`/`./dist`; the config, prepare scripts, and offline-safe disposable-domains before-hook were already wired, just disabled), `main` → dist/manager/index.js, bin/backend-manager requires ../dist/cli/index.js, `start` → prepare:watch (matches the other frameworks), .gitignore gained /dist/, prepublishOnly now prunes the self-test fixture node_modules from BOTH src and dist. Safety verified BEFORE flipping: jetpack.copy preserves symlinks as symlinks (no recursion on the fixture's circular `backend-manager → package root` link; empirically tested) and npm pack excludes symlinks from tarballs. Path-depth audit: dist mirrors src at the same depth, so all `__dirname`-relative escapes (templates/, package root) resolve identically; only src-literal is watch.js's bemSrcDir (BEM dev watch still watches src — correct as the reload trigger since prepare:watch keeps dist synced; noted as dev-flow nuance). Consumers deep-requiring src/ still work (files ships src/ unchanged; the corpus itself requires ../src in ~10 test files). GATES GREEN: standalone suite 2 passing via bin→dist; full sandbox corpus 1203 passing / 1247 total (exit 0, 84s) via consumer `npx mgr`→dist; cross-stack e2e 11/11; pack-smoke: tarball 1.2M with 432 dist files + fixture symlinks excluded, scratch-install resolves main to dist/manager/index.js, installed `mgr version` runs from dist, self-containment grep clean. 🔥 LATENT BUG FIXED (checkpoint-10 wiring): sandbox `test:backend` used `--workspace=apps/backend`, but npm resolves the project root to the MONOREPO (sandbox-brand is itself a root workspace; workspace globs don't recurse) so the flag never resolved — the corpus had only ever been run from inside apps/backend directly. Now `cd apps/backend && npm run test:offline`; CI's sandbox job would have failed on first push without this.
    * [x] Task 1.4b: consumer `public/*` becomes generated + gitignored — COMPLETE, all 4 gates green. Built: `src/cli/utils/public-files.js` (SSOT generator: writePublicFiles overwrite-mode for setup, ensurePublicFiles fill-if-missing reading the consumer's JSON5 backend-manager-config.json for the brand url); setup-tests/public-html-files.js delegates to it; ensure wired into EmulatorCommand.startEmulators() (covers `mgr emulator` AND `mgr test` auto-start — a fresh clone has NO public/ since it's gitignored, and firebase-tools refuses to boot hosting without the folder) and into `mgr deploy` (bare deploy includes hosting); templates/_.gitignore gained `/public/` under "Hosting boilerplate (generated ...)"; standalone fixture pre-tracked its missing public/404.html (generated via the real helper) so the ensure never creates untracked strays; sandbox dogfooded the REAL `mgr setup` (gitignore smart-merge added `/public/` ✓, plus legit consumer updates: functions/.nvmrc created, projectScripts synced into functions/package.json, firestore.rules version stamp, required BEM indexes, emulator-config ui.enabled=true) and sandbox public/* untracked (git rm --cached). GATES ALL GREEN: ✅ fresh-clone corpus (rm -rf public → `mgr test` regenerates → 1203 passing exit 0); ✅ pack-smoke 11/11 (new: shipped gitignore template has /public/; ensurePublicFiles from a scratch consumer install generates from a JSON5 config with url substitution and refuses to overwrite existing files); ✅ standalone suite 2 passing (fixture tree stays clean — pre-tracked 404.html, no ensure strays); ✅ fresh-clone e2e 11/11 (rm -rf public → `mgr emulator` regenerates with the REAL brand url from the JSON5 config — found+fixed mid-checkpoint: the first ensure draft used strict JSON.parse, which silently fell back to url:'' on BEM's JSON5 config). Port-conflict interlude resolved (Ian closed somiibo serve on 5002; this session's 3 orphaned java emulators killed with his go). 🔥 FINDINGS (logged, not fixed): (a) full `mgr setup` CANNOT complete on an emulator-only consumer — firestore-indexes-synced fetches LIVE indexes (403 on demo-sandbox-brand), crashes with no summary, and leaves the raw error text in a stray `_firestore.indexes.json`; setup should skip live-project checks for `demo-*` project IDs (firebase-tools' offline convention) — harmonization candidate; (b) nvmrc-version's fix is deliberately two-phase (writes .nvmrc then throws so the user re-runs) — surprising on first setup of a fresh consumer; (c) `mgr test` teardown can orphan the java emulator grandchildren (firestore/database/pubsub survived with PPID 1).
    * [x] Task 1.4c: defaults expansion to parity — BEM's setup scaffolds through the devkit defaults engine. `copyDefaults`'s hand-rolled walk (archive-skip, `_.` renames, MERGEABLE_BASENAMES merge) → `src/utils/scaffold-defaults.js` (engine + BEM FILE_MAP: `'**/*': {overwrite:false}` + mergeLines for CLAUDE.md / `_.gitignore` / `functions/_.env`; exported for the self-test, mirrors BXM); setup.js prints per-file add/merge from the engine's `{written,merged}` return (quiet logger; no-op merges land in `skipped` so "All defaults up to date" stays honest). TREE 5→7 FILES: `templates/_.env` → `src/defaults/functions/_.env`, `templates/_.gitignore` → `src/defaults/_.gitignore` (git mv; engine `_.` strip + path placement puts them at `functions/.env` / `.gitignore`); env-file.js + gitignore.js setup checks retarget `dist/defaults/...` and KEEP their legacy fix paths (old `# BEM>>>` marker strip, functions/.gitignore removal, no-marker→custom migration). WORKFLOWS EXCLUDED from parity by evidence: zero real BEM consumers have .github/workflows (checked ultimate-jekyll/somiibo/studymonkey/slapform backends). 🔄 REVERSE FLOW INTO DEVKIT (standing permission): BEM's merge-line-files had evolved PAST the ckpt-22 canonical — custom-key PROMOTION (framework newly adopts a key the user had in Custom → promoted UP into Default with their value, Custom copy dropped; no more empty-in-Default + set-in-Custom duplicates) absorbed into @omegajs/devkit/merge-line-files + `hasSectionMarkers` export (BEM's setup validators consume it); one refinement over BEM's copy: keys in BOTH sections keep the Custom copy (dropping it would flip dotenv's last-wins effective value — new devkit test). BEM's src/utils/merge-line-files.js → standard shim; setup-tests helpers wrapper re-exports + marker aliases only. EM/BXM inherit promotion via vendored devkit (their merge tests pin nothing contrary — verified before flipping). BEM vendors devkit 2→4 (defaults-engine + merge-line-files); minimatch ^10.2.5 added to BEM deps (BARE_PATTERNS host-dep guard; lockfile delta = 1 line, already hoisted). NEW test/boot/defaults-scaffold.js (framework self-test layer, 4 tests vs mkdtemp dirs): fresh 7-file tree + renames + markers, value/custom preservation (.env + .gitignore), promote-up through the REAL template, run-2 idempotent + run-3 tree-checksum stable (BEM's template has no pre-marker preamble → instantly idempotent, no converge pass unlike EM). Sandbox dogfooded the LIVE `mgr setup`: defaults merge ran (`Merged functions/.env` — its explanatory preamble comment sat in the framework-owned Default section and was dropped per protocol; relocated to the Custom section where it's preserved, re-merge verified 0/0), checks 1–30 green incl. the retargeted [17] gitignore + [18] env; post-30 `firestore-indexes-synced` live-deploy crash on demo-* = the KNOWN parked 1.4b finding (pre-existing; its stray `_firestore.indexes.json` cleaned). GATES GREEN: devkit 69 (67 + 2 net: promote-up rewrite + both-sections + hasSectionMarkers), BEM standalone 6 passing (2 boot + 4 scaffold), sandbox corpus 1203 passing / 1247 total (exact baseline), cross-stack e2e 11/11, EM 755 passing / 5 designed skips + BXM 92 (both unchanged — promotion regression-neutral), pack-smoke-bem-defaults 5/5 (tarball ships dist/defaults + engine + scaffold-defaults, templates/ clean of moved files, self-containment grep, REAL scaffoldDefaults from the installed tarball: fresh + merge + promote-up + idempotent, helpers shim resolves through vendored devkit).
    * [x] Task 1.4d: BEM devkit shims (deferred from Task 1.1c, done ahead of 1.4b/c since the vendor hook was just proven in BEM) — src/cli/utils/{safe-install,attach-log-file}.js flipped to the standard one-line shims (same pattern as extension/desktop). Pre-flip diff confirmed both BEM copies were functionally identical to devkit's canonical versions (comment-only deltas + the already-accepted `# bem log` → `# omega log` header normalization). BEM has NO framework logger to shim — its console+file route logging is a BEM feature (wonderful-log), per the plan. Scope check: nothing in test/, templates/, or src/defaults requires these src paths, so no consumer ever loads the src shims (CLI always runs bin→dist, where the vendor hook rewrites them) — the user.js try/catch fallback stays a user.js-only special case. Vendor now ships account (4) + devkit (2), rewriting 3 dist files. GATES GREEN: standalone 2 passing (exit 0); sandbox corpus 1203 passing (exit 0); cross-stack e2e 11/11 (mgr emulator = the dist CLI path); pack-smoke 9/9 (new check: vendored shims load from a scratch consumer install with intact export shapes — safeInstall fn + callable attachLogFile with detach/createTee).
  * [ ] Task 1.5: @omegajs/config (Phase-1 step 4 — omega.json5 loader/validator/schema + framework flips). ⚠ PLAN DEVIATION (Ian, 2026-07-06): dual-read is CANCELLED — "no backwards compatible things needed. just implement and document the new way ... so that legacy brands can migrate over." Frameworks flip to omega.json5 outright; legacy configs are no longer read anywhere; migration = one-time file conversion per the mapping tables in docs/config.md.
    * [x] Task 1.5a: packages/config created — the package itself, no framework touches. `loadConfig(projectDir, target, {defaults})`: discovery (config/omega.json5, then functions/config/omega.json5 for standalone backends), brand-monorepo walk-up ({brand}/apps/{app} → brand root config, only when the brand root actually has one), five-layer agnostic deep merge (`defaults ← brand shared ← brand targets[t] ← app shared ← app targets[t]`; target sections overlay the TOP LEVEL, so any shared key inside a target entry overrides per-surface), merged `targets` map kept on the resolved config (key presence = enabled; `enabled` flag + getEnabledTargets()), no-target mode = whole-file merge (the disperse shape), hasOmegaConfig() as the frameworks' dual-read probe. HARD FAILS on raw files pre-merge: secret-shaped keys (plan regex `/(secret|privateKey|apiSecret)$/i`; key-NAME test — publishableKey/clientId/firebaseConfig.apiKey pass by design) in ANY target section throw; legacy brand-config `targets` ARRAY throws loudly (would silently mangle to `{0:'web'}` through the merge); JSON5 parse errors carry the file path. Schema findings return as `errors`, never thrown — EM's proven two-mode usage (audit throws, boot warns). Schema: EM's src/config/schema.js entry format + validator walker ported verbatim (runSchema; dropped EM's dead describe() helper); SHARED_SCHEMA seeded from EM's shared entries (brand/firebaseConfig/analytics/payment/sentry/oauth2/theme + targets sanity incl. unknown-target-name typo protection); TARGET_SCHEMAS.desktop seeded from EM's real schema with the plan's `platforms` rename, .backend seeded from the sandbox's REAL backend-manager-config.json (parent/github/reviews/marketing/blog/dataRequest — read, not guessed), web/extension/mobile empty by design (web fills in Phase 2; mobile reserved, MAM parked). SHARED_SECTIONS export = the disperse enumeration. Deliberately deferred: toSiteGlobal() (needs the real Jekyll site.* usage audit — lands with Phase-2 A0/template-kit), framework dual-read adoption (next slices; EM canary is the plan's gate). GATES GREEN: config suite 34/34 (merge/secrets/validate units + loader fixtures under .temp/ per the devkit convention); devkit 32 + account 31 re-run green after the workspace install refresh (lockfile diff = +15 lines, just the new workspace entry); CI test job gained the config suite step (verification still pending first push, like all of ci.yml).
    * [x] Task 1.5b: EM flips HARD to omega.json5 (first framework on the new config; no dual-read per Ian's deviation above). Load points: `Manager.getConfig()` (build/CLI side — the single choke point webpack snapshots into EM_BUILD_JSON, so packaged apps automatically carry the RESOLVED config) and `main.js initialize()` dev fallback both resolve via @omegajs/config (`loadConfig(dir,'desktop')`; missing file → seeded-empty `{brand:{},app:{}}` so non-consumer cwds still run CLI commands; validation reports what's missing). Boot + `gulp audit` validation → `validateConfig(config, {target:'desktop'})`; EM's src/config/schema.js + src/utils/validate-config.js DELETED (engine lives in @omegajs/config — EM's own schema was its seed). Per-OS `config.targets.*` → `config.platforms.*` everywhere (build.js getWindowsSignStrategy, audit.js MAS/snap warnings, build-config.js entitlements/arch/NSIS/snap reads, sign-windows.js cloud provider — the last one caught by the SUITE, not grep: multi-line `?.` chains hide from path greps). Defaults template rewritten as src/defaults/config/omega.json5 (shared sections top-level; app/platforms/autoUpdate/startup/releases/downloads/remoteConfig/restartManager under targets.desktop; all teaching comments preserved); consumer workflow one-liners (snap gate, windows signing strategy) now use the NEW `electron-manager/config` package export → dist/vendor/config/index.js (canonical resolution, brand-monorepo aware) instead of raw JSON5 reads; boot fixture + audit/get-config/config-schema/validate-config/manager/release-pipeline/build-config suites flipped to the new shape (validate-config.test.js rewritten as the @omegajs/config integration surface — engine semantics live in the config package's own 34 tests). EM docs swept (17 files: filename + platforms renames; config-schema.md + CLAUDE.md + defaults CLAUDE.md restructured for the shared schema); NEW monorepo docs/config.md = format spec + per-framework migration mapping tables (EM done; BEM/UJM/omega-manager rows staged for their flips). GATES GREEN: EM suite 751 passing / 5 designed skips (exactly the pre-flip baseline; 1 intermediate failure was the sign-windows cloud-provider path — fixed, full re-run clean); pack-smoke 9/9 (tarball ships vendored config ×6 + omega.json5 template, NO legacy electron-manager.json, self-containment grep clean, `require('electron-manager/config')` resolves from a scratch install, workflow-style strategy read works, installed getConfig() resolves + derives appId/productName, secret-shaped key hard-fails from the installed package, sync-nvmrc.js still ships); config suite 34/34 re-run. NOTE for EM consumers at publish: this ships as the config-flip release — existing consumers must convert config/electron-manager.json → config/omega.json5 (docs/config.md table) when they update.
    * [x] Task 1.5c: BEM flips HARD to functions/config/omega.json5 (second framework; biggest config surface). LOADER FIRST: @omegajs/config gained the functions-dir normalization — BEM's runtime cwd is the functions/ dir (Cloud Functions + emulator both boot there), so findBrandConfigPath now treats an app root as one level up when basename === 'functions'; new load test proves `loadConfig(functionsDir,'backend')` ≡ `loadConfig(appRoot,'backend')` incl. brand walk-up + firebaseConfig from the brand layer (config suite 34→35). MANAGER: init() resolves consumer config via loadConfig(cwd,'backend', {defaults}) where defaults = templates/config/omega.json5 RESOLVED through the same loader (targets stripped) — the template file was git-mv'd + reshaped (shared top-level, parent/github/marketing/blog/reviews/dataRequest under targets.backend; killed a latent duplicate `parent` key JSON5 was silently last-winning); missing consumer config → defaults only (same tolerance as before); boot warns via formatErrors on schema findings (audit-throws/boot-warns contract); REMOVED: backendManagerConfigPath option, self.project.backendManagerConfigPath (+ its options.log line at :384 — greps missed it, caught by the post-flip residue grep), requireJSON5 helper, lodash mergeWith/isArray imports, JSON5 import, legacy `app.id → brand.id` fallback (no-backwards-compat). CLI: setup.js loads RESOLVED config WITHOUT defaults (so the template comparison stays meaningful) via namespaced `omegaConfig.*` (its own loadConfig method collides), scaffolds templates/config/omega.json5 → functions/config/omega.json5, bemConfigJSON → omegaConfigJSON everywhere; setup-tests: bem-config.js → omega-config.js (git mv; compares resolved-vs-resolved, fix() messages point at targets.backend), project-id-consistency checks the RESOLVED projectId (brand-level firebaseConfig counts) + fixes by writing an app-level override, public-html-files rides the rename; test.js loadProjectConfig + self-test fixture key injection through the loader; firebase-init resolveProjectId + public-files ensurePublicFiles through the loader (hasOmegaConfig-guarded). FIXTURE: src/test/fixtures/firebase-project functions/config/omega.json5 (JSON5, targets.backend enablement, backend_manager admin keys stay top-level custom). SANDBOX DOGFOODS THE HIERARCHY: shared sections (brand/firebaseConfig/analytics/payment/sentry/oauth2) moved UP to apps/sandbox-brand/config/omega.json5 (brand level, targets {web:{}, backend:{}} enablement), backend app file = targets.backend only — the corpus + e2e now exercise the five-layer chain + functions-dir walk-up in a real consumer on every run. package.json: devDeps + @omegajs/config; vendor hook now ships config ×6 into BEM dist (rewrote 10 files); EM dist re-vendored so its copy carries the walk-up fix. Docs: BEM CHANGELOG [Unreleased] BREAKING entry with the 5-step consumer migration recipe; README config section rewritten in the new shape (killed a stale example that showed `googleAnalytics.secret` IN the config — the new loader would hard-fail it; backendManagerConfigPath row dropped); CLAUDE.md, defaults CLAUDE.md (+ sandbox mirror), 13 docs/*.md swept contextually (consent parent under targets.backend, migration.md step 7 = convert-then-edit, directory-structure/key-files templates paths); docs/config.md BEM table → DONE + functions-dir rule documented in Resolution; monorepo CHANGELOG entry. NOT touched: test/_legacy/* (2 refs to the dead backendManagerConfigPath option — off-limits dir, scripts are not in the suite). GATES GREEN: config suite 35 (fail 0), BEM self-test 2 passing (Manager booted in the emulator functions runtime off the new fixture config), sandbox corpus 1203 passing / 44 skipped (EXACT pre-flip baseline, now THROUGH the brand walk-up), cross-stack e2e 11/11, pack-smoke-bem-config 9/9 (vendored config ships, omega.json5 template ships + legacy template ABSENT, CI self-containment grep clean, functions-dir walk-up + secret hard-fail + template-defaults resolution all proven from a scratch install, bin runs from dist).
    * [x] Task 1.5d: BXM flips HARD to config/omega.json5 (third framework; smallest flip — targets.extension near-empty by design; NO loader changes needed, the generic apps/-walk-up already covers extension apps). MANAGER: build.js getConfig() → hasOmegaConfig-guarded loadConfig(cwd, 'extension').config (missing file → {} — EM's tolerance semantic, replacing the old crash-on-missing JSON5.parse; schema findings console.warn ONCE per process via a module flag, because ~9 gulp tasks each call getConfig() at require time and BXM has no separate audit surface). Config flows unchanged to all gulp tasks + the BXM_BUILD_JSON runtime snapshot. SECRET KILLED: the defaults template carried `analytics.providers.google.secret` (the LAST secret-in-config in any framework — the new loader hard-fails it by design); moved to .env as GOOGLE_ANALYTICS_SECRET (BEM/EM convention; _.env defaults template gained the entry; package.js bakes it into build.json/BXM_BUILD_JSON from process.env at build time — same value flow web-manager's GA4 measurement-protocol path expects via `analytics.googleSecret`; dotenv already loaded by cli.js + gulp/main.js). TEMPLATE: src/defaults/config/browser-extension-manager.json git-mv'd → omega.json5 + reshaped (EM-style header comments, shared sections top-level, `targets: { extension: {} }` presence-enablement). DEFAULTS TASK: FILE_MAP key renamed (overwrite+merge semantics kept) + REAL BUG FIXED: mergeConfigs started from the template and only walked template keys, so consumer-only keys (liveReloadPort, anything under targets.extension) were silently DELETED on every `bxm setup` — latent data-loss made contract-breaking by the new shape (targets.extension is DESIGNED to hold consumer keys); mergeNested now copies source-only keys at every level. EXPORTS: package.json gained `./config` → dist/vendor/config/index.js (BXM's exports-map test suite auto-generates a test per entry — that's the +1 in the suite count) + @omegajs/config devDep; vendor hook (already wired) ships config ×6 into dist. TESTS: manager.test.js stages config/omega.json5 — old getConfig test replaced by 3 (target overlay onto top level + custom-key passthrough incl. JSON5 comment, missing → {}, secret hard-fail). Docs: BXM CHANGELOG [Unreleased] BREAKING (5-step migration recipe incl. the secret→.env move + brand-monorepo note) + Added (config export) + Fixed (merge bug); README/CLAUDE.md/9 docs swept (managers.md + hooks.md wording upgraded parsed→RESOLVED); defaults CLAUDE.md line 50 rewritten in the new shape; docs/config.md BXM mapping table added (DONE checkpoint 20) + Consumer-access section lists the BXM export; monorepo CHANGELOG entry. A/B methodology note: baseline 85 verified by stash-roundtrip (HEAD+fresh-dist = exactly 85; flip = 88 = 85 + 2 net new getConfig tests + 1 exports-map auto-test). GATES GREEN: BXM suite 88 passing (fail 0), config suite 35 (fail 0, untouched this checkpoint), pack-smoke-bxm-config 9/9 (vendored config ×6 ships, omega.json5 defaults template ships + legacy file ABSENT from tarball, CI self-containment grep clean, installed Manager.getConfig resolves brand walk-up from apps/{brand}/apps/extension + targets.extension overlay, secret hard-fail, `browser-extension-manager/config` export resolves, shipped template resolves clean+enabled, bin runs from dist).
  * [ ] Task 1.6: devkit remaining slices (plan Phase-1 step 5; risk order cli-router → defaults engine → build-task factories; the defaults engine unblocks Task 1.4c)
    * [x] Task 1.6b: defaults-engine slice — devkit gained `src/defaults-engine.js` (`applyDefaults({ defaultsDir, outputDir, fileMap, files, transform, logger })` → `{ written, merged, skipped }`): minimatch FILE_MAP (dot:true, last-match-wins) with overwrite/skip (bool|fn), name/path (fn), template (tolerant `{{ key.path }}` — EM's renderer is the standard, GitHub `${{ }}` survives), merge (JSON5 defaults merge — BXM's semantics incl. the checkpoint-20 consumer-key fix, exported as `mergeJson5Defaults`), mergeLines (marker protocol); built-ins: `_.` segment strip, archive-dir skip (non-final `_x`, verified safe — BXM tree has NO `_x/` dirs, `test/_init.js` is a FILE and ships), `.gitkeep` → dir-only, `.DS_Store` skip, write-only-if-changed, binary passthrough (BXM's ext list), symlink-safe walk. PLUS `src/merge-line-files.js`: EM's evolved copy verbatim (canonical — .env double-quote normalization, order-safe substitution, pre-marker lines treated as default; BXM's older inline variant DROPPED pre-marker content silently). EM ADOPTED: `copyDefaults(targetDir?)` → engine call owning only the file map (catch-all preserve, 3 marker merges, always-re-render YMLs; `.nvmrc` rule dropped — no such default file, sync-nvmrc.js owns consumer .nvmrc); `utils/merge-line-files.js` → standard shim; exported copyDefaults + NEW build/defaults-scaffold suite (4 tests vs temp dirs: renames/archive/workflow-render, preserve, .env migrate+survive, converge-then-stable — first re-merge normalizes EM's pre-marker `_.env` preamble below the marker, pre-existing protocol behavior). BXM ADOPTED: gulp task keeps shell+watcher, guts → exported `scaffoldDefaults({ outputDir, files })`; FILE_MAP sheds the `_.` name-fns + `.DS_Store` rule (built-ins); global `[ site.x ]` bracket pass on html/md/liquid/json/yml/yaml became the engine `transform` hook (createTemplateTransform util untouched — distribute still streams it); `.nvmrc` template normalized `v{ versions.node }/*` → `v{{ versions.node }}/*` (CAUGHT pre-flip: single-brace node-powertools tokens vs the `{{ }}` engine standard; rendered output byte-identical `v22/*`); NEW build/defaults-scaffold suite (4 tests incl. the omega.json5 consumer-key regression THROUGH the engine + converge — JSON5 comments strip on first merge, pre-existing). UNBLOCKS Task 1.4c. GATES GREEN: devkit 67 (+21: 9 merge-line ported cases + 12 engine), EM 755 passing / 5 designed skips (760 total = 756 + 4), BXM 92 (88 + 4), pack-smoke-defaults-engine 6/6 (both tarballs ship engine+merge vendored — devkit ×9 now; EM's REAL copyDefaults scaffolds AND re-merges from the installed package preserving a custom .env value; BXM's vendored engine scaffolds the shipped defaults tree — brand.id + liveReloadPort survive the omega.json5 merge, theme arrives; self-containment greps clean; both bins dispatch).
    * [x] Task 1.6a: cli-router slice — `createCliRouter({ commandsDir, aliases, defaultCommand='setup' })` (src/cli-router.js + `./cli-router` export) extracted from the drift-identical ×3 UJM/BXM/EM router: positional command → alias table → flag-style alias → default; loads `commands/<name>.js`; missing command file errors cleanly; command errors log with the command name + rethrow to the bin. `Main.config` exposes the resolved dispatch table for structure tests (no more source-scraping). ADOPTED BXM + EM: each src/cli.js → thin wrapper owning only its alias table + commandsDir (dotenv line stays framework-side; EM's bin-side ELECTRON_RUN_AS_NODE strip untouched); the vendor scan auto-picked up the new module (devkit ×7 into both dists). EM's build/cli.test.js alias-integrity test was the ONLY breakage (it regex-scraped `const ALIASES` from dist/cli.js) — rewritten to introspect `Main.config` from the installed class, now also covering defaultCommand; suite count unchanged. Normalization (licensed): EM's error line gains the command name (BXM's variant = the shared standard). BEM DOCUMENTED as NOT this router — colon-style utility commands dispatching stateful command classes is a different design, not duplication (devkit CLAUDE.md modules table says so). GATES GREEN: devkit suite 46 (+10: 9 router tests w/ fixture command modules + Main.config exposure), BXM 88 (unchanged), EM 751 passing / 5 designed skips (756 total unchanged), pack-smoke-cli-router 8/8 (router ships vendored in both tarballs; positional `version` + flag `-v` dispatch from scratch installs of BOTH bins; unknown command exits 1 with `Command "nope" not found`; Main.config introspection from installed dists; both dists self-containment-grep clean).
* [ ] Phase 2: Frontend bake-off → @omegajs/web (plan §4)
  * [x] Task 2.1: A0 baseline + shared corpus — spikes/bakeoff-shared created (private workspace package). **REAL somiibo-website Jekyll baseline MEASURED FIRST (read-only): 332s cold `npm run build`** — jekyll 2.98min (self-reported 178.23s; 1,030 posts + 105 pages + defaults → 2,608 HTML files), sass+PurgeCSS 1.99min, imagemin 1.82min (WARM cache: 1,783/1,936 hits from cache-uj-imagemin), minifyHtml 24s, webpack 12s, translation 0.4s (warm cache), clean+setup ~9s (tasks overlap; M1 Max/10-core/64GB, ruby 4.0.0/jekyll 4.4.1, UJM 1.9.31 = npm latest so no setup self-update; site resolved via omega-manager `npm run brands -- --path=website`; git status delta after run = the SAME 4 pre-existing dirty files, zero new tracked modifications; note: UJM's build pushes its imagemin cache to the site's dedicated `cache-uj-imagemin` branch — the pipeline's own designed behavior). Full method + env + content-shape + targets in BASELINE.md. CONTENT SHAPE MEASURED (corrected the plan's guess): posts live in src/_posts/{2017..2026,other,seo} (1,030 — the "1031" was this ±1); pages are NOT mostly blueprint pages — 59× platform-bot + 32× solution + 5× package CUSTOM-layout data-frontmatter pages (70–120-line YAML: nested card arrays, HTML-in-YAML, 199 `{{ site.* }}` refs = the variable-resolver load) + 6× theme-bracket .html + 3 blueprint singles; collections = 20 _alternatives only (_team/_updates empty). GENERATOR: src/corpus-spec.js = SSOT of every measured number; generate-corpus.js (seeded mulberry32, byte-reproducible, no Date.now/Math.random) emits _posts (exact per-dir counts, YYYY-MM-DD-slug.md convention, word quartiles generated {360,891,1072,1852,7566} vs real {346,875,1017,1797,7522}), pages (exact archetype mix into the real subdir tree), _alternatives, site-data.json (the candidates' site.* source), corpus-manifest.json (deterministic, no timestamps); corpus/ output gitignored — the generator is the artifact. bench.js = hyperfine-style repeated-run harness (zero deps; hyperfine not installed). RESULTS.md = A2 scorecard skeleton (weights, <0.5/5→Eleventy rule, A1 slice checklist incl. HTTPS dev-server decision). Bug caught by the suite: solution-page labels collided (adj/noun pools cycled in lockstep → 32 solutions collapsed to 12 unique paths, files silently overwritten) — fixed with lap-shifted noun index; test asserts 99 md + 6 html exactly. GATES: bakeoff-shared suite 4/4 (spec counts + per-dir distribution, byte-determinism twice + seed sensitivity, filename/frontmatter contract, layout mix + stressor + site-data), devkit 69 re-run green, lockfile delta 14 lines (new workspace only).
  * [x] Task 2.2: @omegajs/template-kit + toSiteGlobal() — COMPLETE. AUDIT FIRST (all read-only): jekyll-uj-powertools = 13 filters (lib/filters/main.rb) + 16 tags (4 registered UNPREFIXED: iftruthy/iffalsy/iffile/urlmatches) + variable_resolver helper; usage ranking across UJM src + somiibo src: uj_icon 612, iftruthy ~370, uj_content_format 97, iffalsy 72, uj_liquify 71, uj_member 62 … (uj_increment_return/uj_random/append_param/iffile near-zero but ported anyway — complete surface); Jekyll-specific filter usage is TINY (only slugify/date_to_xmlschema/jsonify/strip_html/markdownify/push — somiibo uses ZERO); site.* usage: brand 265/url 161/theme 101 + socials/download/translation/extension/analytics/web_manager… = the resolved omega.json5 shape (engine-owned posts/pages/data/time excluded). BUILT packages/template-kit (private, CJS, node:test): src/filters.js (10 pure + 3 factories: createLiquify/createContentFormat/createIncrementReturn), src/tags/{conditionals,content,media,collections}.js + index (engine-neutral defs `{block, render(ctx, markup, renderInner)}`; ctx = lookup/page/site accessors/options; icon+logo SVG roots INJECTABLE — Ruby hardcoded node_modules/ultimate-jekyll-manager; member/post image-tag calls shared buildImageHtml() directly instead of re-parsing a {% uj_image %} string), src/jekyll-compat.js (slugify, date_to_xmlschema, date_to_rfc822, jsonify, strip_html, number_of_words, subset where_exp/group_by_exp + relative_url/absolute_url/markdownify factories), src/register-liquid.js (LiquidJS adapter: getSync-based lookup, getAll-scope async liquify, WeakMap increment counter, block-tag token collection, buildTagCtx), src/data/* MACHINE-EXTRACTED from the Ruby (184 languages incl. escape handling, 43 flag mappings, 15 social patterns — regenerate-don't-hand-edit headers). RUBY PARITY PINNED vs real ruby 4.0.0: uj_hash byte-exact (994@hello/1000, 305@somiibo/360 — BigInt full-hexdigest), title_case confirmed, uj_commaify DELIBERATE divergence (Ruby groups decimal digits '1234.5678'→'1,234.5,678' = latent bug; ours '1,234.5678'; unknown-language echo is DOWNCASED like Ruby). toSiteGlobal(resolvedConfig) in packages/config src/site-global.js (+ ./site-global export): identity map, strips targets/enabled, url := url||brand.url, baseurl default — 3 new tests. Docs: docs/template-kit.md (inventory tables, adapter contract, deliberate-exclusions: generators/hooks are ENGINE features for A1). GATES: template-kit 41/41 (filters incl. parity pins; tag semantics vs fake ctx incl. SVG fixtures for icon brands-fallback/flag-mapping/logo id-prefixing; full adapter vs REAL liquidjs 10.27.1 — one first-run failure was a WRONG TEST expectation, code was Ruby-correct), config 38 (35+3), BXM 92 re-run (fastest vendored-config consumer — new file flows through prepare/vendor cleanly; site-global not required by any framework dist so no re-vendor needed), CI + template-kit & bakeoff-shared steps (bakeoff step was missed in ckpt 24 — wired now).
  * [ ] Task 2.3: A1 candidate spikes — bakeoff-eleventy + bakeoff-astro build the identical slice against the corpus (checklist in spikes/bakeoff-shared/RESULTS.md)
    * [x] Task 2.3a: spikes/bakeoff-eleventy — the COMPLETE A1 slice on Eleventy 3.1.6. **COLD FULL BUILD 3.60s mean (σ0.025, ×3+warmup) vs 332s Jekyll = ~92×**; 1,279 HTML files (1,030 posts + 105 pages + 20 alternatives + 103 blog-index + 18 taxonomy + 3 defaults); phases: esbuild+sass 0.60s / Eleventy 2.64s / PurgeCSS 0.42s; watch reload ~1.9–2.0s (CAVEAT recorded: --incremental doesn't narrow under collection-paginated templates — all 1,279 re-render; B-phase optimization). LOAD-BEARING BETS ALL VALIDATED (probed FIRST, before the slice): v3 virtual templates as LAYERED layouts (zero copying; cross-layer layout chaining; extensionless Jekyll names resolve against virtuals — TemplateLayoutPathResolver consults virtualTemplates), jekyllInclude + root:-array layered includes, registerLiquid via amendLibrary, data cascade IS page.resolved (layout frontmatter deep-merges under page frontmatter natively; eleventyComputed.resolved alias + mechanical `page.resolved.`→`resolved.` rewrite covers UJM's 967 refs — `layout.*` refs measured ZERO). Symlink FARM = the DEV mode (virtual content is captured at config time → not watchable; farm must live OUTSIDE the input dir or Eleventy processes the symlinks as content). ENGINE CORE src/omega-web.js: layered theme chain (active→classy→core), frontmatter {{ site.* }} preprocessor (cached per raw string; skips permalink/pagination + machinery keys — walking the full cascade crashes on other templates' raw content; WeakSet cycle guard), legacy `[ site.theme.id ]` bracket-layout via addLayoutAlias TABLE (layout values resolve BEFORE preprocessors — a data transform can never catch them), Jekyll conventions (dated filenames → /blog/:slug/ via fileSlug; /about → /about/; timezoneOffset 0 = CI-parity dates), collections + taxonomy from post.categories/tags, consumer-suppressible virtual DEFAULT pages (signin/signup/404/blog/blog-category/blog-tag/about; suppression = cheap permalink scan — corpus about.md verified to beat the default). SLICE: blueprint index/pricing (Liquid resolve-plan math: $8/mo billed annually, save 33%), auth pages bundling the REAL @omegajs/client via `web-manager` esbuild alias (3.3MB IIFE, bundles clean), dusk theme (overrides head include + 404 layout + _variables.scss; classy fills gaps), 3-layer page modules (site>theme>core, content-hashed manifest, pageModule frontmatter cascades from layouts), sass layering via `omega:` SCHEME importer (REAL FINDING: bare @use resolves file-relative BEFORE loadPaths — loadPaths can never layer), PurgeCSS post-pass (unused selectors stripped, verified). HTTPS DEV DECIDED: dev-server native https:{key,cert} (+http/2) w/ mkcert — no BrowserSync proxy. GATES: spike suite 17/17 (13 slice invariants through REAL Eleventy incl. dusk layering + farm-vs-virtual byte-parity — needed setUseTemplateCache(false), Eleventy's module-level layout cache leaks across same-process builds; 4 asset tests incl. real-client bundle + purge), full corpus build + spot-checks (plans/icons/readtime/taxonomy/manifest wiring), CI + bakeoff-eleventy step, RESULTS.md Eleventy column + checklist filled, spike README = architecture + 8 migration findings.
  * [ ] Task 2.4: A2 scoring + decision memo (RESULTS.md scorecard; margin <0.5/5 → Eleventy)

## ✅ Completed Task List
